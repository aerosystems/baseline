---
title: "Поняття паралельного виконання"
type: lecture
order: 1
preview: "Паралелізм vs конкурентність, потоки виконання, основи багатопоточності."
---

## Чому ваш комп'ютер робить 100 справ одночасно

Відкрийте прямо зараз диспетчер задач (Windows) або htop (Linux). Скільки процесів ви бачите? 200? 300? 500?

А тепер подивіться на характеристики свого процесора. Скільки ядер? 4? 8? Навіть якщо 16 — це все одно набагато менше, ніж кількість процесів.

**Як комп'ютер з 4 ядрами "одночасно" виконує 400 процесів?**

Відповідь на це питання — фундаментальна концепція операційних систем, яка визначає, як працює кожен сучасний пристрій: від смартфона до суперкомп'ютера.

```
┌───────────────────────────────────────────────────────────────────────┐
│                         ІЛЮЗІЯ ОДНОЧАСНОСТІ                           │
│                                                                       │
│   Ваш комп'ютер прямо зараз:                                          │
│                                                                       │
│   ┌─────────────────────────────────────────────────────────────────┐ │
│   │  Браузер (10+ вкладок) ████████████████████                     │ │
│   │  Spotify (музика)      ██████████                               │ │
│   │  VS Code (редактор)    ████████████████                         │ │
│   │  Telegram              ████████                                 │ │
│   │  Discord               ██████                                   │ │
│   │  Антивірус             ████                                     │ │
│   │  Системні служби       ████████████████████████████████████     │ │
│   │  ... ще 200+ процесів                                           │ │
│   └─────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│                          ▼                                            │
│                                                                       │
│   ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐                     │
│   │  Core 0 │ │  Core 1 │ │  Core 2 │ │  Core 3 │   ← 4 ядра          │
│   └─────────┘ └─────────┘ └─────────┘ └─────────┘                     │
│                                                                       │
│   Як 4 ядра обслуговують 400+ процесів? Магія? Ні — планування!       │
└───────────────────────────────────────────────────────────────────────┘
```

**Запитання до аудиторії**: Уявіть, що ви — єдиний офіціант у ресторані з 50 столиками. Як ви обслуговуватимете всіх клієнтів "одночасно"?

## Паралелізм vs Конкурентність

Це два терміни, які часто плутають, але вони означають різні речі. Розуміння різниці — ключ до глибокого знання операційних систем.

### Аналогія: Кухня ресторану

```
┌───────────────────────────────────────────────────────────────────────┐
│                    КОНКУРЕНТНІСТЬ vs ПАРАЛЕЛІЗМ                       │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   КОНКУРЕНТНІСТЬ (Concurrency)              ПАРАЛЕЛІЗМ (Parallelism)  │
│   ─────────────────────────────             ────────────────────────  │
│                                                                       │
│   Один кухар готує 5 страв:                 5 кухарів готують 5 страв:│
│                                                                       │
│   ┌─────────────────────────┐               ┌─────┬─────┬─────┬──────┐│
│   │  Кухар                  │               │Кух 1│Кух 2│Кух 3│Кух 4 ││
│   │  ┌───┐                  │               ├─────┼─────┼─────┼──────┤│
│   │  │ 1 │ ← готує борщ     │               │Борщ │Салат│ М'со│Десерт││
│   │  └───┘                  │               └─────┴─────┴─────┴──────┘│
│   │    │ перемикається                                               ││
│   │  ┌─▼─┐                  │               Справжнє одночасне        │
│   │  │ 2 │ ← ріже салат     │               виконання!                │
│   │  └───┘                  │                                         │
│   │    │ перемикається      │               Кожна страва готується    │
│   │  ┌─▼─┐                  │               на своїй "станції"        │
│   │  │ 3 │ ← смажить м'ясо  │               незалежно від інших.      │
│   │  └───┘                  │                                         │
│   │    │ ... і так далі     │                                         │
│   └─────────────────────────┘                                         │
│                                                                       │
│   Прогрес у кількох задачах,               Виконання кількох задач    │
│   але в один момент часу —                 буквально в один момент    │
│   лише одна активна.                       часу.                      │
│                                                                       │
│   Потрібен: 1 виконавець                   Потрібно: N виконавців     │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

### Формальні визначення

| Концепція | Визначення | Приклад |
|-----------|------------|---------|
| **Конкурентність** | Структура програми, що дозволяє працювати з кількома задачами, які можуть прогресувати одночасно | Один процесор перемикається між браузером і музичним плеєром |
| **Паралелізм** | Фактичне одночасне виконання кількох задач | 4-ядерний процесор виконує 4 різних обчислення одночасно |

**Rob Pike (творець Go):**
> "Concurrency is about dealing with lots of things at once. Parallelism is about doing lots of things at once."
>
> "Конкурентність — це про керування багатьма речами одночасно. Паралелізм — це про виконання багатьох речей одночасно."

### Конкурентність без паралелізму

На одноядерному процесорі:

```
┌───────────────────────────────────────────────────────────────────────┐
│              КОНКУРЕНТНІСТЬ НА ОДНОМУ ЯДРІ (Time-slicing)             │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   Процес A:  ████░░░░████░░░░████░░░░████                             │
│   Процес B:  ░░░░████░░░░████░░░░████░░░░                             │
│   Процес C:  ░░░░░░░░░░░░░░░░████████████  (чекає на I/O, потім run)  │
│              ────────────────────────────────────────────► Час        │
│              │    │    │    │    │    │    │                          │
│              0   10   20   30   40   50   60 ms                       │
│                                                                       │
│   Кожен процес отримує "квант часу" (time slice), типово 1-10 мс      │
│   Перемикання настільки швидке, що здається одночасним!               │
│                                                                       │
│   Це називається: ВИТІСНЯЮЧА БАГАТОЗАДАЧНІСТЬ                         │
│                   (Preemptive Multitasking)                           │
└───────────────────────────────────────────────────────────────────────┘
```

### Паралелізм з конкурентністю

На багатоядерному процесорі:

```
┌───────────────────────────────────────────────────────────────────────┐
│                  ПАРАЛЕЛІЗМ НА 4 ЯДРАХ                                │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   Ядро 0:  ████████████████████████████████████████████████           │
│            (Процес A виконується безперервно)                         │
│                                                                       │
│   Ядро 1:  ████████████████████████████████████████████████           │
│            (Процес B виконується безперервно)                         │
│                                                                       │
│   Ядро 2:  ████████░░░░████████░░░░████████░░░░████████               │
│            (Процеси C та D діляться ядром)                            │
│                                                                       │
│   Ядро 3:  ████░░░░░░░░░░░░░░░░████████████████████████               │
│            (Процес E чекав на I/O, потім працює)                      │
│                                                                       │
│              ────────────────────────────────────────────► Час        │
│                                                                       │
│   Справжнє паралельне виконання: A, B, C одночасно в момент t=10      │
└───────────────────────────────────────────────────────────────────────┘
```

### Практична перевірка

```bash
# Скільки ядер у вашому процесорі?
nproc                    # Linux: кількість логічних ядер
lscpu | grep "CPU(s)"    # Детальна інформація

# або
cat /proc/cpuinfo | grep "processor" | wc -l

# Скільки процесів зараз у системі?
ps aux | wc -l

# Завантаження ядер у реальному часі
htop                     # Натисніть F2 → Display → Show CPU usage
```

**Запитання**: Якщо у вас 8 ядер і 300 процесів, скільки з них виконуються паралельно в даний момент? (Максимум 8!)

## Потоки виконання (Threads)

### Процес vs Потік

Ми вже знаємо, що **процес** — це програма, що виконується. Але всередині процесу може бути кілька **потоків** (threads).

```
┌───────────────────────────────────────────────────────────────────────┐
│                       ПРОЦЕС vs ПОТІК                                 │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   ПРОЦЕС (Process)                    ПОТІК (Thread)                  │
│   ─────────────────                   ──────────────                  │
│                                                                       │
│   ┌─────────────────────┐             ┌─────────────────────┐         │
│   │ Процес A (PID 1234) │             │ Процес A (PID 1234) │         │
│   │                     │             │                     │         │
│   │  ┌───────────────┐  │             │  ┌───┐ ┌───┐ ┌───┐  │         │
│   │  │   Код         │  │             │  │T1 │ │T2 │ │T3 │  │ ← Потоки│
│   │  │   Дані        │  │             │  └─┬─┘ └─┬─┘ └─┬─┘  │         │
│   │  │   Heap        │  │             │    │     │     │    │         │
│   │  │   Stack       │  │             │    ▼     ▼     ▼    │         │
│   │  └───────────────┘  │             │  ┌───────────────┐  │         │
│   │                     │             │  │ Спільні:      │  │         │
│   └─────────────────────┘             │  │ • Код         │  │         │
│                                       │  │ • Дані        │  │         │
│   Один потік виконання                │  │ • Heap        │  │         │
│                                       │  │ • Файли       │  │         │
│                                       │  └───────────────┘  │         │
│                                       │                     │         │
│                                       │  Окремі для кожного:│         │
│                                       │  • Stack            │         │
│                                       │  • Регістри CPU     │         │
│                                       │  • Program Counter  │         │
│                                       └─────────────────────┘         │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

### Аналогія: Офіс компанії

**Процес** — це окремий офіс компанії з власною адресою, меблями, документами.

**Потоки** — це працівники в цьому офісі:
- Вони **ділять** офісне приміщення, принтер, кухню (спільна пам'ять)
- Кожен має **свій** робочий стіл, блокнот, задачі (окремий стек, регістри)
- Вони можуть **комунікувати** напряму (швидко!)
- Але якщо один працівник "зламається" — може вплинути на інших

### Чому потоки?

| Перевага | Пояснення |
|----------|-----------|
| **Швидше створення** | Створення потоку ~10-100x швидше, ніж процесу |
| **Менше пам'яті** | Потоки ділять код, дані, heap — економія RAM |
| **Швидша комунікація** | Спільна пам'ять замість IPC (міжпроцесна комунікація) |
| **Простіша синхронізація** | Доступ до спільних даних без копіювання |

**Недоліки**:
- Складніше програмувати (race conditions, deadlocks)
- Помилка в одному потоці може "впустити" весь процес
- Потребує синхронізації доступу до спільних даних

### Приклад: Браузер Chrome

```
┌─────────────────────────────────────────────────────────────────────┐
│                       CHROME: ПРОЦЕСИ + ПОТОКИ                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ Browser process: інтерфейс, вкладки, диспетчер              │    │
│  │ потоки: UI, IO, пул фонових завдань                         │    │
│  └──────┬──────────────────┬───────────────────┬───────────────┘    │
│         │ IPC (Mojo)       │                   │                    │
│  ┌──────▼──────┐   ┌───────▼───────┐   ┌───────▼────────┐           │
│  │ GPU process │   │ Network       │   │ Storage, Audio │           │
│  │ малює все   │   │ service       │   │ utility        │           │
│  └─────────────┘   └───────────────┘   └────────────────┘           │
│                                                                     │
│  ┌─────────────┐   ┌───────────────┐   ┌────────────────┐           │
│  │ Renderer    │   │ Renderer      │   │ Extension      │           │
│  │ google.com  │   │ wikipedia.org │   │                │           │
│  │ потоки: main│   │ JS, compositor│   │                │           │
│  └─────────────┘   └───────────────┘   └────────────────┘           │
│                                                                     │
│  Чому окремі процеси для сайтів (Site Isolation, 2018)?             │
│  • Безпека: після атак Spectre дані одного сайту не мають бути      │
│    в одному адресному просторі з кодом іншого                       │
│  • Стабільність: «падіння» вкладки не вбиває браузер                │
│                                                                     │
│  Чому потоки всередині процесу?                                     │
│  • JavaScript, розбір сторінки й композиція кадру — паралельно      │
│  • Спільна пам'ять без копіювання між етапами                       │
└─────────────────────────────────────────────────────────────────────┘
```

### Потоки очима ядра Linux

Ядро Linux не має окремих понять «процес» і «потік»: і те, і інше — **задача** (структура `task_struct`), яку створює системний виклик `clone()`. Різниця лише в прапорцях: потік створюють з `CLONE_VM | CLONE_FILES | CLONE_SIGHAND | CLONE_THREAD` — «спільна пам'ять, спільні файли, спільні сигнали, одна група потоків», процес — майже без спільного. Тому в ядрі в кожного потоку є власний ідентифікатор (TID), а PID процесу — це TID його першого потоку. Планувальник розподіляє між ядрами саме потоки, а не процеси.

```bash
ls /proc/$$/task/                     # потоки поточної оболонки: один — вона сама
ps -eLf | head                        # LWP — ідентифікатор кожного потоку
grep -E 'Tgid|^Pid|Threads' /proc/$(pgrep -o firefox)/status
```

### Практична демонстрація

```bash
# Подивитися потоки процесу
ps -T -p $(pgrep -f firefox | head -1)

# Або через htop:
# htop → F2 (Setup) → Display options → Show custom thread names → F10
# Потім натисніть H для показу потоків

# Скільки потоків у Chrome?
ps -T -p $(pgrep -f chrome | head -1) | wc -l

# Детально про потоки процесу
cat /proc/$(pgrep -f firefox | head -1)/status | grep Threads
```

## Основи багатопоточності

### Створення потоків у C (POSIX Threads)

```c
#include <stdio.h>
#include <pthread.h>
#include <unistd.h>

// Функція, яку виконуватиме потік
void* worker(void* arg) {
    int id = *(int*)arg;
    for (int i = 0; i < 5; i++) {
        printf("Потік %d: ітерація %d\n", id, i);
        sleep(1);  // Пауза 1 секунда
    }
    return NULL;
}

int main() {
    pthread_t thread1, thread2;
    int id1 = 1, id2 = 2;

    // Створюємо два потоки
    pthread_create(&thread1, NULL, worker, &id1);
    pthread_create(&thread2, NULL, worker, &id2);

    // Головний потік чекає завершення
    pthread_join(thread1, NULL);
    pthread_join(thread2, NULL);

    printf("Всі потоки завершили роботу\n");
    return 0;
}
```

Компіляція та запуск:

```bash
# Компіляція з бібліотекою pthread
gcc -o threads threads.c -pthread

# Запуск
./threads

# Очікуваний вивід (порядок може відрізнятися!):
# Потік 1: ітерація 0
# Потік 2: ітерація 0
# Потік 1: ітерація 1
# Потік 2: ітерація 1
# ...
```

### Головна проблема: Race Condition

```c
#include <stdio.h>
#include <pthread.h>

int counter = 0;  // Спільна змінна

void* increment(void* arg) {
    for (int i = 0; i < 1000000; i++) {
        counter++;  // НЕ атомарна операція!
    }
    return NULL;
}

int main() {
    pthread_t t1, t2;

    pthread_create(&t1, NULL, increment, NULL);
    pthread_create(&t2, NULL, increment, NULL);

    pthread_join(t1, NULL);
    pthread_join(t2, NULL);

    // Очікуємо 2,000,000, але отримаємо менше!
    printf("Counter: %d (очікувалось: 2000000)\n", counter);
    return 0;
}
```

```bash
gcc -O0 -pthread race.c -o race       # -O0: без оптимізацій, щоб гонитву було видно
for i in 1 2 3; do ./race; done       # щоразу інше число, менше за 2000000
```

З оптимізацією `-O2` компілятор може «згорнути» цикл в одне додавання `counter += 1000000`, і гонитва трапиться рідко. Помилка від цього не зникає — вона лише стає рідкісною, а отже, ще небезпечнішою: така програма пройде тести й зламається в роботі.

```
┌──────────────────────────────────────────────────────────────────────┐
│                         RACE CONDITION                               │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   counter++ — це НЕ одна операція, а три:                            │
│                                                                      │
│   1. READ:  Прочитати значення з пам'яті → регістр                   │
│   2. INCREMENT: Збільшити значення в регістрі                        │
│   3. WRITE: Записати значення з регістра → пам'ять                   │
│                                                                      │
│   Проблема виникає при перемиканні між потоками:                     │
│                                                                      │
│   Потік 1                      Потік 2                     counter   │
│   ──────────────────────       ──────────────────────      ────────  │
│   READ counter (=5)                                             5    │
│                                READ counter (=5)                5    │
│   INCREMENT (reg=6)                                             5    │
│                                INCREMENT (reg=6)                5    │
│   WRITE counter (=6)                                            6    │
│                                WRITE counter (=6)               6    │
│                                                                      │
│   Результат: counter = 6 замість очікуваних 7!                       │
│   Ми "втратили" один інкремент!                                      │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

### Рішення: Mutex (Mutual Exclusion)

```c
#include <stdio.h>
#include <pthread.h>

int counter = 0;
pthread_mutex_t lock;  // М'ютекс для захисту

void* increment(void* arg) {
    for (int i = 0; i < 1000000; i++) {
        pthread_mutex_lock(&lock);    // Захопити замок
        counter++;                     // Критична секція
        pthread_mutex_unlock(&lock);  // Звільнити замок
    }
    return NULL;
}

int main() {
    pthread_t t1, t2;
    pthread_mutex_init(&lock, NULL);

    pthread_create(&t1, NULL, increment, NULL);
    pthread_create(&t2, NULL, increment, NULL);

    pthread_join(t1, NULL);
    pthread_join(t2, NULL);

    pthread_mutex_destroy(&lock);

    // Тепер завжди 2,000,000!
    printf("Counter: %d\n", counter);
    return 0;
}
```

## Чому паралельне виконання важливе

### 1. Використання багатоядерних процесорів

```
┌───────────────────────────────────────────────────────────────────────┐
│                    ЕВОЛЮЦІЯ ПРОЦЕСОРІВ                                │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   2000-2005: Гонка за частотою                                        │
│   ─────────────────────────────                                       │
│   1 GHz → 2 GHz → 3 GHz → ... → 3.8 GHz (межа!)                       │
│                                                                       │
│   Проблема: потужність P = C·V²·f, а вища частота потребує вищої      │
│   напруги V — тож P росте приблизно як f³. Близько 2005 року          │
│   закінчилося «масштабування Деннарда»: менші транзистори вже не      │
│   давали вищої частоти без перегріву                                  │
│                                                                       │
│   2005+: Гонка за ядрами                                              │
│   ──────────────────────                                              │
│   1 ядро → 2 ядра → 4 ядра → 8 ядер → 16 ядер → 64+ ядер              │
│                                                                       │
│   ┌──────────────────────────────────────────────────────────────┐    │
│   │  Pentium 4       │  Core 2 Duo    │  Core i9-13900K          │    │
│   │  (2004)          │  (2006)        │  (2022)                  │    │
│   │                  │                │                          │    │
│   │  3.8 GHz         │  2.4 GHz       │  5.8 GHz (boost)         │    │
│   │  1 ядро          │  2 ядра        │  24 ядра (8P + 16E)      │    │
│   │                  │                │                          │    │
│   │  ┌───┐           │  ┌───┐ ┌───┐   │  ┌─┐┌─┐┌─┐┌─┐┌─┐┌─┐...   │    │
│   │  │ 1 │           │  │ 1 │ │ 2 │   │  │ ││ ││ ││ ││ ││ │      │    │
│   │  └───┘           │  └───┘ └───┘   │  └─┘└─┘└─┘└─┘└─┘└─┘      │    │
│   └──────────────────────────────────────────────────────────────┘    │
│                                                                       │
│   Висновок: Щоб використати сучасний CPU на 100%,                     │
│             програма ПОВИННА бути багатопоточною!                     │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

### 2. Продуктивність I/O-bound задач

```
┌──────────────────────────────────────────────────────────────────────┐
│                    ПОСЛІДОВНО vs КОНКУРЕНТНО (I/O)                   │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   ПОСЛІДОВНО (один запит за раз):                                    │
│                                                                      │
│   HTTP запит 1:  ████████████████░░░░░░░░░░░░░░░░░░░░ (чекаємо)      │
│   HTTP запит 2:  ░░░░░░░░░░░░░░░░████████████████░░░░ (чекаємо)      │
│   HTTP запит 3:  ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░████████████████    │
│                  ───────────────────────────────────────► 30 сек     │
│                                                                      │
│   КОНКУРЕНТНО (потоки або асинхронний I/O):                          │
│                                                                      │
│   HTTP запит 1:  ████████████████                                    │
│   HTTP запит 2:  ████████████████                                    │
│   HTTP запит 3:  ████████████████                                    │
│                  ───────────────►  10 сек (3x швидше!)               │
│                                                                      │
│   Реальний приклад: Завантаження 100 картинок                        │
│   • Послідовно: 100 × 100ms = 10 секунд                              │
│   • Паралельно (10 потоків): ~1 секунда                              │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

Для задач, що чекають на мережу чи диск, потоки — не єдиний шлях. **Асинхронний I/O** дозволяє одному потоку тримати тисячі з'єднань: програма реєструє їх у ядрі (`epoll` у Linux, `kqueue` у BSD і macOS, IOCP у Windows) і отримує повідомлення лише про ті, де з'явилися дані. Так працюють nginx, Node.js і Redis: кілька потоків — десятки тисяч одночасних клієнтів.

### 3. Responsive UI (Відгукливий інтерфейс)

```
┌──────────────────────────────────────────────────────────────────────┐
│                    UI THREAD vs WORKER THREADS                       │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   ПОГАНИЙ ПІДХІД (все в одному потоці):                              │
│                                                                      │
│   ┌─────────────────────────────────────────────────────────────┐    │
│   │  UI Thread                                                  │    │
│   │  ┌──────────┐ ┌────────────────────────────────┐ ┌────────┐ │    │
│   │  │ Кнопка   │ │ Обробка файлу 5 ГБ...          │ │ Готово │ │    │
│   │  │ натиснута│ │ (UI не відповідає 10 хвилин!)  │ │        │ │    │
│   │  └──────────┘ └────────────────────────────────┘ └────────┘ │    │
│   └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│   Результат: "Not Responding", користувач думає, що програма зависла │
│                                                                      │
│   ПРАВИЛЬНИЙ ПІДХІД (окремі потоки):                                 │
│                                                                      │
│   ┌─────────────────────────────────────────────────────────────┐    │
│   │  UI Thread (завжди responsive!)                             │    │
│   │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐      │    │
│   │  │Click │ │Hover │ │Scroll│ │Type  │ │Click │ │Done! │      │    │
│   │  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘      │    │
│   └─────────────────────────────────────────────────────────────┘    │
│                            │                                         │
│                            │ передає задачу                          │
│                            ▼                                         │
│   ┌─────────────────────────────────────────────────────────────┐    │
│   │  Worker Thread                                              │    │
│   │  ████████████████████████████████████████████████████████   │    │
│   │  (обробка 5 ГБ файлу у фоні, UI працює нормально)           │    │
│   └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

### Межа прискорення: закон Амдала

Чи можна, додавши ядер, пришвидшувати програму нескінченно? Ні. 1967 року Джин Амдал (Gene Amdahl, IBM) показав: якщо частка програми `p` розпаралелюється, а решта `1 − p` лишається послідовною, то на `N` ядрах прискорення

S(N) = 1 / ((1 − p) + p / N)

Таблиця 1 — Прискорення за законом Амдала

| Паралельна частка p | 4 ядра | 16 ядер | 1000 ядер | Межа (N → ∞) |
|---------------------|--------|---------|-----------|--------------|
| 50 % | 1,6× | 1,9× | 2,0× | 2× |
| 90 % | 3,1× | 6,4× | 9,9× | 10× |
| 95 % | 3,5× | 9,1× | 19,6× | 20× |
| 99 % | 3,9× | 13,9× | 91,0× | 100× |

Навіть якщо лише 5 % програми послідовні (читання вхідного файлу, об'єднання результатів, робота під спільним м'ютексом), тисяча ядер дасть не більше ніж 20-кратне прискорення. Звідси практичний висновок: оптимізувати треба передусім послідовну частину, а кожен зайвий м'ютекс у «гарячому» циклі — це шматок послідовного коду.

Показова історія — Python. Його глобальний замок інтерпретатора (GIL) дозволяв виконувати байт-код лише одному потоку за раз, тож потоки Python прискорювали I/O, але не обчислення. Лише з версії 3.13 (2024) з'явилася експериментальна збірка без GIL, а в 3.14 (2025) її офіційно підтримують.

### Практичний приклад: Порівняння швидкості

```bash
#!/bin/bash
# Порівняння послідовного vs паралельного виконання

echo "=== Послідовне виконання ==="
time (
    sleep 1
    sleep 1
    sleep 1
)
# Результат: ~3 секунди

echo "=== Паралельне виконання ==="
time (
    sleep 1 &
    sleep 1 &
    sleep 1 &
    wait  # Чекаємо завершення всіх фонових процесів
)
# Результат: ~1 секунда
```

## 🏢 Real World: Як це використовують у великих компаніях

```
┌─────────────────────────────────────────────────────────────────────┐
│                       ПАРАЛЕЛІЗМ У PRODUCTION                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  GOOGLE                                                             │
│  ├── За словами Jeff Dean (2009), пошуковий запит одночасно         │
│  │   обробляють близько тисячі машин — кожна свою частину індексу   │
│  └── MapReduce (2004): обробка петабайтів як «розбий на частини,    │
│      обчисли паралельно, об'єднай результат»                        │
│                                                                     │
│  NGINX, NODE.JS, REDIS                                              │
│  └── Не потік на клієнта, а цикл подій на epoll: кілька потоків     │
│      обслуговують десятки тисяч з'єднань                            │
│                                                                     │
│  DISCORD, WHATSAPP                                                  │
│  ├── Erlang/Elixir (віртуальна машина BEAM): мільйони легких        │
│  │   процесів, які планує сама BEAM, а не ОС                        │
│  └── Кожна сесія користувача — окремий ізольований процес BEAM      │
│                                                                     │
│  ВІДЕОКОДУВАННЯ (Netflix, YouTube)                                  │
│  └── Фільм ріжуть на короткі фрагменти й кодують тисячами           │
│      незалежних завдань одночасно — майже ідеальний випадок         │
│      закону Амдала з p близьким до 100 %                            │
│                                                                     │
│  GPU ТА ШІ                                                          │
│  └── Тисячі простих ядер виконують ту саму операцію над різними     │
│      даними: множення матриць у нейромережах, рендеринг кадрів      │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

## 💼 Career Spotlight

```
┌───────────────────────────────────────────────────────────────────────┐
│                    КАР'ЄРНІ МОЖЛИВОСТІ                                │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  BACKEND DEVELOPER (High-Performance)                                 │
│  Розробка високонавантажених систем, що обробляють мільйони запитів   │
│  Зарплата: $100K-$180K (USA) | €50K-€90K (EU)                         │
│  Компанії: Netflix, Spotify, Cloudflare, Fastly                       │
│                                                                       │
│  SYSTEMS PROGRAMMER                                                   │
│  Розробка ядер ОС, драйверів, runtime-систем                          │
│  Зарплата: $140K-$250K (USA) | €70K-€120K (EU)                        │
│  Компанії: Microsoft, Apple, Google, Intel, AMD                       │
│                                                                       │
│  GAME ENGINE DEVELOPER                                                │
│  Багатопоточна архітектура, фізика, рендеринг                         │
│  Зарплата: $100K-$170K (USA) | €55K-€95K (EU)                         │
│  Компанії: Epic Games, Unity, EA, Ubisoft, Valve                      │
│                                                                       │
│  HPC ENGINEER (High-Performance Computing)                            │
│  Наукові обчислення, симуляції, Big Data                              │
│  Зарплата: $120K-$200K (USA) | €60K-€100K (EU)                        │
│  Компанії: NVIDIA, NASA, CERN, фарма-компанії                         │
│                                                                       │
│  EMBEDDED SYSTEMS DEVELOPER                                           │
│  Real-time системи, автомобілі, медичне обладнання                    │
│  Зарплата: $90K-$160K (USA) | €50K-€85K (EU)                          │
│  Компанії: Tesla, Bosch, Siemens, Medtronic                           │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

## 🔗 Корисні ресурси

### Онлайн-практика

| Ресурс | Опис | Посилання |
|--------|------|-----------|
| **OSTEP** | "Operating Systems: Three Easy Pieces" — безкоштовна книга | `pages.cs.wisc.edu/~remzi/OSTEP` |
| **Concurrency Visualizer** | Візуалізація паралельного виконання | `deadlockempire.github.io` |
| **pthread Tutorial** | Практичні приклади POSIX threads | `hpc-tutorials.llnl.gov/posix` |
| **Go Tour (Concurrency)** | Інтерактивні вправи на конкурентність | `go.dev/tour/concurrency` |
| **Rust Book (Concurrency)** | Безпечна конкурентність у Rust | `doc.rust-lang.org/book/ch16-00-concurrency.html` |

## 📋 Cheat Sheet

```
┌───────────────────────────────────────────────────────────────────────┐
│                 ШПАРГАЛКА: ПАРАЛЕЛЬНЕ ВИКОНАННЯ                       │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  КЛЮЧОВІ КОНЦЕПЦІЇ:                                                   │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │ Concurrency   = Керування багатьма задачами (структура)         │  │
│  │ Parallelism   = Виконання багатьох задач одночасно (виконання)  │  │
│  │ Process       = Ізольована програма з власною пам'яттю          │  │
│  │ Thread        = Легкий потік всередині процесу                  │  │
│  │ Race Condition = Помилка при одночасному доступі до даних       │  │
│  │ Mutex         = Замок для захисту критичної секції              │  │
│  │ Time-slicing  = Розділення часу CPU між задачами                │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  ПОРІВНЯННЯ:                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │           │ Процес              │ Потік                         │  │
│  │ ──────────│─────────────────────│──────────────────────────     │  │
│  │ Пам'ять   │ Ізольована          │ Спільна (крім stack)          │  │
│  │ Створення │ Повільне (~мс)      │ Швидке (~мкс)                 │  │
│  │ Комунікація│ IPC (повільно)     │ Спільна пам'ять (швидко)      │  │
│  │ Безпека   │ Ізоляція помилок    │ Помилка вб'є весь процес      │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  КОМАНДИ LINUX:                                                       │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │ nproc                → кількість ядер CPU                       │  │
│  │ ps aux | wc -l       → кількість процесів                       │  │
│  │ ps -T -p PID         → потоки конкретного процесу               │  │
│  │ htop + H             → показати потоки                          │  │
│  │ cat /proc/PID/status → деталі процесу                           │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  PTHREAD (C):                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │ pthread_create(&t, NULL, func, arg) → створити потік            │  │
│  │ pthread_join(t, NULL)               → чекати завершення         │  │
│  │ pthread_mutex_lock(&m)              → захопити mutex            │  │
│  │ pthread_mutex_unlock(&m)            → звільнити mutex           │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

## 🎯 Міні-проект (30 хв)

### Завдання: Дослідження паралелізму у вашій системі

Проведіть дослідження того, як ваша операційна система використовує паралельне виконання, та створіть звіт.

**Кроки:**

1. Визначте характеристики вашого CPU:
```bash
echo "=== CPU ХАРАКТЕРИСТИКИ ===" > ~/parallel-report.txt
echo "Кількість ядер: $(nproc)" >> ~/parallel-report.txt
lscpu | grep "Model name" >> ~/parallel-report.txt
lscpu | grep "Thread(s) per core" >> ~/parallel-report.txt
lscpu | grep "Core(s) per socket" >> ~/parallel-report.txt
```

2. Порахуйте процеси та потоки:
```bash
echo "" >> ~/parallel-report.txt
echo "=== ПРОЦЕСИ ТА ПОТОКИ ===" >> ~/parallel-report.txt
echo "Всього процесів: $(ps aux | wc -l)" >> ~/parallel-report.txt
echo "Всього потоків: $(ps -eLf | wc -l)" >> ~/parallel-report.txt
```

3. Знайдіть топ-5 процесів за кількістю потоків:
```bash
echo "" >> ~/parallel-report.txt
echo "=== ТОП-5 ЗА КІЛЬКІСТЮ ПОТОКІВ ===" >> ~/parallel-report.txt
ps -eo pid,nlwp,comm --sort=-nlwp | head -6 >> ~/parallel-report.txt
```

4. Проведіть тест паралельного виконання:
```bash
echo "" >> ~/parallel-report.txt
echo "=== ТЕСТ ПАРАЛЕЛІЗМУ ===" >> ~/parallel-report.txt

echo "Послідовне виконання:" >> ~/parallel-report.txt
{ time (sleep 0.5; sleep 0.5; sleep 0.5; sleep 0.5); } 2>> ~/parallel-report.txt

echo "Паралельне виконання:" >> ~/parallel-report.txt
{ time (sleep 0.5 & sleep 0.5 & sleep 0.5 & sleep 0.5 & wait); } 2>> ~/parallel-report.txt
```

5. Перегляньте результат:
```bash
cat ~/parallel-report.txt
```

**Очікуваний результат:**
Файл `parallel-report.txt` з інформацією про ваш CPU, кількість процесів і потоків, топ багатопоточних програм та результати тесту, що демонструє різницю між послідовним і паралельним виконанням.

**Бонус (для допитливих):**
- Напишіть простий bash-скрипт, який симулює навантаження на CPU (`while true; do :; done &`) і спостерігайте за htop
- Знайдіть, скільки потоків використовує ваш браузер
- Скомпілюйте програму з прикладу race condition з `-O0` і з `-O2`, запустіть кожну по десять разів і порівняйте результати
- Обчисліть за законом Амдала, яку частку `p` має ваш тест `sleep` і чому для нього прискорення майже дорівнює кількості команд

## Тест для самоперевірки

Десять запитань, у кожному одна правильна відповідь. Якщо тема винесена вашій групі на самостійне опрацювання, цей самий тест публікується в Google Classroom — 10 балів, по одному за кожне запитання.

**1.** Чим конкурентність відрізняється від паралелізму?

- конкурентність — це керування багатьма задачами, паралелізм — їх одночасне виконання
- конкурентність — це одночасне виконання, паралелізм — чергування задач
- це два терміни для одного явища
- конкурентність можлива лише на багатоядерних процесорах

**2.** Чи можлива конкурентність на одноядерному процесорі?

- так, задачі чергуються, отримуючи по кванту часу
- ні, для неї потрібно щонайменше два ядра
- так, але тільки для системних процесів
- ні, одне ядро виконує лише одну програму за весь сеанс

**3.** Що потоки одного процесу мають спільним?

- адресний простір і відкриті файли
- стек і регістри
- лічильник команд
- нічого, вони повністю ізольовані

**4.** Що кожен потік має власне?

- стек і набір регістрів
- купу (heap)
- таблицю відкритих файлів
- глобальні змінні

**5.** Чому створення потоку швидше за створення процесу?

- не треба будувати окремий адресний простір — він уже є в процесі
- потік не потребує планування
- потік виконується без участі ядра
- потоку не потрібен стек

**6.** Що таке стан гонитви (race condition)?

- результат залежить від того, у якому порядку потоки дістануться до спільних даних
- процес не може отримати процесорний час
- два процеси взаємно блокують один одного назавжди
- потік завершується раніше за батьківський процес

**7.** Для чого призначений м'ютекс?

- щоб у критичну секцію заходив лише один потік одночасно
- щоб пришвидшити перемикання контексту
- щоб розподілити потоки між ядрами
- щоб завершити потік ззовні

**8.** Яка функція POSIX Threads створює новий потік?

- pthread_create
- pthread_join
- pthread_mutex_lock
- fork

**9.** Що робить `pthread_join`?

- чекає завершення вказаного потоку
- створює потік
- звільняє м'ютекс
- переводить потік у режим очікування

**10.** Чим помилка в потоці небезпечніша за помилку в окремому процесі?

- аварія одного потоку завершує весь процес разом з рештою потоків
- потік неможливо перезапустити
- помилка в потоці спричиняє kernel panic
- потік не можна відстежити відлагоджувачем

## Підсумок

| Термін | Визначення |
|--------|------------|
| **Конкурентність (Concurrency)** | Структура програми для роботи з кількома задачами |
| **Паралелізм (Parallelism)** | Фактичне одночасне виконання на кількох ядрах |
| **Процес (Process)** | Ізольований екземпляр програми з власною пам'яттю |
| **Потік (Thread)** | Легкий потік виконання всередині процесу |
| **Race Condition** | Помилка через неконтрольований доступ до спільних даних |
| **Mutex** | Механізм взаємного виключення для синхронізації |
| **Time-slicing** | Розділення часу CPU між задачами |
| **Preemptive Multitasking** | ОС примусово перемикає задачі |
| **Закон Амдала** | Прискорення обмежене послідовною частиною програми: S = 1 / ((1 − p) + p / N) |
| **Асинхронний I/O** | Один потік обслуговує багато з'єднань, отримуючи від ядра повідомлення про готовність (epoll) |

| Концепція | Ключова ідея |
|-----------|--------------|
| Процес vs Потік | Потоки легші, діляться пам'яттю, швидша комунікація |
| Навіщо паралелізм | Використання багатоядерних CPU, швидший I/O, responsive UI |
| Головна проблема | Race conditions — потребують синхронізації |

На наступній лекції — стани процесів та діаграми станів: як ОС керує життєвим циклом кожного процесу.
