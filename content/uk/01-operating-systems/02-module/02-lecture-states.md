---
title: "Стан і діаграми станів для процесів"
type: lecture
order: 2
preview: "Стани процесу: new, ready, running, waiting, terminated. Діаграми переходів станів. Process Control Block."
---

## Кожен процес має свою історію

Уявіть, що ви працюєте в ресторані швидкого харчування. Кожне замовлення проходить певні етапи:

1. **Прийняте** — офіціант записав замовлення
2. **В черзі** — чекає на кухаря
3. **Готується** — кухар активно працює
4. **Очікує інгредієнти** — пауза, поки принесуть продукти
5. **Видане** — страва готова, замовлення закрите

Процеси в операційній системі працюють так само! Вони народжуються, чекають своєї черги, виконуються, іноді зупиняються і, врешті-решт, завершуються.

**Запитання до аудиторії**: Що відбувається, коли ви натискаєте Play у Spotify, а потім вмикаєте гру? Чому музика продовжує грати, хоча ви "перемкнулися" на гру?

```
┌───────────────────────────────────────────────────────────────────────┐
│                    ЖИТТЄВИЙ ЦИКЛ ЗАМОВЛЕННЯ                           │
│                                                                       │
│   [Прийняте] ──► [В черзі] ──► [Готується] ──► [Видане]               │
│                                     │                                 │
│                                     ▼                                 │
│                          [Очікує інгредієнти]                         │
│                                     │                                 │
│                                     └─────────────► [Готується]       │
│                                                                       │
│   Процес у комп'ютері проходить схожі стани!                          │
└───────────────────────────────────────────────────────────────────────┘
```

## Стани процесу: п'ять ключових станів

Операційна система відстежує кожен процес через його **стан**. Класична модель включає п'ять станів:

### 1. New (Новий)

Процес щойно створено. ОС виділяє ресурси, створює структури даних, але процес ще не готовий до виконання.

```bash
# Коли ви запускаєте програму, перші мілісекунди вона у стані New
./my_program &
# ОС: виділяє PID, створює PCB, завантажує код у пам'ять
```

**Аналогія**: Ви щойно влаштувалися на роботу. HR оформляє документи, видає пропуск, але до робочого місця ви ще не дійшли.

### 2. Ready (Готовий)

Процес готовий до виконання і чекає, коли планувальник призначить йому процесорний час.

```bash
# Linux не розрізняє Ready і Running: обидва — стан R (runnable)
ps -eo pid,stat,comm | awk '$2 ~ /^R/'
```

**Аналогія**: Ви сидите в черзі до лікаря. Ви готові увійти, але треба дочекатися виклику.

### 3. Running (Виконується)

Процес активно використовує CPU. В один момент часу на одному ядрі може бути тільки **один** процес у стані Running.

```bash
# На 4-ядерному процесорі максимум 4 процеси одночасно у Running
# Решта чекають у Ready
nproc  # кількість ядер
```

**Аналогія**: Ви в кабінеті лікаря, він вас оглядає.

### 4. Waiting / Blocked (Очікування)

Процес зупинено, бо він чекає на зовнішню подію: введення з клавіатури, читання з диска, відповідь мережі.

```bash
# Процеси у стані Waiting (STAT: S, D)
ps aux | awk '$8 ~ /S|D/ {print $2, $8, $11}' | head -10
# S = Sleeping (interruptible)
# D = Disk sleep (uninterruptible)
```

**Аналогія**: Лікар відправив вас на аналізи. Ви чекаєте результатів і не можете продовжити огляд.

### 5. Terminated (Завершений)

Процес закінчив виконання. ОС звільняє ресурси, але зберігає інформацію про завершення (exit code) для батьківського процесу.

```bash
# Zombie-процеси: завершилися, але батько не забрав статус
ps aux | awk '$8 ~ /Z/ {print $2, $11}'

# Exit code останньої команди
echo $?
# 0 = успіх, інше = помилка
```

**Аналогія**: Ви вийшли з кабінету, огляд завершено. Залишається тільки запис у картці.

```
┌───────────────────────────────────────────────────────────────────────┐
│                    П'ЯТЬ СТАНІВ ПРОЦЕСУ                               │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   ┌─────────┐    ┌─────────┐    ┌─────────────┐    ┌─────────────┐    │
│   │   NEW   │───►│  READY  │◄──►│   RUNNING   │───►│ TERMINATED  │    │
│   │ (новий) │    │(готовий)│    │(виконується)│    │ (завершений)│    │
│   └─────────┘    └────┬────┘    └──────┬──────┘    └─────────────┘    │
│                       │                │                              │
│                       │                ▼                              │
│                       │         ┌────────────┐                        │
│                       └─────────│  WAITING   │                        │
│                                 │(очікування)│                        │
│                                 └────────────┘                        │
│                                                                       │
│   NEW        → Створення процесу, виділення ресурсів                  │
│   READY      → Готовий виконуватись, чекає CPU                        │
│   RUNNING    → Активно використовує процесор                          │
│   WAITING    → Чекає на I/O або подію                                 │
│   TERMINATED → Завершив роботу, звільняє ресурси                      │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

## Діаграма переходів станів

Процес не може переходити між станами довільно. Існують чіткі правила переходів:

```
┌─────────────────────────────────────────────────────────────────────┐
│                      ДІАГРАМА ПЕРЕХОДІВ СТАНІВ                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│    ┌───────┐ admitted ┌─────────┐ dispatch ┌─────────┐ exit         │
│    │  NEW  │─────────►│  READY  │─────────►│ RUNNING │──────┐       │
│    └───────┘          │         │◄─────────│         │      │       │
│                       └─────────┘ preempt  └────┬────┘      ▼       │
│                            ▲                    │    ┌────────────┐ │
│                            │                    │    │ TERMINATED │ │
│              I/O або подія │                    │    └────────────┘ │
│              завершилася   │   ┌─────────┐      │ I/O, sleep,       │
│                            └───│ WAITING │◄─────┘ очікування        │
│                                └─────────┘        блокування        │
│                                                                     │
│  preempt — квант часу минув або з'явився важливіший процес.         │
│  З WAITING шлях лише в READY; у WAITING — лише з RUNNING.           │
└─────────────────────────────────────────────────────────────────────┘
```

### Переходи та їх причини

| Перехід | Причина | Приклад |
|---------|---------|---------|
| **New → Ready** | Admitted: ОС прийняла процес | `fork()` завершився успішно |
| **Ready → Running** | Dispatch: планувальник обрав процес | Процес отримав CPU |
| **Running → Ready** | Preempt: час вичерпано або пріоритетніший процес | Таймер переривання |
| **Running → Waiting** | I/O wait: процес чекає на ресурс | `read()` з диска |
| **Waiting → Ready** | I/O complete: ресурс став доступний | Дані прочитані |
| **Running → Terminated** | Exit: процес завершився | `exit(0)` |

### Заборонені переходи

Деякі переходи **неможливі** за логікою роботи ОС:

- **Waiting → Running**: Процес не може одразу почати виконуватись після I/O. Спочатку він має стати Ready і дочекатися планувальника.
- **Ready → Waiting**: Процес не може чекати на I/O, якщо він не виконується. Тільки Running-процес може ініціювати операцію I/O.
- **Terminated → будь-що**: Мертвий процес не воскресає.

### Від п'яти станів до справжнього Linux

П'ятистанна модель — навчальна абстракція. Реальні ОС додають деталі, і знати відповідність корисно, бо саме ці літери ви бачите в `ps` і `top`.

Таблиця 1 — Стани процесу в класичній моделі та в Linux

| Класична модель | Linux (`ps`, колонка STAT) | Пояснення |
|-----------------|----------------------------|-----------|
| New | — | Існує лише мить усередині `fork()`, у `ps` не видно |
| Ready | `R` | Стоїть у черзі готових (runqueue) |
| Running | `R` | Та сама літера: ядро не розрізняє «готовий» і «виконується» |
| Waiting | `S` | Переривне очікування: сигнал розбудить процес (`sleep`, очікування мережі, клавіатури) |
| Waiting | `D` | Непереривне очікування всередині драйвера, найчастіше дискового чи мережевого сховища |
| — | `T`, `t` | Зупинений сигналом (Ctrl+Z, `kill -STOP`) або відлагоджувачем |
| Terminated | `Z` | Зомбі: завершився, але батько ще не прочитав код повернення |
| — | `I` | Бездіяльний потік ядра (idle), з Linux 4.14 |

У підручнику Столлінгса модель розширено до семи станів: додано **призупинені** (suspended ready і suspended blocked) — процеси, вивантажені з оперативної пам'яті на диск. У сучасних ОС вивантажують не процес цілком, а окремі сторінки пам'яті (модуль 4), тому окремих станів для цього немає.

Два факти про стан `D`, які часто ставлять в глухий кут. Перший: процес у стані `D` не реагує навіть на `kill -9` — сигнал буде доставлено, лише коли драйвер поверне керування (з версії 2.6.25 з'явився компромісний стан `TASK_KILLABLE`, у якому фатальні сигнали все ж діють). Другий: у Linux, на відміну від інших Unix, процеси в стані `D` враховуються в **середньому навантаженні** (load average). Тому сервер з «завислим» мережевим диском NFS може показувати load average 50 при майже бездіяльному процесорі.

```bash
cat /proc/$$/wchan; echo      # функція ядра, в якій чекає процес: do_wait
ps -eo pid,stat,wchan:22,comm | awk '$2 ~ /^D/'   # хто в стані D і де саме
cat /proc/loadavg             # середнє навантаження: R + D
```

```bash
# Спостерігаємо за станами процесів у реальному часі
watch -n 1 'ps aux | head -15'

# Або через top (натисніть q для виходу)
top
# Колонка S (Status): R=Running, S=Sleeping, D=Disk, Z=Zombie
```

## Process Control Block (PCB)

**Process Control Block (PCB)** — структура даних, яка містить всю інформацію про процес. Це "паспорт" процесу, який ОС використовує для керування.

```
┌───────────────────────────────────────────────────────────────────────┐
│                    PROCESS CONTROL BLOCK (PCB)                        │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   ┌───────────────────────────────────────────────────────────────┐   │
│   │                         PCB                                   │   │
│   ├───────────────────────────────────────────────────────────────┤   │
│   │  PID: 1234                    │ Process Identifier            │   │
│   ├───────────────────────────────┼───────────────────────────────┤   │
│   │  State: RUNNING               │ Поточний стан процесу         │   │
│   ├───────────────────────────────┼───────────────────────────────┤   │
│   │  PC: 0x55d4c3a1b2f0           │ Program Counter (наступна     │   │
│   │                               │ інструкція)                   │   │
│   ├───────────────────────────────┼───────────────────────────────┤   │
│   │  Registers: RAX, RBX, RCX...  │ Значення регістрів CPU        │   │
│   ├───────────────────────────────┼───────────────────────────────┤   │
│   │  Memory: mm → page tables     │ Опис адресного простору і     │   │
│   │          (CR3 на x86)         │ таблиць сторінок              │   │
│   ├───────────────────────────────┼───────────────────────────────┤   │
│   │  Priority: 20                 │ Пріоритет планування          │   │
│   ├───────────────────────────────┼───────────────────────────────┤   │
│   │  CPU Time: 00:05:32           │ Використаний час CPU          │   │
│   ├───────────────────────────────┼───────────────────────────────┤   │
│   │  PPID: 1                      │ PID батьківського процесу     │   │
│   ├───────────────────────────────┼───────────────────────────────┤   │
│   │  Open Files: [fd0, fd1, fd2]  │ Відкриті файлові дескриптори  │   │
│   ├───────────────────────────────┼───────────────────────────────┤   │
│   │  UID/GID: 1000/1000           │ Власник процесу               │   │
│   └───────────────────────────────┴───────────────────────────────┘   │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

### Компоненти PCB

| Компонент | Призначення | Команда для перегляду |
|-----------|-------------|----------------------|
| **PID** | Унікальний ідентифікатор | `echo $$` |
| **State** | Поточний стан (R, S, D, Z, T) | `ps -o stat -p PID` |
| **Program Counter** | Адреса наступної інструкції | ззовні не видно; `/proc/PID/wchan` покаже, де чекає процес у ядрі |
| **Registers** | Збережені значення регістрів | зберігаються ядром під час перемикання контексту |
| **Memory Info** | Карта пам'яті процесу | `cat /proc/PID/maps` |
| **Priority** | Пріоритет (nice value) | `ps -o ni -p PID` |
| **CPU Time** | Використаний час CPU | `ps -o time -p PID` |
| **PPID** | Батьківський процес | `ps -o ppid -p PID` |
| **Open Files** | Файлові дескриптори | `ls -la /proc/PID/fd` |
| **UID/GID** | Власник | `ps -o uid,gid -p PID` |

### Перегляд PCB через /proc

Linux надає доступ до інформації про процеси через віртуальну файлову систему `/proc`:

```bash
# Інформація про поточний процес
echo "PID поточного shell: $$"

# Статус процесу (детальний PCB)
cat /proc/$$/status

# Карта пам'яті
cat /proc/$$/maps | head -10

# Відкриті файли
ls -la /proc/$$/fd

# Командний рядок
cat /proc/$$/cmdline

# Поточна робоча директорія
readlink /proc/$$/cwd

# Батьківський процес
cat /proc/$$/status | grep PPid
```

У Linux роль PCB виконує структура `task_struct` — кілька кілобайтів на кожен потік, а поля на кшталт відкритих файлів і карти пам'яті зберігаються окремими структурами, на які вона посилається. Завдяки цьому потоки одного процесу просто посилаються на спільні `mm_struct` і таблицю файлів.

### Навіщо потрібен PCB?

PCB критично важливий для:

1. **Context Switch** — при перемиканні між процесами ОС зберігає стан одного процесу (в його PCB) і відновлює стан іншого
2. **Планування** — планувальник використовує пріоритет і CPU time для вибору наступного процесу
3. **Захист** — UID/GID визначають права доступу
4. **Відновлення** — після I/O операції ОС знає, куди повернути процес

## Життєвий цикл процесу

Від народження до смерті процес проходить повний життєвий цикл:

```
┌──────────────────────────────────────────────────────────────────────┐
│                    ЖИТТЄВИЙ ЦИКЛ ПРОЦЕСУ                             │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   1. НАРОДЖЕННЯ (fork/exec)                                          │
│   ┌─────────────────────────────────────────────────────────────┐    │
│   │  Батьківський процес викликає fork()                        │    │
│   │  ОС: створює PCB, виділяє PID; пам'ять не копіює, а робить  │    │
│   │  спільною з позначкою copy-on-write (копія — при записі)    │    │
│   │  Новий процес викликає exec() для завантаження програми     │    │
│   └─────────────────────────────────────────────────────────────┘    │
│                              │                                       │
│                              ▼                                       │
│   2. ЖИТТЯ (ready ↔ running ↔ waiting)                               │
│   ┌─────────────────────────────────────────────────────────────┐    │
│   │  Процес циклічно переходить між станами:                    │    │
│   │  • Отримує CPU час (running)                                │    │
│   │  • Перервано таймером (ready)                               │    │
│   │  • Чекає на I/O (waiting)                                   │    │
│   │  • I/O завершено (ready)                                    │    │
│   └─────────────────────────────────────────────────────────────┘    │
│                              │                                       │
│                              ▼                                       │
│   3. СМЕРТЬ (exit/kill)                                              │
│   ┌─────────────────────────────────────────────────────────────┐    │
│   │  Процес завершується: exit() або отримує сигнал             │    │
│   │  ОС: звільняє пам'ять, закриває файли                       │    │
│   │  PCB зберігається (zombie) поки батько не викличе wait()    │    │
│   │  Після wait() PCB видаляється повністю                      │    │
│   └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

### Створення процесу: fork() та exec()

```bash
# fork() створює копію поточного процесу
# exec() замінює код процесу новою програмою

# Приклад: bash створює новий процес для команди
bash -c 'echo "PID: $$"; sleep 5'

# Подивитися дерево процесів
pstree -p $$
```

```
┌───────────────────────────────────────────────────────────────────────┐
│                    FORK + EXEC                                        │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   Батьківський процес (PID: 1000)                                     │
│   │                                                                   │
│   │ fork()                                                            │
│   │                                                                   │
│   ├──────────────────────────────────────────┐                        │
│   │                                          │                        │
│   ▼                                          ▼                        │
│   Батько (PID: 1000)              Дитина (PID: 1001)                  │
│   продовжує роботу                fork() повертає 0                   │
│   fork() повертає 1001            │                                   │
│                                   │ exec("/bin/ls")                   │
│                                   │                                   │
│                                   ▼                                   │
│                                   Дитина тепер виконує ls             │
│                                   (код bash замінено на ls)           │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

Докладно про `fork()`, `exec()`, `wait()` і їхні аналоги у Windows — у лекції «Керування процесами в різних ОС». Тут важливо одне: `fork()` не копіює пам'ять одразу. Батько й дитина отримують спільні сторінки з позначкою **copy-on-write**, і копія сторінки створюється лише тоді, коли хтось із них у неї пише. Тому `fork()` великого процесу займає мікросекунди, а не секунди.

### Завершення процесу та Zombie

Зомбі можна створити однією командою — це безпечно:

```bash
bash -c 'sleep 1 & exec sleep 30' &   # дитина sleep 1 завершиться, а батько
                                       # (тепер це sleep 30) ніколи не викличе wait()
sleep 2
ps -o pid,ppid,stat,cmd --ppid $!     # Z  [sleep] <defunct>
```

Трюк у `exec`: оболонка запускає дитину `sleep 1`, а потім замінює саму себе програмою `sleep 30`, яка нічого не знає про дітей і не забирає їхній статус. Через 30 секунд батько завершиться, осиротілого зомбі «всиновить» init (PID 1) і прибере його.

Зомбі не займає пам'яті програми — лише запис у таблиці процесів. Але кожен із них тримає PID, а PID-и обмежені (`cat /proc/sys/kernel/pid_max`): програма, що породжує зомбі в циклі, рано чи пізно не дасть системі створити жодного нового процесу.

**Запитання**: Чому zombie-процес не можна «вбити» командою `kill`? Тому що він уже мертвий: сигнали мертвому процесу нічого не дають. Прибрати зомбі можна, лише змусивши батька викликати `wait()` або завершивши батька — тоді зомбі перейде до init, а той його прибере.

## Практичні команди для роботи зі станами

### ps - Process Status

```bash
# Базова інформація про процеси
ps aux

# Формат виводу:
# USER       PID %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND
# root         1  0.0  0.1 169256 13120 ?        Ss   Sep01   0:14 /sbin/init

# STAT - стан процесу:
# R = Running (виконується)
# S = Sleeping (очікує, можна перервати)
# D = Disk sleep (очікує I/O, не можна перервати)
# Z = Zombie (завершений, чекає на батька)
# T = Stopped (зупинений, наприклад Ctrl+Z)
# + = Foreground process group

# Фільтрація за станом
ps aux | awk '$8 ~ /R/ {print "Running:", $11}'
ps aux | awk '$8 ~ /S/ {print "Sleeping:", $11}' | head -5
ps aux | awk '$8 ~ /Z/ {print "Zombie:", $11}'
```

### top - Real-time Monitor

```bash
# Інтерактивний монітор процесів
top

# Корисні клавіші в top:
# M - сортувати за пам'яттю
# P - сортувати за CPU
# T - сортувати за часом
# k - вбити процес (ввести PID)
# r - змінити пріоритет (renice)
# 1 - показати всі ядра CPU
# q - вийти

# Або htop для кращого інтерфейсу
htop
```

### Моніторинг конкретного процесу

```bash
# Відстежувати стан конкретного процесу
PID=$$  # PID поточного shell

# Стан
cat /proc/$PID/status | grep State

# Або через ps
ps -o pid,stat,cmd -p $PID

# Безперервний моніторинг
watch -n 0.5 "ps -o pid,stat,%cpu,%mem,cmd -p $PID"
```

## Черги процесів

ОС організовує процеси в **черги** для ефективного керування:

```
┌───────────────────────────────────────────────────────────────────────┐
│                    ЧЕРГИ ПРОЦЕСІВ В ОС                                │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   JOB QUEUE (всі процеси в системі)                                   │
│   ┌─────────────────────────────────────────────────────────────┐     │
│   │ [P1] [P2] [P3] [P4] [P5] [P6] [P7] [P8] [P9] [P10] ...      │     │
│   └─────────────────────────────────────────────────────────────┘     │
│                                                                       │
│   READY QUEUE (готові до виконання)                                   │
│   ┌─────────────────────────────────────────────────────────────┐     │
│   │ ► [P3] → [P7] → [P1] → [P9] →                               │     │
│   └───────────────────────────────────────────────┬─────────────┘     │
│                                                   │                   │
│                                      Scheduler    │                   │
│                                                   ▼                   │
│                                              ┌────────┐               │
│                                              │  CPU   │               │
│                                              │ [P5]   │               │
│                                              └────────┘               │
│                                                                       │
│   DEVICE QUEUES (очікують на пристрої)                                │
│   ┌─────────────────────────────────────────────────────────────┐     │
│   │ Disk:      [P2] → [P6] →                                    │     │
│   │ Network:   [P4] → [P8] →                                    │     │
│   │ Keyboard:  [P10] →                                          │     │
│   └─────────────────────────────────────────────────────────────┘     │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

### Типи черг

| Черга | Призначення | Коли процес потрапляє |
|-------|-------------|----------------------|
| **Job Queue** | Всі процеси в системі | При створенні |
| **Ready Queue** | Готові до виконання | Після створення або завершення I/O |
| **Device Queues** | Чекають на конкретний пристрій | При запиті I/O |

«Job queue» — термін з епохи пакетної обробки, коли завдання спершу потрапляли в чергу на диску і лише потім у пам'ять. У Linux немає однієї черги готових: кожне ядро процесора має власну чергу (runqueue), а планувальник періодично переносить задачі між ними, щоб вирівняти навантаження. Замість «черг пристроїв» використовуються **черги очікування** (wait queues) — окрема черга на кожну подію: «прийшли дані на цей сокет», «завершилося читання цього блоку».

## Приклад: відстеження життєвого циклу

Створимо простий скрипт для демонстрації станів:

```bash
#!/bin/bash
# lifecycle_demo.sh - демонстрація станів процесу

echo "1. Процес створено (NEW -> READY)"
echo "   PID: $$"
echo "   Стан: $(cat /proc/$$/status | grep State)"

echo ""
echo "2. Процес виконується (RUNNING)"
echo "   Виконуємо обчислення..."
for i in {1..1000000}; do
    : # порожня команда, споживає CPU
done
echo "   Обчислення завершено"

echo ""
echo "3. Процес чекає на I/O (WAITING)"
echo "   Читаємо з диска..."
cat /etc/passwd > /dev/null
echo "   Читання завершено"

echo ""
echo "4. Процес завершується (TERMINATED)"
echo "   Exit code: 0"
exit 0
```

```bash
# Запустити демонстрацію
chmod +x lifecycle_demo.sh
./lifecycle_demo.sh

# Моніторити стан у реальному часі (в іншому терміналі)
watch -n 0.1 "ps -o pid,stat,cmd -p $(pgrep lifecycle_demo)"
```

## 🏢 Real World: Як це використовують у великих компаніях

```
┌─────────────────────────────────────────────────────────────────────┐
│                     СТАНИ ПРОЦЕСІВ У PRODUCTION                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  META: PSI (PRESSURE STALL INFORMATION)                             │
│  ├── Метрика ядра з Linux 4.20 (2018): яку частку часу задачі       │
│  │   чекали на CPU, пам'ять чи введення-виведення                   │
│  ├── cat /proc/pressure/cpu — «скільки часу ми стояли в READY»      │
│  └── На ній працюють oomd від Meta і systemd-oomd: завершують       │
│      процеси до того, як система «застигне» від браку пам'яті       │
│                                                                     │
│  NETFLIX: RUNQLAT                                                   │
│  └── Brendan Gregg зробив інструмент runqlat (BCC/eBPF): гістограма │
│      часу від READY до RUNNING — прямий вимір черги готових         │
│                                                                     │
│  КОНТЕЙНЕРИ І ЗОМБІ                                                 │
│  ├── У контейнері ваша програма стає PID 1 і має сама прибирати     │
│  │   зомбі-нащадків, а звичайні програми цього не вміють            │
│  └── Тому існують tini і docker run --init: крихітний init,         │
│      що лише викликає wait() для всіх сиріт                         │
│                                                                     │
│  NFS І СТАН D                                                       │
│  └── Недоступний мережевий диск лишає процеси в стані D; load       │
│      average росте до десятків, хоча процесор вільний               │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

## 💼 Career Spotlight

```
┌───────────────────────────────────────────────────────────────────────┐
│                    КАР'ЄРНІ МОЖЛИВОСТІ                                │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   LINUX SYSTEMS ENGINEER                                              │
│   Налаштування та оптимізація серверів, моніторинг процесів           │
│   Зарплата: $100K-$160K (USA) | €55K-€85K (EU)                        │
│   Компанії: Red Hat, Canonical, SUSE, будь-яка tech-компанія          │
│                                                                       │
│   PERFORMANCE ENGINEER                                                │
│   Профілювання, оптимізація часу виконання, аналіз станів             │
│   Зарплата: $130K-$200K (USA) | €70K-€110K (EU)                       │
│   Компанії: Netflix, Uber, Datadog, New Relic                         │
│                                                                       │
│   KERNEL DEVELOPER                                                    │
│   Розробка ядра, scheduler, керування процесами                       │
│   Зарплата: $150K-$250K (USA) | €80K-€130K (EU)                       │
│   Компанії: Google, Meta, Intel, AMD, Microsoft                       │
│                                                                       │
│   SRE (SITE RELIABILITY ENGINEER)                                     │
│   Моніторинг, автоматизація, реагування на інциденти                  │
│   Зарплата: $140K-$220K (USA) | €65K-€100K (EU)                       │
│   Компанії: Google, LinkedIn, Dropbox, Stripe                         │
│                                                                       │
│   OBSERVABILITY ENGINEER                                              │
│   Системи моніторингу, трасування, профілювання                       │
│   Зарплата: $120K-$180K (USA) | €60K-€95K (EU)                        │
│   Компанії: Grafana, Honeycomb, Lightstep, Splunk                     │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

## 🔗 Корисні ресурси

### Онлайн-практика

| Ресурс | Опис | Посилання |
|--------|------|-----------|
| **Linux Process Explorer** | Візуалізація станів процесів | `htop` (встановити локально) |
| **Killercoda** | Інтерактивні Linux-сценарії | `killercoda.com` |
| **OverTheWire: Bandit** | Практика роботи з процесами | `overthewire.org/wargames/bandit` |
| **Linux Journey: Processes** | Теорія + практика | `linuxjourney.com/lesson/monitor-processes-ps-command` |
| **OSTEP: Processes** | Безкоштовна книга | `pages.cs.wisc.edu/~remzi/OSTEP/cpu-intro.pdf` |

### Книги

| Книга | Автор | Для чого |
|-------|-------|----------|
| "Operating Systems: Three Easy Pieces" | Arpaci-Dusseau | Теорія процесів (безкоштовна) |
| "Linux Kernel Development" | Robert Love | Реалізація в Linux |
| "Systems Performance" | Brendan Gregg | Профілювання та оптимізація |
| "Understanding the Linux Kernel" | Bovet, Cesati | Глибоке занурення в ядро |

## 📋 Cheat Sheet

```
┌───────────────────────────────────────────────────────────────────────┐
│                    ШПАРГАЛКА: СТАНИ ПРОЦЕСІВ                          │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   П'ЯТЬ СТАНІВ:                                                       │
│   ┌─────────────────────────────────────────────────────────────────┐ │
│   │ NEW        = Процес створюється                                 │ │
│   │ READY      = Готовий, чекає на CPU                              │ │
│   │ RUNNING    = Виконується на CPU                                 │ │
│   │ WAITING    = Чекає на I/O або подію                             │ │
│   │ TERMINATED = Завершив роботу                                    │ │
│   └─────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│   КОДИ СТАНІВ У ps/top (колонка STAT):                                │
│   ┌─────────────────────────────────────────────────────────────────┐ │
│   │ R = Running/Runnable                                            │ │
│   │ S = Sleeping (interruptible)                                    │ │
│   │ D = Disk sleep (uninterruptible I/O)                            │ │
│   │ Z = Zombie (terminated, waiting for parent)                     │ │
│   │ T = Stopped (Ctrl+Z)                                            │ │
│   └─────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│   PCB (Process Control Block):                                        │
│   ┌─────────────────────────────────────────────────────────────────┐ │
│   │ PID, PPID, State, Priority, Registers, Memory Map               │ │
│   │ CPU Time, Open Files, UID/GID                                   │ │
│   └─────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│   КОМАНДИ:                                                            │
│   ┌─────────────────────────────────────────────────────────────────┐ │
│   │ ps aux              → список всіх процесів                      │ │
│   │ ps -o stat -p PID   → стан конкретного процесу                  │ │
│   │ top / htop          → інтерактивний монітор                     │ │
│   │ cat /proc/PID/status→ детальна інформація (PCB)                 │ │
│   │ pstree              → дерево процесів                           │ │
│   │ watch -n 1 "ps..." → моніторинг у реальному часі                │ │
│   └─────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│   ПЕРЕХОДИ:                                                           │
│   ┌─────────────────────────────────────────────────────────────────┐ │
│   │ NEW→READY       : admitted (процес прийнято)                    │ │
│   │ READY→RUNNING   : dispatch (планувальник обрав)                 │ │
│   │ RUNNING→READY   : preempt (час вичерпано)                       │ │
│   │ RUNNING→WAITING : I/O wait (запит I/O)                          │ │
│   │ WAITING→READY   : I/O complete (I/O завершено)                  │ │
│   │ RUNNING→TERMINATED: exit (завершення)                           │ │
│   └─────────────────────────────────────────────────────────────────┘ │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

## 🎯 Міні-проект (30 хв)

### Завдання: Детектив станів процесів

Створіть скрипт, який аналізує стани процесів у системі та генерує звіт. Це типова задача для SRE/DevOps інженера.

**Кроки:**

1. Створіть файл скрипта:
```bash
#!/bin/bash
# process_detective.sh - аналіз станів процесів

REPORT_FILE=~/process_report_$(date +%Y%m%d_%H%M%S).txt

echo "=== ЗВІТ ПРО СТАНИ ПРОЦЕСІВ ===" > $REPORT_FILE
echo "Дата: $(date)" >> $REPORT_FILE
echo "Hostname: $(hostname)" >> $REPORT_FILE
echo "" >> $REPORT_FILE
```

2. Додайте підрахунок процесів за станами:
```bash
echo "--- СТАТИСТИКА СТАНІВ ---" >> $REPORT_FILE
echo "Running (R):  $(ps aux | awk '$8 ~ /R/ {count++} END {print count+0}')" >> $REPORT_FILE
echo "Sleeping (S): $(ps aux | awk '$8 ~ /S/ {count++} END {print count+0}')" >> $REPORT_FILE
echo "Disk I/O (D): $(ps aux | awk '$8 ~ /D/ {count++} END {print count+0}')" >> $REPORT_FILE
echo "Zombie (Z):   $(ps aux | awk '$8 ~ /Z/ {count++} END {print count+0}')" >> $REPORT_FILE
echo "Stopped (T):  $(ps aux | awk '$8 ~ /T/ {count++} END {print count+0}')" >> $REPORT_FILE
echo "" >> $REPORT_FILE
```

3. Додайте виявлення проблемних процесів:
```bash
echo "--- ПОТЕНЦІЙНІ ПРОБЛЕМИ ---" >> $REPORT_FILE

# Zombie-процеси
echo "Zombie-процеси:" >> $REPORT_FILE
ps aux | awk '$8 ~ /Z/ {print "  PID:", $2, "CMD:", $11}' >> $REPORT_FILE

# Процеси у D-state (можуть вказувати на проблеми з диском)
echo "Процеси в uninterruptible sleep (D):" >> $REPORT_FILE
ps aux | awk '$8 ~ /D/ {print "  PID:", $2, "CMD:", $11}' >> $REPORT_FILE
echo "" >> $REPORT_FILE
```

4. Додайте топ споживачів CPU:
```bash
echo "--- ТОП-5 ПО CPU ---" >> $REPORT_FILE
ps aux --sort=-%cpu | head -6 | tail -5 | \
  awk '{printf "  PID: %s, CPU: %s%%, CMD: %s\n", $2, $3, $11}' >> $REPORT_FILE
echo "" >> $REPORT_FILE
```

5. Виведіть результат:
```bash
echo "--- READY QUEUE (процеси, що чекають CPU) ---" >> $REPORT_FILE
ps aux | awk '$8 ~ /R/ && $3 < 1 {print "  PID:", $2, "STAT:", $8, "CMD:", $11}' >> $REPORT_FILE

echo "" >> $REPORT_FILE
echo "Звіт збережено: $REPORT_FILE"
cat $REPORT_FILE
```

**Очікуваний результат:**

Файл `process_report_YYYYMMDD_HHMMSS.txt` з:
- Статистикою процесів за станами
- Списком zombie-процесів (якщо є)
- Процесами у D-state
- Топ-5 споживачів CPU

**Бонус (для допитливих):**
- Додайте перевірку кожні 5 секунд протягом хвилини та порівняйте зміни
- Додайте alert якщо zombie-процесів більше 0
- Використайте `/proc/PID/status` для детальнішої інформації про конкретний процес
- Додайте розділ «Тиск» з `/proc/pressure/cpu`, `memory` та `io` і поясніть значення `avg10`

## Тест для самоперевірки

Десять запитань, у кожному одна правильна відповідь. Якщо тема винесена вашій групі на самостійне опрацювання, цей самий тест публікується в Google Classroom — 10 балів, по одному за кожне запитання.

**1.** Скільки основних станів має процес у класичній моделі?

- п'ять: новий, готовий, виконується, очікує, завершений
- три: створений, виконується, завершений
- сім
- два: активний і неактивний

**2.** Чому процес не може перейти зі стану очікування одразу у виконання?

- після завершення введення-виведення він стає готовим і чекає, поки його обере планувальник
- бо такий перехід заборонений апаратно
- бо він втрачає свій PCB
- бо спершу має завершитися й запуститися знову

**3.** Який перехід називають витісненням (preempt)?

- виконується → готовий, коли вичерпано квант часу
- готовий → виконується, коли планувальник обрав процес
- виконується → очікує, коли запитано введення-виведення
- новий → готовий, коли процес прийнято в систему

**4.** Що означає стан `R` у колонці STAT команди `ps`?

- процес виконується або готовий до виконання
- процес спить в очікуванні події
- процес завершився й чекає на батька
- процес зупинено сполученням Ctrl+Z

**5.** Що означає стан `Z`?

- процес завершився, але батько ще не прочитав його код повернення
- процес заблокований на дисковому введенні-виведенні
- процес зупинено користувачем
- процес виконується з підвищеним пріоритетом

**6.** Чому процес-зомбі не вдається завершити командою `kill`?

- він уже завершений; запис зникне, коли батьківський процес забере його статус
- він виконується в режимі ядра
- у нього немає PID
- його захищає система прав доступу

**7.** Що означає стан `D`?

- непереривне очікування введення-виведення, зазвичай дискового
- процес виконується на іншому ядрі
- процес зупинено відлагоджувачем
- процес завершується

**8.** Що зберігається у блоці керування процесом (PCB)?

- PID, стан, пріоритет, вміст регістрів, карта пам'яті, відкриті файли
- лише ім'я програми та її аргументи
- вихідний код програми
- історія введених користувачем команд

**9.** Скільки процесів може одночасно перебувати у стані виконання на восьмиядерному процесорі?

- не більше восьми
- лише один
- необмежена кількість
- удвічі більше за кількість ядер

**10.** Яка команда покаже дерево процесів?

- pstree
- ps aux
- top
- watch

## Підсумок

| Термін | Визначення |
|--------|------------|
| **Стан процесу** | Поточний етап життєвого циклу (new, ready, running, waiting, terminated) |
| **New** | Процес створюється, виділяються ресурси |
| **Ready** | Готовий до виконання, чекає на CPU |
| **Running** | Активно виконується на процесорі |
| **Waiting** | Заблокований, чекає на I/O або подію |
| **Terminated** | Завершив виконання |
| **PCB** | Process Control Block - структура даних з інформацією про процес |
| **Context Switch** | Перемикання CPU між процесами зі збереженням/відновленням стану |
| **Zombie** | Завершений процес, чий статус не забрано батьком |
| **Ready Queue** | Черга процесів, готових до виконання; у Linux — окрема на кожне ядро |
| **Copy-on-write** | Спільні після `fork()` сторінки копіюються лише під час запису |
| **Стан D** | Непереривне очікування в драйвері; враховується в load average |
| **PSI** | Метрика ядра: частка часу, коли задачі чекали на CPU, пам'ять чи I/O |

| Команда | Призначення |
|---------|-------------|
| `ps aux` | Список всіх процесів зі станами |
| `top` / `htop` | Інтерактивний монітор процесів |
| `cat /proc/PID/status` | Детальна інформація про процес (PCB) |
| `pstree` | Дерево процесів |

На наступній лекції — диспетчеризація та перемикання контексту: як ОС швидко перемикається між процесами.
