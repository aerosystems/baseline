---
title: "Керування процесами в різних ОС"
type: lecture
order: 5
preview: "Процеси в Windows та Linux: Task Manager, ps, top, htop, fork/exec, CreateProcess."
---

## Ви вбиваєте процеси щодня

Кожного разу, коли ви натискаєте "X" на зависшій програмі або відкриваєте Task Manager, щоб "вбити" Chrome, який з'їв 8 ГБ RAM, ви виконуєте операцію керування процесами.

Але що насправді відбувається, коли ви клікаєте "End Task"? Як ОС знає, який процес завершити? І чому іноді навіть це не допомагає?

```
┌───────────────────────────────────────────────────────────────────────┐
│                    КОЖЕН ДЕНЬ ВИ ЦЕ РОБИТЕ                            │
│                                                                       │
│   "Chrome знову завис..."        "Чому ця програма не закривається?"  │
│                                                                       │
│   ┌──────────────────────┐       ┌──────────────────────┐             │
│   │   Task Manager       │       │   $ kill -9 1234     │             │
│   │   ┌────────────────┐ │       │                      │             │
│   │   │ chrome.exe     │ │       │   "Process killed"   │             │
│   │   │ ▓▓▓▓▓▓▓ 4.2 GB │ │       │                      │             │
│   │   │ [End Task]     │ │       │   Що саме сталося?   │             │
│   │   └────────────────┘ │       └──────────────────────┘             │
│   └──────────────────────┘                                            │
│                                                                       │
│   Сьогодні ми розберемося:                                           │
│   • Як переглядати процеси в Windows та Linux                        │
│   • Як процеси створюються та завершуються                           │
│   • Чому Windows і Linux роблять це по-різному                       │
│   • Як стати майстром керування процесами                            │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

**Запитання до аудиторії**: Як ви зазвичай закриваєте зависшу програму? Task Manager? Перезавантаження? А чи знаєте ви, що є командний рядок, який може зробити це швидше і точніше?

## Інструменти моніторингу процесів

### Windows: Task Manager

Task Manager (Диспетчер завдань) - це графічний інструмент Windows для керування процесами. Швидкий доступ: `Ctrl + Shift + Esc`.

```
┌───────────────────────────────────────────────────────────────────────┐
│                    TASK MANAGER: ВКЛАДКИ                              │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   ┌─────────────────────────────────────────────────────────────────┐ │
│   │ Processes │ Performance │ App history │ Startup │ Details │ ... │ │
│   └─────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│   PROCESSES (Процеси):                                               │
│   ├── Apps (запущені програми)                                       │
│   │   ├── Google Chrome          ▓▓▓▓▓▓ 2.1 GB  │ 4.2% CPU          │
│   │   ├── Visual Studio Code     ▓▓▓   890 MB   │ 1.1% CPU          │
│   │   └── Discord                ▓▓    450 MB   │ 0.3% CPU          │
│   │                                                                  │
│   └── Background processes (фонові процеси)                          │
│       ├── Windows Defender       ▓     120 MB   │ 0.1% CPU          │
│       ├── OneDrive               ▓      85 MB   │ 0.0% CPU          │
│       └── ... (200+ процесів)                                        │
│                                                                       │
│   DETAILS (Деталі):                                                  │
│   Показує PID, статус, користувача, CPU, Memory для кожного процесу  │
│                                                                       │
│   PERFORMANCE (Продуктивність):                                      │
│   CPU, Memory, Disk, Network, GPU в реальному часі                   │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

### Windows: Командний рядок (tasklist, taskkill)

Для автоматизації та скриптів використовують команди CMD/PowerShell:

```cmd
:: Показати всі процеси
tasklist

:: Результат:
:: Image Name                     PID Session Name        Mem Usage
:: ========================= ======== ================ ============
:: System Idle Process              0 Services                 8 K
:: System                           4 Services               148 K
:: chrome.exe                   12456 Console            2,156,432 K

:: Фільтрація: знайти процеси Chrome
tasklist /fi "imagename eq chrome.exe"

:: Фільтрація: процеси, що використовують понад 100 МБ RAM
tasklist /fi "memusage gt 100000"

:: Показати процеси у форматі CSV (для скриптів)
tasklist /fo csv

:: Показати процеси з сервісами
tasklist /svc
```

Завершення процесів:

```cmd
:: Завершити процес за ім'ям
taskkill /im notepad.exe

:: Завершити процес за PID
taskkill /pid 12456

:: Примусове завершення (force) - як kill -9 в Linux
taskkill /f /im chrome.exe

:: Завершити процес та всі дочірні процеси (tree)
taskkill /f /t /im chrome.exe

:: Завершити всі процеси, запущені від імені конкретного користувача
taskkill /f /fi "username eq DESKTOP\John"
```

### Linux: ps (process status)

Команда `ps` - це базовий інструмент для перегляду процесів у Linux.

```bash
# Показати процеси поточного терміналу
ps
#   PID TTY          TIME CMD
#  1234 pts/0    00:00:00 bash
#  5678 pts/0    00:00:00 ps

# Показати ВСІ процеси (найпопулярніший варіант)
ps aux
# USER       PID %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND
# root         1  0.0  0.1 169356 13120 ?        Ss   10:00   0:02 /sbin/init
# user      1234  0.5  2.1 458932 172456 ?       Sl   10:01   0:45 /usr/bin/chrome
# ...

# Що означають колонки:
# USER   - власник процесу
# PID    - унікальний ідентифікатор
# %CPU   - використання CPU
# %MEM   - використання RAM
# VSZ    - віртуальна пам'ять (KB)
# RSS    - фізична пам'ять (KB)
# TTY    - термінал (? = фоновий процес)
# STAT   - стан процесу (S=sleep, R=running, Z=zombie)
# START  - час запуску
# TIME   - загальний час CPU
# COMMAND - команда запуску

# Показати дерево процесів (ієрархія)
ps auxf

# Показати процеси конкретного користувача
ps -u username

# Показати тільки PID та команду
ps -eo pid,cmd

# Сортування за використанням пам'яті
ps aux --sort=-%mem | head -10

# Сортування за використанням CPU
ps aux --sort=-%cpu | head -10
```

### Linux: top (table of processes)

Команда `top` показує процеси в реальному часі.

```bash
# Запустити top
top

# Результат (оновлюється кожні 3 секунди):
```

```
┌───────────────────────────────────────────────────────────────────────┐
│ top - 14:25:30 up 5 days, 2:15, 3 users, load average: 0.52, 0.48, 0.45│
│ Tasks: 287 total,   1 running, 286 sleeping,   0 stopped,   0 zombie │
│ %Cpu(s):  2.5 us,  0.8 sy,  0.0 ni, 96.5 id,  0.1 wa,  0.0 hi,  0.1 si│
│ MiB Mem:  16000.0 total,  4500.0 free,  8500.0 used,  3000.0 buff/cache│
│ MiB Swap:  8192.0 total,  8192.0 free,     0.0 used.  7000.0 avail Mem│
│                                                                       │
│   PID USER      PR  NI    VIRT    RES    SHR S  %CPU  %MEM     TIME+  │
│  1234 user      20   0 4582932 172456  85632 S   4.5   2.1   0:45.32  │
│  5678 user      20   0 1258432  98456  45632 S   2.1   1.2   0:12.45  │
│  9012 root      20   0  458932  42456  12632 S   0.8   0.5   0:05.12  │
│  ...                                                                  │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   ГАРЯЧІ КЛАВІШІ в top:                                              │
│   ┌──────────────────────────────────────────────────────────────┐   │
│   │  q       - вийти                                             │   │
│   │  k       - вбити процес (введіть PID)                        │   │
│   │  r       - змінити пріоритет (renice)                        │   │
│   │  M       - сортувати за пам'яттю                             │   │
│   │  P       - сортувати за CPU                                  │   │
│   │  1       - показати кожне ядро окремо                        │   │
│   │  h       - допомога                                          │   │
│   └──────────────────────────────────────────────────────────────┘   │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

### Linux: htop (human-readable top)

`htop` - це покращена версія `top` з кольоровим інтерфейсом та зручною навігацією.

```bash
# Встановлення (якщо не встановлено)
sudo apt install htop   # Debian/Ubuntu
sudo dnf install htop   # Fedora/RHEL

# Запуск
htop
```

```
┌───────────────────────────────────────────────────────────────────────┐
│                              htop                                      │
├───────────────────────────────────────────────────────────────────────┤
│   CPU  [||||||||||||||||||||              45.2%]                      │
│   CPU  [||||||||||||||||                  38.7%]                      │
│   CPU  [||||||||||||||||||||||            52.1%]                      │
│   CPU  [|||||||||||||||                   35.8%]                      │
│   Mem  [||||||||||||||||||||||||||||      8.5G/16.0G]                 │
│   Swp  [                                   0K/8.0G]                   │
│                                                                       │
│   PID USER    PRI  NI  VIRT   RES   SHR S CPU% MEM%   TIME+  Command  │
│  1234 user     20   0 4582M  168M 83.6M S  4.5  2.1  0:45.32 chrome   │
│  5678 user     20   0 1258M 96.1M 44.5M S  2.1  1.2  0:12.45 code     │
│  9012 root     20   0  458M 41.4M 12.3M S  0.8  0.5  0:05.12 systemd  │
│                                                                       │
├───────────────────────────────────────────────────────────────────────┤
│ F1Help  F2Setup  F3Search  F4Filter  F5Tree  F6Sort  F9Kill  F10Quit  │
└───────────────────────────────────────────────────────────────────────┘
```

**Переваги htop над top:**
- Кольорове відображення
- Прокрутка (горизонтальна та вертикальна)
- Дерево процесів (F5)
- Пошук та фільтрація (F3, F4)
- Вбивання процесів мишкою (F9)
- Налаштування відображення (F2)

## Завершення процесів

### Linux: kill, killall, pkill

```bash
# kill - надсилає сигнал процесу за PID
kill 1234           # Надсилає SIGTERM (15) - "завершись нормально"
kill -15 1234       # Те саме, що й вище
kill -9 1234        # Надсилає SIGKILL (9) - "вбий негайно"
kill -SIGKILL 1234  # Те саме, що й вище

# killall - завершує процеси за ім'ям
killall chrome      # Завершити всі процеси chrome
killall -9 firefox  # Примусово вбити всі процеси firefox

# pkill - завершує процеси за шаблоном
pkill chrome        # Завершити процеси, що містять "chrome"
pkill -u user       # Завершити всі процеси користувача
pkill -f "python script.py"  # За повним командним рядком

# Подивитися всі доступні сигнали
kill -l
#  1) SIGHUP       2) SIGINT       3) SIGQUIT      4) SIGILL
#  5) SIGTRAP      6) SIGABRT      7) SIGBUS       8) SIGFPE
#  9) SIGKILL     10) SIGUSR1     11) SIGSEGV     12) SIGUSR2
# 13) SIGPIPE     14) SIGALRM     15) SIGTERM     ...
```

### Найважливіші сигнали

| Сигнал | Номер | Опис | Можна перехопити? |
|--------|-------|------|-------------------|
| **SIGTERM** | 15 | "Будь ласка, завершись" (graceful shutdown) | Так |
| **SIGKILL** | 9 | "Вбий негайно" (не дає процесу зреагувати) | **Ні** |
| **SIGINT** | 2 | Переривання (Ctrl+C) | Так |
| **SIGHUP** | 1 | "Термінал закрито" (часто = перезавантаження конфігу) | Так |
| **SIGSTOP** | 19 | Призупинити процес | **Ні** |
| **SIGCONT** | 18 | Продовжити призупинений процес | Так |

```bash
# Практичний приклад: коректне завершення
kill 1234           # Спробувати SIGTERM
sleep 5             # Почекати 5 секунд
kill -0 1234 2>/dev/null && kill -9 1234  # Якщо ще живий - SIGKILL
```

## Пріоритети процесів: nice та renice

Кожен процес має пріоритет, який визначає, скільки часу CPU він отримує.

```
┌───────────────────────────────────────────────────────────────────────┐
│                    ПРІОРИТЕТИ ПРОЦЕСІВ (nice)                         │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   nice value:  -20 ─────────────────────────────────────────── +19    │
│                 │                    │                          │     │
│                 ▼                    ▼                          ▼     │
│            Найвищий             Звичайний              Найнижчий      │
│            пріоритет            пріоритет              пріоритет      │
│            (root only)          (default=0)           (будь-хто)     │
│                                                                       │
│   Приклади:                                                          │
│   ┌───────────────────────────────────────────────────────────────┐  │
│   │  nice = -20  │  Системні критичні процеси (тільки root)       │  │
│   │  nice = -10  │  Важливі сервіси (тільки root)                 │  │
│   │  nice =   0  │  Звичайні програми (default)                   │  │
│   │  nice = +10  │  Фонові завдання (backup, індексація)          │  │
│   │  nice = +19  │  Найменш важливі (torrent, render у фоні)      │  │
│   └───────────────────────────────────────────────────────────────┘  │
│                                                                       │
│   Правило: Чим МЕНШЕ число → ТИМ БІЛЬШЕ пріоритет                    │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

```bash
# nice - запустити процес з певним пріоритетом
nice -n 10 ./heavy_task.sh    # Низький пріоритет (+10)
nice -n -5 ./important.sh     # Високий пріоритет (-5, потрібен sudo)
sudo nice -n -20 ./critical   # Максимальний пріоритет (тільки root)

# renice - змінити пріоритет запущеного процесу
renice 10 -p 1234             # Знизити пріоритет процесу 1234
sudo renice -5 -p 1234        # Підвищити пріоритет (потрібен sudo)
renice 15 -u username         # Знизити пріоритет всіх процесів користувача

# Подивитися nice value в ps
ps -eo pid,ni,cmd | head -10
#   PID  NI CMD
#     1   0 /sbin/init
#  1234  10 /usr/bin/backup
#  5678 -10 /usr/bin/mysql
```

## Створення процесів: fork/exec vs CreateProcess

Це одна з фундаментальних відмінностей між Linux та Windows.

### Linux: fork() + exec()

Linux використовує двоетапну модель створення процесів:

```
┌───────────────────────────────────────────────────────────────────────┐
│                    LINUX: fork() + exec()                             │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   1. fork() - КЛОНУВАННЯ                                             │
│                                                                       │
│   ┌─────────────────┐          ┌─────────────────┐                   │
│   │  Parent Process │   fork   │  Parent Process │                   │
│   │  PID: 1000      │ ───────► │  PID: 1000      │                   │
│   │                 │          │  fork() = 1001  │ (повертає PID)    │
│   │  ┌───────────┐  │          │  ┌───────────┐  │                   │
│   │  │  Код      │  │          │  │  Код      │  │                   │
│   │  │  Дані     │  │          │  │  Дані     │  │                   │
│   │  │  Heap     │  │          │  │  Heap     │  │                   │
│   │  │  Stack    │  │          │  │  Stack    │  │                   │
│   │  └───────────┘  │          │  └───────────┘  │                   │
│   └─────────────────┘          └─────────────────┘                   │
│                                        │                              │
│                                        │ (створюється копія)         │
│                                        ▼                              │
│                                ┌─────────────────┐                   │
│                                │  Child Process  │                   │
│                                │  PID: 1001      │                   │
│                                │  fork() = 0     │ (повертає 0)      │
│                                │  ┌───────────┐  │                   │
│                                │  │  Код      │  │ (копія!)         │
│                                │  │  Дані     │  │ (копія!)         │
│                                │  │  Heap     │  │ (копія!)         │
│                                │  │  Stack    │  │ (копія!)         │
│                                │  └───────────┘  │                   │
│                                └─────────────────┘                   │
│                                                                       │
│   2. exec() - ЗАМІНА (опціонально)                                   │
│                                                                       │
│   ┌─────────────────┐  exec    ┌─────────────────┐                   │
│   │  Child Process  │ ("/bin") │  Child Process  │                   │
│   │  PID: 1001      │ ───────► │  PID: 1001      │ (PID той самий!)  │
│   │  ┌───────────┐  │          │  ┌───────────┐  │                   │
│   │  │ Старий код│  │          │  │ Новий код │  │ (заміна!)        │
│   │  │ Старі дані│  │          │  │ Нові дані │  │ (заміна!)        │
│   │  └───────────┘  │          │  └───────────┘  │                   │
│   └─────────────────┘          └─────────────────┘                   │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

**Приклад на C:**

```c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/wait.h>

int main() {
    printf("Батьківський процес: PID = %d\n", getpid());

    pid_t pid = fork();  // Створюємо копію процесу

    if (pid < 0) {
        // Помилка
        perror("fork failed");
        exit(1);
    }
    else if (pid == 0) {
        // Дочірній процес (fork повернув 0)
        printf("Дочірній процес: PID = %d, Parent PID = %d\n",
               getpid(), getppid());

        // Замінюємо себе на іншу програму
        execlp("ls", "ls", "-la", NULL);

        // Якщо exec успішний, цей код НЕ виконається
        perror("exec failed");
        exit(1);
    }
    else {
        // Батьківський процес (fork повернув PID дитини)
        printf("Батько: створив дитину з PID = %d\n", pid);

        // Чекаємо завершення дочірнього процесу
        int status;
        waitpid(pid, &status, 0);
        printf("Батько: дитина завершилась зі статусом %d\n",
               WEXITSTATUS(status));
    }

    return 0;
}
```

```bash
# Компіляція та запуск
gcc -o fork_demo fork_demo.c
./fork_demo

# Вивід:
# Батьківський процес: PID = 1234
# Батько: створив дитину з PID = 1235
# Дочірній процес: PID = 1235, Parent PID = 1234
# total 8
# drwxr-xr-x  2 user user 4096 Oct 15 14:30 .
# ...
# Батько: дитина завершилась зі статусом 0
```

**Чому fork+exec?**
- **Гнучкість**: між fork і exec можна налаштувати дочірній процес (переспрямування I/O, зміна середовища)
- **Ефективність**: Copy-on-Write (CoW) - фізичне копіювання відбувається лише при модифікації
- **Unix-філософія**: прості примітиви, що комбінуються

### Windows: CreateProcess()

Windows використовує одноетапну модель:

```
┌───────────────────────────────────────────────────────────────────────┐
│                    WINDOWS: CreateProcess()                           │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   ┌─────────────────┐                      ┌─────────────────┐       │
│   │  Parent Process │   CreateProcess      │  New Process    │       │
│   │  PID: 1000      │ ─────────────────►   │  PID: 2000      │       │
│   │                 │   ("notepad.exe")    │                 │       │
│   │  ┌───────────┐  │                      │  ┌───────────┐  │       │
│   │  │  Код A    │  │   Не клонує!         │  │  Код B    │  │       │
│   │  │  Дані A   │  │   Створює новий      │  │  Дані B   │  │       │
│   │  └───────────┘  │   процес з нуля      │  │  (нові!)  │  │       │
│   └─────────────────┘                      │  └───────────┘  │       │
│                                            └─────────────────┘       │
│                                                                       │
│   Один виклик = завантаження exe + створення процесу                 │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

**Приклад на C (Windows API):**

```c
#include <windows.h>
#include <stdio.h>

int main() {
    STARTUPINFO si;
    PROCESS_INFORMATION pi;

    ZeroMemory(&si, sizeof(si));
    si.cb = sizeof(si);
    ZeroMemory(&pi, sizeof(pi));

    // Створюємо новий процес
    if (!CreateProcess(
        NULL,                   // Ім'я модуля (NULL = з командного рядка)
        "notepad.exe",          // Командний рядок
        NULL,                   // Атрибути безпеки процесу
        NULL,                   // Атрибути безпеки потоку
        FALSE,                  // Наслідування хендлів
        0,                      // Прапори створення
        NULL,                   // Середовище (NULL = батьківське)
        NULL,                   // Робочий каталог (NULL = поточний)
        &si,                    // Структура STARTUPINFO
        &pi))                   // Структура PROCESS_INFORMATION
    {
        printf("CreateProcess failed (%d).\n", GetLastError());
        return 1;
    }

    printf("Successfully created process with PID: %lu\n",
           pi.dwProcessId);

    // Чекаємо завершення процесу
    WaitForSingleObject(pi.hProcess, INFINITE);

    // Закриваємо хендли
    CloseHandle(pi.hProcess);
    CloseHandle(pi.hThread);

    return 0;
}
```

### Порівняння: fork/exec vs CreateProcess

| Аспект | Linux (fork/exec) | Windows (CreateProcess) |
|--------|-------------------|-------------------------|
| **Кількість етапів** | 2 (fork + exec) | 1 |
| **Клонування батька** | Так (fork) | Ні |
| **Copy-on-Write** | Так | Ні (одразу нова пам'ять) |
| **Гнучкість** | Висока (налаштування між fork і exec) | Параметри через структури |
| **Наслідування** | Повне клонування | Вибіркове (хендли, середовище) |
| **Типове використання** | Shell, демони, fork-сервери | Запуск програм, сервіси |
| **Складність API** | Простіший (2 прості функції) | Складніший (багато параметрів) |

## Завершення процесів

### Нормальне завершення

```c
// Linux
#include <stdlib.h>

int main() {
    // ... код програми ...

    exit(0);        // Нормальне завершення
    exit(1);        // Завершення з помилкою

    return 0;       // Еквівалент exit(0) для main()
}
```

```c
// Windows
#include <windows.h>

int main() {
    // ... код програми ...

    ExitProcess(0);  // Завершити поточний процес
    return 0;
}
```

### Примусове завершення

```
┌───────────────────────────────────────────────────────────────────────┐
│                    ЗАВЕРШЕННЯ ПРОЦЕСІВ                                │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   LINUX                              WINDOWS                          │
│   ─────                              ───────                          │
│                                                                       │
│   kill PID                           taskkill /pid PID                │
│   └── SIGTERM (graceful)             └── WM_CLOSE (graceful)          │
│                                                                       │
│   kill -9 PID                        taskkill /f /pid PID             │
│   └── SIGKILL (force)                └── TerminateProcess (force)     │
│                                                                       │
│   kill -STOP PID                     (немає прямого аналога)          │
│   └── Призупинити                                                     │
│                                                                       │
│   kill -CONT PID                     (немає прямого аналога)          │
│   └── Продовжити                                                      │
│                                                                       │
│   ┌───────────────────────────────────────────────────────────────┐   │
│   │  Що відбувається при завершенні:                              │   │
│   │  1. ОС звільняє всю пам'ять процесу                          │   │
│   │  2. Закриваються всі відкриті файли/сокети                   │   │
│   │  3. Дочірні процеси "усиновлюються" init/systemd (Linux)     │   │
│   │  4. Батьківський процес отримує exit code                    │   │
│   │  5. PCB (Process Control Block) видаляється                  │   │
│   └───────────────────────────────────────────────────────────────┘   │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

## Ієрархія процесів

### Linux: Дерево процесів

У Linux кожен процес (крім init/systemd) має батька. Це утворює дерево:

```
┌───────────────────────────────────────────────────────────────────────┐
│                    LINUX: ДЕРЕВО ПРОЦЕСІВ                             │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   init/systemd (PID 1) ── КОРІНЬ ДЕРЕВА                              │
│   │                                                                   │
│   ├── systemd-journald (PID 234)                                     │
│   │                                                                   │
│   ├── sshd (PID 567)                                                 │
│   │   │                                                               │
│   │   └── sshd (PID 890) ── session                                  │
│   │       │                                                           │
│   │       └── bash (PID 891)                                         │
│   │           │                                                       │
│   │           ├── vim (PID 1234)                                     │
│   │           │                                                       │
│   │           └── python (PID 1235)                                  │
│   │               │                                                   │
│   │               └── worker (PID 1236)                              │
│   │                                                                   │
│   ├── gdm (PID 678)                                                  │
│   │   │                                                               │
│   │   └── gnome-shell (PID 789)                                      │
│   │       │                                                           │
│   │       ├── firefox (PID 1000)                                     │
│   │       │   ├── Web Content (PID 1001)                             │
│   │       │   ├── Web Content (PID 1002)                             │
│   │       │   └── WebExtensions (PID 1003)                           │
│   │       │                                                           │
│   │       └── nautilus (PID 1100)                                    │
│   │                                                                   │
│   └── cron (PID 345)                                                 │
│                                                                       │
│   Команди для перегляду:                                             │
│   pstree           - ASCII-дерево процесів                           │
│   pstree -p        - з PID                                           │
│   pstree -u        - з іменами користувачів                          │
│   ps auxf          - дерево в форматі ps                             │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

```bash
# Приклад виводу pstree
pstree -p

# systemd(1)─┬─ModemManager(789)───2*[{ModemManager}]
#            ├─NetworkManager(654)───2*[{NetworkManager}]
#            ├─gdm(678)───gdm-session-wor(890)───gdm-wayland-ses(891)─┬─...
#            │                                                        └─gnome-shell(892)
#            ├─sshd(567)───sshd(1234)───bash(1235)───vim(1236)
#            └─systemd-journal(234)
```

### Orphan та Zombie процеси

```
┌───────────────────────────────────────────────────────────────────────┐
│                    ORPHAN та ZOMBIE ПРОЦЕСИ                           │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   ORPHAN (Сирота)                                                    │
│   ───────────────                                                    │
│   Батько завершився раніше, ніж дитина                               │
│                                                                       │
│   ┌────────┐         ┌────────┐         ┌────────┐                   │
│   │ Parent │ ────►   │  X X   │         │ init/  │                   │
│   │ PID:100│ dies    │ Parent │         │systemd │                   │
│   └────────┘         │ gone!  │         │ PID:1  │                   │
│       │              └────────┘         └────────┘                   │
│       │                                      │                        │
│   ┌───▼────┐                            ┌───▼────┐                   │
│   │ Child  │  ──── "усиновлюється" ───► │ Child  │                   │
│   │ PID:101│                            │ PID:101│                   │
│   │ PPID:100                            │ PPID:1 │ ← новий батько    │
│   └────────┘                            └────────┘                   │
│                                                                       │
│   Orphan процеси - це НОРМАЛЬНО. Init/systemd їх "підбирає".         │
│                                                                       │
│   ─────────────────────────────────────────────────────────────────  │
│                                                                       │
│   ZOMBIE (Зомбі)                                                     │
│   ──────────────                                                     │
│   Дитина завершилась, але батько НЕ викликав wait()                  │
│                                                                       │
│   ┌────────┐                                                         │
│   │ Parent │  ← не викликає wait(), зайнятий чимось іншим            │
│   │ PID:100│                                                         │
│   └────────┘                                                         │
│       │                                                               │
│   ┌───▼────┐                                                         │
│   │ Child  │  ← ZOMBIE: завершився, але PCB залишається              │
│   │ PID:101│    (чекає, поки батько прочитає exit code)              │
│   │ STATUS:│                                                         │
│   │   Z    │  ← Стан "Z" в ps                                        │
│   └────────┘                                                         │
│                                                                       │
│   ps aux | grep Z    ← Показати zombie процеси                       │
│   # USER PID %CPU %MEM ... STAT ... COMMAND                          │
│   # user 1234 0.0  0.0 ... Z    ... [defunct]                        │
│                                                                       │
│   Проблема: zombie займають запис у таблиці процесів.                │
│   Рішення: батько має викликати wait() або завершитися.              │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

### Windows: Плоска модель

У Windows немає суворої ієрархії. Процеси більш незалежні:

```
┌───────────────────────────────────────────────────────────────────────┐
│                    WINDOWS: ПЛОСКА МОДЕЛЬ                             │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐               │
│   │   smss.exe   │  │  csrss.exe   │  │ services.exe │               │
│   │   PID: 4     │  │   PID: 456   │  │   PID: 567   │               │
│   └──────────────┘  └──────────────┘  └──────────────┘               │
│          │                                    │                       │
│          │                                    │                       │
│   ┌──────▼──────┐                     ┌──────▼──────┐                │
│   │ winlogon.exe│                     │ svchost.exe │                │
│   │  PID: 678   │                     │  PID: 789   │                │
│   └─────────────┘                     └─────────────┘                │
│                                               │                       │
│   ┌─────────────┐  ┌─────────────┐           │                       │
│   │ explorer.exe│  │ chrome.exe  │    ┌──────▼──────┐                │
│   │  PID: 890   │  │  PID: 1234  │    │ svchost.exe │                │
│   └─────────────┘  └─────────────┘    │  PID: 891   │                │
│          │                            └─────────────┘                │
│          │                                                            │
│   ┌──────▼──────┐                                                    │
│   │ notepad.exe │  ← Запущений з Explorer, але після закриття       │
│   │  PID: 2345  │    Explorer продовжує працювати!                  │
│   └─────────────┘                                                    │
│                                                                       │
│   Відмінності від Linux:                                             │
│   • Немає "усиновлення" orphan процесів                              │
│   • Батьківський PID зберігається, але батько може не існувати      │
│   • Процеси більш автономні                                          │
│   • Job Objects для групування (аналог process groups)               │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

## Порівняння Windows vs Linux

| Аспект | Linux | Windows |
|--------|-------|---------|
| **Перегляд процесів (GUI)** | htop, System Monitor | Task Manager |
| **Перегляд процесів (CLI)** | ps, top, htop | tasklist |
| **Завершення процесів (CLI)** | kill, killall, pkill | taskkill |
| **Створення процесів** | fork() + exec() | CreateProcess() |
| **Модель ієрархії** | Дерево (init = корінь) | Плоска (незалежні) |
| **Orphan процеси** | Усиновлюються init | Залишаються незалежними |
| **Zombie процеси** | Можливі | Немає (інша модель) |
| **Сигнали** | Розвинена система (64+) | Обмежена (WM_CLOSE, TerminateProcess) |
| **Пріоритети** | nice (-20 до +19) | Priority Class (6 рівнів) |
| **Ідентифікатор** | PID (число) | PID + Handle |

## 🏢 Real World: Як це використовують у великих компаніях

```
┌───────────────────────────────────────────────────────────────────────┐
│                КЕРУВАННЯ ПРОЦЕСАМИ У PRODUCTION                       │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  NETFLIX                                                              │
│  ├── systemd для керування сотнями мікросервісів                     │
│  ├── Process supervisors: runit, supervisord для автоперезапуску     │
│  └── Результат: 99.99% uptime, мільйони стрімів одночасно            │
│                                                                       │
│  GOOGLE                                                               │
│  ├── Borg/Kubernetes: оркестрація мільйонів контейнерів              │
│  ├── Кожен контейнер = ізольований процес                            │
│  └── cgroups для обмеження ресурсів кожного процесу                  │
│                                                                       │
│  CLOUDFLARE                                                           │
│  ├── Worker processes: тисячі процесів на кожному сервері            │
│  ├── NGINX fork-модель: master + worker processes                    │
│  └── Graceful reload: оновлення без downtime                         │
│                                                                       │
│  MICROSOFT AZURE                                                      │
│  ├── Windows Server: Service Control Manager (SCM)                   │
│  ├── Job Objects для ізоляції груп процесів                          │
│  └── Process isolation для безпеки VM                                │
│                                                                       │
│  POSTGRESQL / MYSQL                                                   │
│  ├── postmaster/mysqld: головний процес                              │
│  ├── Fork для кожного клієнтського з'єднання                         │
│  └── Shared memory для кешу (ефективна комунікація)                  │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

## 💼 Career Spotlight

```
┌───────────────────────────────────────────────────────────────────────┐
│                    КАР'ЄРНІ МОЖЛИВОСТІ                                │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  LINUX SYSTEM ADMINISTRATOR                                           │
│  Керування серверами, моніторинг процесів, автоматизація             │
│  Зарплата: $70K-$130K (USA) | €45K-€75K (EU)                         │
│  Компанії: Red Hat, Canonical, будь-яка IT-компанія                  │
│                                                                       │
│  SITE RELIABILITY ENGINEER (SRE)                                      │
│  Забезпечення надійності сервісів, process management at scale       │
│  Зарплата: $130K-$220K (USA) | €70K-€120K (EU)                       │
│  Компанії: Google, Meta, Netflix, Spotify, Stripe                    │
│                                                                       │
│  DEVOPS ENGINEER                                                      │
│  CI/CD, контейнеризація, оркестрація процесів                        │
│  Зарплата: $100K-$180K (USA) | €55K-€95K (EU)                        │
│  Компанії: GitLab, HashiCorp, Docker, AWS, Microsoft                 │
│                                                                       │
│  WINDOWS SYSTEMS ENGINEER                                             │
│  Адміністрування Windows Server, Active Directory, PowerShell        │
│  Зарплата: $80K-$140K (USA) | €50K-€85K (EU)                         │
│  Компанії: Microsoft, Enterprise-компанії, банки, уряд               │
│                                                                       │
│  SECURITY ENGINEER                                                    │
│  Аналіз процесів, виявлення malware, форензика                       │
│  Зарплата: $110K-$190K (USA) | €60K-€100K (EU)                       │
│  Компанії: CrowdStrike, Palo Alto, Mandiant, урядові агенції         │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

## 🔗 Корисні ресурси

### Онлайн-практика

| Ресурс | Опис | Посилання |
|--------|------|-----------|
| **Linux Journey** | Інтерактивний курс з process management | `linuxjourney.com/lesson/processes` |
| **OverTheWire Bandit** | CTF для практики Linux команд | `overthewire.org/wargames/bandit` |
| **Killercoda** | Інтерактивні Linux сценарії в браузері | `killercoda.com/linux` |
| **TryHackMe** | Linux Fundamentals | `tryhackme.com/room/linuxfundamentalspart1` |
| **Microsoft Learn** | Windows Process Management | `learn.microsoft.com/windows` |

## 📋 Cheat Sheet

```
┌───────────────────────────────────────────────────────────────────────┐
│              ШПАРГАЛКА: КЕРУВАННЯ ПРОЦЕСАМИ                           │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  LINUX - ПЕРЕГЛЯД:                                                   │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │ ps aux              → всі процеси (snapshot)                    │  │
│  │ ps auxf             → дерево процесів                           │  │
│  │ top                 → процеси в реальному часі                  │  │
│  │ htop                → покращений top з кольорами                │  │
│  │ pstree -p           → дерево з PID                              │  │
│  │ ps -eo pid,ni,cmd   → PID, nice, команда                        │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  LINUX - ЗАВЕРШЕННЯ:                                                 │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │ kill PID            → SIGTERM (graceful)                        │  │
│  │ kill -9 PID         → SIGKILL (force)                           │  │
│  │ killall name        → всі процеси за ім'ям                      │  │
│  │ pkill pattern       → процеси за шаблоном                       │  │
│  │ kill -STOP PID      → призупинити                               │  │
│  │ kill -CONT PID      → продовжити                                │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  LINUX - ПРІОРИТЕТИ:                                                 │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │ nice -n 10 cmd      → запустити з низьким пріоритетом          │  │
│  │ renice 10 -p PID    → змінити пріоритет процесу                 │  │
│  │ nice range: -20 (highest) to +19 (lowest)                       │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  WINDOWS - CMD:                                                      │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │ tasklist            → список процесів                           │  │
│  │ tasklist /fi "..."  → з фільтром                                │  │
│  │ taskkill /pid PID   → завершити за PID                          │  │
│  │ taskkill /f /im name→ примусово за ім'ям                        │  │
│  │ taskkill /f /t /im  → з дочірніми процесами                     │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  СТВОРЕННЯ ПРОЦЕСІВ:                                                 │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │ Linux:  fork() → клонування, exec() → заміна                    │  │
│  │ Windows: CreateProcess() → одразу новий процес                  │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  ІЄРАРХІЯ:                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │ Linux:  Дерево (init/systemd = корінь, orphan усиновлюються)   │  │
│  │ Windows: Плоска модель (процеси більш незалежні)               │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

## ❓ Питання для самоперевірки

1. **Чим відрізняється SIGTERM від SIGKILL? Коли варто використовувати кожен?**

2. **Що таке zombie-процес і як він виникає? Чи може zombie-процес споживати CPU?**

3. **Поясніть модель fork/exec у Linux. Чому не створити одразу новий процес, як у Windows?**

4. **Яка різниця між ієрархією процесів у Linux та Windows? Що відбувається з дочірнім процесом, коли батько завершується?**

5. **Як змінити пріоритет вже запущеного процесу в Linux? Чому для підвищення пріоритету потрібен root?**

## 🎯 Міні-проект (30 хв)

### Завдання: Створення Process Monitor скрипта

Створіть bash-скрипт, який моніторить процеси у вашій системі та генерує звіт.

**Кроки:**

1. Створіть скрипт:
```bash
#!/bin/bash
# process_monitor.sh - Моніторинг процесів

REPORT_FILE="$HOME/process_report_$(date +%Y%m%d_%H%M%S).txt"

echo "========================================" > $REPORT_FILE
echo "    PROCESS MONITORING REPORT" >> $REPORT_FILE
echo "    $(date)" >> $REPORT_FILE
echo "========================================" >> $REPORT_FILE
```

2. Додайте інформацію про систему:
```bash
echo "" >> $REPORT_FILE
echo "=== SYSTEM INFO ===" >> $REPORT_FILE
echo "Hostname: $(hostname)" >> $REPORT_FILE
echo "Kernel: $(uname -r)" >> $REPORT_FILE
echo "Uptime: $(uptime -p)" >> $REPORT_FILE
```

3. Зберіть статистику процесів:
```bash
echo "" >> $REPORT_FILE
echo "=== PROCESS STATISTICS ===" >> $REPORT_FILE
echo "Total processes: $(ps aux | wc -l)" >> $REPORT_FILE
echo "Running: $(ps aux | grep -c ' R ')" >> $REPORT_FILE
echo "Sleeping: $(ps aux | grep -c ' S ')" >> $REPORT_FILE
echo "Zombie: $(ps aux | grep -c ' Z ')" >> $REPORT_FILE
```

4. Знайдіть ТОП-5 процесів за CPU та RAM:
```bash
echo "" >> $REPORT_FILE
echo "=== TOP 5 BY CPU ===" >> $REPORT_FILE
ps aux --sort=-%cpu | head -6 >> $REPORT_FILE

echo "" >> $REPORT_FILE
echo "=== TOP 5 BY MEMORY ===" >> $REPORT_FILE
ps aux --sort=-%mem | head -6 >> $REPORT_FILE
```

5. Додайте дерево процесів поточного користувача:
```bash
echo "" >> $REPORT_FILE
echo "=== MY PROCESS TREE ===" >> $REPORT_FILE
pstree -p $USER >> $REPORT_FILE

echo "" >> $REPORT_FILE
echo "Report saved to: $REPORT_FILE"
cat $REPORT_FILE
```

6. Зробіть скрипт виконуваним та запустіть:
```bash
chmod +x process_monitor.sh
./process_monitor.sh
```

**Очікуваний результат:**
Файл `process_report_YYYYMMDD_HHMMSS.txt` зі статистикою процесів, топ-споживачами CPU та RAM, та деревом ваших процесів.

**Бонус (для допитливих):**
- Додайте моніторинг конкретного процесу (наприклад, firefox) з записом у лог кожні 5 секунд
- Реалізуйте алерт, якщо процес споживає понад 50% CPU
- Створіть аналогічний скрипт для Windows (PowerShell):
  ```powershell
  Get-Process | Sort-Object CPU -Descending | Select-Object -First 5
  ```

## Підсумок

| Термін | Визначення |
|--------|------------|
| **PID** | Process ID - унікальний ідентифікатор процесу |
| **PPID** | Parent Process ID - PID батьківського процесу |
| **fork()** | Системний виклик Linux для клонування процесу |
| **exec()** | Заміна коду процесу на новий executable |
| **CreateProcess()** | Windows API для створення нового процесу |
| **SIGTERM** | Сигнал "завершись коректно" (можна перехопити) |
| **SIGKILL** | Сигнал "завершись негайно" (не можна перехопити) |
| **nice/renice** | Керування пріоритетом процесу (-20 до +19) |
| **Zombie** | Завершений процес, що чекає wait() від батька |
| **Orphan** | Процес, чий батько завершився (усиновлюється init) |

| Операція | Linux | Windows |
|----------|-------|---------|
| Перегляд процесів | ps, top, htop | tasklist, Task Manager |
| Завершення процесу | kill, killall | taskkill |
| Створення процесу | fork + exec | CreateProcess |
| Ієрархія | Дерево (init = корінь) | Плоска модель |

На наступній лекції - алгоритми планування процесів: як ОС вирішує, який процес отримає CPU наступним.
