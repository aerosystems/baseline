---
title: "Огляд операційної системи Linux"
type: lecture
order: 16
preview: "Історія Linux, ядро, GNU/Linux."
---

## Linux — основа сучасної інфраструктури

Linux працює скрізь:
- **близько 90%** сайтів, чию ОС удається визначити, працюють на Unix-подібних системах; сам Linux — приблизно 60% (W3Techs, 2026)
- **близько 70%** мобільних пристроїв — Android, тобто ядро Linux (StatCounter, 2026)
- **100%** суперкомп'ютерів зі списку TOP500 (червень 2026)
- **Tesla**, SpaceX Dragon, марсіанський гелікоптер NASA Ingenuity
- Ваш роутер, Smart TV, холодильник

При цьому на десктопах лише ~4%. Чому така різниця? Розберемося.

## Народження Linux

```
┌────────────────────────────────────────────────────────────────┐
│                    25 СЕРПНЯ 1991 РОКУ                         │
│                                                                │
│   Лінус Торвальдс (21 рік, студент Університету Гельсінкі)     │
│   публікує в comp.os.minix:                                    │
│                                                                │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │ "Hello everybody out there using minix -                │  │
│   │                                                         │  │
│   │  I'm doing a (free) operating system (just a hobby,     │  │
│   │  won't be big and professional like gnu) for 386(486)   │  │
│   │  AT clones."                                            │  │
│   │                                                         │  │
│   │  "It is NOT portable (uses 386 task switching etc),     │  │
│   │   and it probably never will support anything other     │  │
│   │   than AT-harddisks, as that's all I have :-("          │  │
│   └─────────────────────────────────────────────────────────┘  │
│                                                                │
│   Сьогодні Linux:                                              │
│   • понад 30 мільйонів рядків коду                             │
│   • ~2 000 розробників у кожному випуску, понад 20 000 за      │
│     історію проєкту                                            │
│   • Працює на ARM, x86, RISC-V, PowerPC, s390...               │
└────────────────────────────────────────────────────────────────┘
```

### Хронологія

| Рік | Подія |
|-----|-------|
| 1991 | Linux 0.01 — перший реліз (10,000 рядків) |
| 1992 | Linux під GPL, перші дистрибутиви |
| 1994 | Linux 1.0 |
| 1996 | Tux (пінгвін) — офіційний маскот |
| 2000 | IBM інвестує $1 млрд в Linux |
| 2003 | Linux 2.6 — масштабованість для серверів |
| 2008 | Android 1.0 (Linux kernel) |
| 2011 | Linux 3.0 |
| 2015 | Linux 4.0 |
| 2019 | Linux 5.0 |
| 2022 | Linux 6.0; у 6.1 — перший код мовою Rust |
| 2023 | Linux 6.6 — новий планувальник EEVDF замість CFS |
| 2024 | Linux 6.12 — PREEMPT_RT (реальний час) в основному ядрі |

Нові «великі» номери версій нічого не означають: Торвальдс змінює першу цифру, коли друга стає, за його словами, завеликою, щоб рахувати на пальцях рук і ніг. Важливі зміни потрапляють у ядро поступово, у кожному випуску раз на дев'ять-десять тижнів.

## GNU/Linux

```
┌────────────────────────────────────────────────────────────────┐
│                    GNU + LINUX = GNU/Linux                     │
│                                                                │
│   1983: Richard Stallman оголошує проект GNU                   │
│         (GNU's Not Unix)                                       │
│                                                                │
│   Мета: створити повністю вільну UNIX-подібну ОС               │
│                                                                │
│   GNU проект створив:                                          │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │  • GCC (компілятор)                                     │  │
│   │  • Bash (shell)                                         │  │
│   │  • coreutils (ls, cp, mv, rm...)                        │  │
│   │  • glibc (стандартна бібліотека C)                      │  │
│   │  • Emacs (редактор)                                     │  │
│   │  • ...але не ядро (GNU Hurd досі в розробці)            │  │
│   └─────────────────────────────────────────────────────────┘  │
│                              +                                 │
│   1991: Лінус Торвальдс створює ядро Linux                     │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │  • Kernel (ядро операційної системи)                    │  │
│   └─────────────────────────────────────────────────────────┘  │
│                              =                                 │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │              Повноцінна ОС: GNU/Linux                   │  │
│   │                                                         │  │
│   │  Формально правильно: GNU/Linux                         │  │
│   │  Всі кажуть просто: Linux                               │  │
│   └─────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
```

## Архітектура Linux

```
┌─────────────────────────────────────────────────────────────────┐
│                    АРХІТЕКТУРА LINUX                            │
│                                                                 │
│   USER SPACE                                                    │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                                                         │   │
│   │  Applications                                           │   │
│   │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐    │   │
│   │  │ Firefox  │ │ Terminal │ │ VS Code  │ │  Docker  │    │   │
│   │  └──────────┘ └──────────┘ └──────────┘ └──────────┘    │   │
│   │                                                         │   │
│   │  System Libraries                                       │   │
│   │  ┌──────────┐ ┌──────────┐ ┌──────────┐                 │   │
│   │  │  glibc   │ │libstdc++ │ │ libssl   │ ...             │   │
│   │  └──────────┘ └──────────┘ └──────────┘                 │   │
│   │                                                         │   │
│   │  Shell & Utilities (GNU)                                │   │
│   │  ┌──────────┐ ┌──────────┐ ┌──────────┐                 │   │
│   │  │  bash    │ │coreutils │ │  grep    │ ...             │   │
│   │  └──────────┘ └──────────┘ └──────────┘                 │   │
│   │                                                         │   │
│   └───────────────────────────┬─────────────────────────────┘   │
│                               │ System Calls                    │
│   ════════════════════════════╪════════════════════════════     │
│                               │                                 │
│   KERNEL SPACE                ▼                                 │
│   ┌────────────────────────────────────────────────────────┐    │
│   │                                                        │    │
│   │  System Call Interface                                 │    │
│   │  ────────────────────────────────────────────────────  │    │
│   │                                                        │    │
│   │  ┌────────────┐ ┌────────────┐ ┌────────────┐          │    │
│   │  │  Process   │ │   Memory   │ │   Virtual  │          │    │
│   │  │ Management │ │ Management │ │ Filesystem │          │    │
│   │  └────────────┘ └────────────┘ └────────────┘          │    │
│   │                                                        │    │
│   │  ┌────────────┐ ┌────────────┐ ┌────────────┐          │    │
│   │  │  Network   │ │  Device    │ │  Security  │          │    │
│   │  │   Stack    │ │  Drivers   │ │   (LSM)    │          │    │
│   │  └────────────┘ └────────────┘ └────────────┘          │    │
│   │                                                        │    │
│   │  ────────────────────────────────────────────────────  │    │
│   │  Architecture-dependent code (arch/x86, arch/arm...)   │    │
│   └────────────────────────────────────────────────────────┘    │
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                      HARDWARE                           │   │
│   └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

## Ядро Linux

### Що входить в ядро

```
┌────────────────────────────────────────────────────────────────┐
│                    КОМПОНЕНТИ ЯДРА LINUX                       │
│                                                                │
│   ┌──────────────────────────────────────────────────────────┐ │
│   │ ПЛАНУВАЛЬНИК ПРОЦЕСІВ (scheduler)                        │ │
│   │ • EEVDF — за замовчуванням з 6.6 (раніше CFS)            │ │
│   │ • FIFO, Round-Robin — для real-time                      │ │
│   │ • Deadline — для задач з дедлайнами                      │ │
│   └──────────────────────────────────────────────────────────┘ │
│                                                                │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │ ПІДСИСТЕМА ПАМ'ЯТІ (mm)                                 │  │
│   │ • Віртуальна пам'ять                                    │  │
│   │ • Сторінки (pages), swap                                │  │
│   │ • NUMA support                                          │  │
│   │ • OOM Killer                                            │  │
│   └─────────────────────────────────────────────────────────┘  │
│                                                                │
│   ┌──────────────────────────────────────────────────────────┐ │
│   │ ВІРТУАЛЬНА ФАЙЛОВА СИСТЕМА (VFS)                         │ │
│   │ • Єдиний інтерфейс для ext4, XFS, Btrfs, NFS...          │ │
│   │ • Кешування (page cache)                                 │ │
│   │ • Inode, dentry, superblock                              │ │
│   └──────────────────────────────────────────────────────────┘ │
│                                                                │
│   ┌──────────────────────────────────────────────────────────┐ │
│   │ МЕРЕЖЕВИЙ СТЕК                                           │ │
│   │ • TCP/IP, UDP, ICMP                                      │ │
│   │ • Sockets API                                            │ │
│   │ • Netfilter (iptables/nftables)                          │ │
│   └──────────────────────────────────────────────────────────┘ │
│                                                                │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │ ДРАЙВЕРИ ПРИСТРОЇВ                                      │  │
│   │ • Block devices (NVMe, SATA, USB storage)               │  │
│   │ • Character devices (terminals, serial)                 │  │
│   │ • Network devices (Ethernet, WiFi)                      │  │
│   │ • GPU (DRM subsystem)                                   │  │
│   └─────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
```

### Практичні команди

```bash
# Версія ядра
uname -r
# Приклад: 6.8.0-45-generic

# Повна інформація
uname -a

# Де знаходиться ядро
ls -la /boot/vmlinuz*

# Параметри завантаження
cat /proc/cmdline

# Завантажені модулі ядра
lsmod | head -20

# Інформація про модуль
modinfo ext4

# Параметри ядра (runtime)
sysctl -a | head -20

# Змінити параметр (тимчасово)
sudo sysctl vm.swappiness=10

# Логи ядра
dmesg | tail -30
journalctl -k
```

## Файлова система Linux

### Ієрархія FHS (Filesystem Hierarchy Standard)

```
┌────────────────────────────────────────────────────────────────┐
│                    СТРУКТУРА КАТАЛОГІВ LINUX                   │
│                                                                │
│   /                          Root — корінь усього              │
│   ├── bin/ → usr/bin         Базові команди (ls, cp, bash)     │
│   ├── boot/                  Ядро та завантажувач              │
│   │   ├── vmlinuz-*          Ядро                              │
│   │   └── initrd.img-*       Initial ramdisk                   │
│   ├── dev/                   Файли пристроїв                   │
│   │   ├── sda, nvme0n1       Диски (SATA/USB, NVMe)            │
│   │   ├── tty*               Термінали                         │
│   │   └── null               /dev/null — "чорна діра"          │
│   ├── etc/                   Конфігурація системи              │
│   │   ├── passwd             Список користувачів               │
│   │   ├── fstab              Монтування файлових систем        │
│   │   └── ssh/               Конфігурація SSH                  │
│   ├── home/                  Домашні каталоги користувачів     │
│   │   └── username/                                            │
│   ├── lib/, lib64/           Системні бібліотеки               │
│   ├── mnt/, media/           Точки монтування                  │
│   ├── opt/                   Опціональне ПЗ                    │
│   ├── proc/                  Віртуальна ФС — процеси           │
│   │   ├── 1/                 PID 1 (init/systemd)              │
│   │   ├── cpuinfo            Інформація про CPU                │
│   │   └── meminfo            Інформація про пам'ять            │
│   ├── root/                  Домашній каталог root             │
│   ├── run/                   Дані роботи служб (у пам'яті)     │
│   ├── sbin/                  Системні команди (адмін)          │
│   ├── srv/                   Дані сервісів (сайти, FTP)        │
│   ├── sys/                   Віртуальна ФС — ядро/пристрої     │
│   ├── tmp/                   Тимчасові файли                   │
│   ├── usr/                   Userland програми                 │
│   │   ├── bin/               Користувацькі програми            │
│   │   ├── lib/               Бібліотеки                        │
│   │   ├── local/             Локальні встановлення             │
│   │   └── share/             Дані (документація, icons)        │
│   └── var/                   Змінні дані                       │
│       ├── log/               Логи системи                      │
│       ├── www/               Web-сервер                        │
│       └── lib/               Бази даних, стан                  │
└────────────────────────────────────────────────────────────────┘
```

Звідки взагалі два набори каталогів — `/bin` і `/usr/bin`? Історія UNIX: на початку 1970-х система на PDP-11 перестала вміщатися на перший диск, і частину програм перенесли на другий, змонтований у `/usr` (спершу там жили домашні каталоги користувачів — звідси й назва). У `/bin` лишилося лише те, що потрібне для завантаження до монтування другого диска. Причина давно зникла, а розділення жило десятиліттями. Сучасні дистрибутиви повернули все в `/usr`, лишивши `/bin` і `/lib` символьними посиланнями для сумісності.

Каталог `/run` — молодший: він живе в оперативній пам'яті (tmpfs) і очищується під час кожного завантаження. Там служби тримають те, що має сенс лише поки система працює: номери своїх процесів, сокети для зв'язку, блокування.

### Практика

```bash
# Де я зараз?
pwd

# Перейти
cd /var/log

# Показати вміст
ls -la

# Розмір каталогів
du -sh /var/*

# Монтування
mount | column -t

# Дисковий простір
df -h

# Тип файлової системи
df -T
```

## Користувачі та права

```
┌────────────────────────────────────────────────────────────────┐
│                    ПРАВА ДОСТУПУ В LINUX                       │
│                                                                │
│   ls -la file.txt                                              │
│   -rw-r--r-- 1 user group 1234 Jan 15 10:30 file.txt           │
│   ││  │  │                                                     │
│   ││  │  └── others: read only                                 │
│   ││  └───── group: read only                                  │
│   │└──────── owner: read + write                               │
│   └───────── type: - (file), d (dir), l (link)                 │
│                                                                │
│   Числове представлення:                                       │
│   r=4, w=2, x=1                                                │
│   rw-r--r-- = 644                                              │
│   rwxr-xr-x = 755                                              │
│   rwx------ = 700                                              │
└────────────────────────────────────────────────────────────────┘
```

```bash
# Поточний користувач
whoami
id

# Змінити права
chmod 755 script.sh
chmod +x script.sh      # Додати executable
chmod u+w,g-w file.txt  # User +write, group -write

# Змінити власника
sudo chown user:group file.txt

# Список користувачів
cat /etc/passwd

# Групи користувача
groups
groups username
```

## Процеси та systemd

```
┌────────────────────────────────────────────────────────────────┐
│                       ІЄРАРХІЯ ПРОЦЕСІВ                        │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│   PID 1: systemd — перший процес простору користувача          │
│       ├── PID 350: systemd-journald                            │
│       ├── PID 800: sshd                                        │
│       │       └── PID 1000: sshd (сеанс)                       │
│       │               └── PID 1001: bash                       │
│       │                       └── PID 1050: vim                │
│       ├── PID 820: NetworkManager                              │
│       ├── PID 900: containerd                                  │
│       │       └── PID 1200: containerd-shim                    │
│       │               └── PID 1210: процеси контейнера         │
│       └── PID 950: dockerd (передає команди containerd)        │
│                                                                │
│   PID 2: kthreadd — предок усіх потоків ядра                   │
│       ├── [kworker/0:1]                                        │
│       └── [ksoftirqd/0] ...                                    │
│                                                                │
│   PID 0 у списках не видно: це «простій» (idle) процесора,     │
│   який ядро запускає, коли більше нікому працювати.            │
│                                                                │
│   systemd — сучасна система ініціалізації;                     │
│   замінила SysVinit (скрипти в /etc/init.d)                    │
└────────────────────────────────────────────────────────────────┘
```

```bash
# Дерево процесів
pstree -p

# Список процесів
ps aux
ps aux | grep nginx

# Інтерактивний моніторинг
htop
top

# systemd: статус сервісу
systemctl status nginx

# Запустити/зупинити
sudo systemctl start nginx
sudo systemctl stop nginx
sudo systemctl restart nginx

# Увімкнути автозапуск
sudo systemctl enable nginx

# Логи сервісу
journalctl -u nginx -f
```

## Пакетний менеджер

```
┌────────────────────────────────────────────────────────────────┐
│                    ПАКЕТНІ МЕНЕДЖЕРИ                           │
│                                                                │
│   Debian/Ubuntu:              Red Hat/Fedora:                  │
│   ┌────────────────────┐      ┌────────────────────┐           │
│   │ apt / apt-get      │      │ dnf / yum          │           │
│   │                    │      │                    │           │
│   │ .deb пакети        │      │ .rpm пакети        │           │
│   │ dpkg (низький      │      │ rpm (низький       │           │
│   │ рівень)            │      │ рівень)            │           │
│   └────────────────────┘      └────────────────────┘           │
│                                                                │
│   Arch:                       Universal:                       │
│   ┌────────────────────┐      ┌────────────────────┐           │
│   │ pacman             │      │ snap, flatpak      │           │
│   └────────────────────┘      └────────────────────┘           │
└────────────────────────────────────────────────────────────────┘
```

```bash
# Debian/Ubuntu
sudo apt update                    # Оновити список пакетів
sudo apt upgrade                   # Оновити встановлені
sudo apt install nginx             # Встановити
sudo apt remove nginx              # Видалити
apt search nginx                   # Пошук

# Fedora/RHEL
sudo dnf update
sudo dnf install nginx
sudo dnf remove nginx

# Arch
sudo pacman -Syu                   # Оновити все
sudo pacman -S nginx               # Встановити
```

## Чому Linux?

| Перевага | Пояснення |
|----------|-----------|
| **Безкоштовний** | Немає ліцензійних витрат |
| **Open Source** | Можна модифікувати, аудитувати код |
| **Стабільний** | Сервери місяцями працюють без збоїв; ядро можна латати навіть без перезавантаження (livepatch) |
| **Безпечний** | Швидкі патчі, менше malware |
| **Гнучкий** | Від IoT до суперкомп'ютерів |
| **Ефективний** | Менше ресурсів для тієї ж роботи |
| **Спільнота** | Величезна база знань, підтримка |

## Практичне завдання

```bash
# 1. Інформація про систему
uname -a
cat /etc/os-release
hostnamectl

# 2. Ресурси
free -h
df -h
lscpu

# 3. Процеси
ps aux | wc -l
pstree | head -30

# 4. Мережа
ip addr
ss -tuln

# 5. Логи
journalctl -n 50 --no-pager

# 6. Встановлені пакети (Debian/Ubuntu)
dpkg -l | wc -l
apt list --installed | head -20
```

## 🏢 Real World: Як це використовують у великих компаніях

```
┌─────────────────────────────────────────────────────────────────┐
│              LINUX KERNEL ТА GNU TOOLS У PRODUCTION             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   NETFLIX                                                       │
│   ├── роздача відео — FreeBSD (Open Connect)                    │
│   └── керування сервісом — Linux у хмарі AWS                    │
│                                                                 │
│   GOOGLE                                                        │
│   ├── модифіковане ядро Linux на серверах                       │
│   ├── Borg, а згодом Kubernetes — планувальники задач           │
│   └── cgroups — розробка інженерів Google (у ядрі з 2008)       │
│                                                                 │
│   META (FACEBOOK)                                               │
│   ├── cgroup v2 — значною мірою робота розробників Facebook     │
│   ├── Btrfs на мільйонах серверів; створена в Oracle (2007),    │
│   │   головний автор Кріс Мейсон з 2013 працює в Facebook       │
│   └── io_uring (2019) — асинхронне введення-виведення           │
│                                                                 │
│   SPACEX                                                        │
│   └── Linux на бортових комп'ютерах Dragon і Falcon 9           │
│                                                                 │
│   AWS / AZURE / GCP                                             │
│   └── більшість клієнтських віртуальних машин — на Linux        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 💼 Career Spotlight

```
┌────────────────────────────────────────────────────────────────┐
│        КАР'ЄРА: LINUX SYSTEM ADMINISTRATOR / SRE               │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│   LINUX SYSTEM ADMINISTRATOR                                   │
│   ├── Рівень: Junior → Senior                                  │
│   ├── Зарплата: $50K-$120K (USA) | €45K-€100K (EU)             │
│   ├── Навички з цієї лекції:                                   │
│   │   ✓ Розуміння архітектури Linux kernel                     │
│   │   ✓ Робота з systemd та процесами                          │
│   │   ✓ Управління пакетами (apt, dnf)                         │
│   │   ✓ Файлова система та права доступу                       │
│   └── Компанії: будь-яка IT-компанія                           │
│                                                                │
│   DEVOPS ENGINEER                                              │
│   ├── Рівень: Middle → Senior                                  │
│   ├── Зарплата: $80K-$180K (USA) | €70K-€150K (EU)             │
│   ├── Навички з цієї лекції:                                   │
│   │   ✓ Глибоке розуміння /proc, /sys                          │
│   │   ✓ Налаштування kernel через sysctl                       │
│   │   ✓ Логування через journalctl                             │
│   └── Компанії: Netflix, Spotify, tech startups                │
│                                                                │
│   SITE RELIABILITY ENGINEER (SRE)                              │
│   ├── Рівень: Senior → Principal                               │
│   ├── Зарплата: $120K-$250K (USA) | €100K-€200K (EU)           │
│   ├── Навички з цієї лекції:                                   │
│   │   ✓ Kernel tuning та performance                           │
│   │   ✓ Планувальник процесів (EEVDF)                          │
│   │   ✓ Memory management, OOM Killer                          │
│   └── Компанії: Google, Meta, Amazon, Microsoft                │
│                                                                │
│   EMBEDDED LINUX ENGINEER                                      │
│   ├── Рівень: Junior → Senior                                  │
│   ├── Зарплата: $70K-$150K (USA) | €60K-€130K (EU)             │
│   └── Компанії: Tesla, SpaceX, IoT стартапи                    │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

## 🔗 Корисні ресурси

### Онлайн-практика

| Ресурс | Опис | Посилання |
|--------|------|-----------|
| **Linux Journey** | Безкоштовний інтерактивний курс | linuxjourney.com |
| **OverTheWire: Bandit** | Wargame для вивчення Linux CLI | overthewire.org/wargames/bandit |
| **Linux Survival** | Інтерактивний тренажер команд | linuxsurvival.com |
| **Killercoda** | Hands-on Linux scenarios у браузері | killercoda.com |
| **LPI Learning** | Офіційні матеріали Linux Professional Institute | learning.lpi.org |

## 📋 Cheat Sheet

```
┌────────────────────────────────────────────────────────────────┐
│                    LINUX OVERVIEW CHEAT SHEET                  │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│   ІНФОРМАЦІЯ ПРО СИСТЕМУ                                       │
│   ──────────────────────────────────────────────────────────── │
│   uname -r              # Версія ядра                          │
│   uname -a              # Повна інформація                     │
│   cat /etc/os-release   # Інформація про дистрибутив           │
│   hostnamectl           # Hostname та система                  │
│                                                                │
│   ЯДРО ТА МОДУЛІ                                               │
│   ──────────────────────────────────────────────────────────── │
│   lsmod                 # Завантажені модулі                   │
│   modinfo <module>      # Інформація про модуль                │
│   dmesg                 # Логи ядра                            │
│   sysctl -a             # Параметри ядра                       │
│                                                                │
│   ФАЙЛОВА СИСТЕМА                                              │
│   ──────────────────────────────────────────────────────────── │
│   /etc/                 # Конфігурація системи                 │
│   /var/log/             # Логи                                 │
│   /proc/                # Віртуальна ФС процесів               │
│   /sys/                 # Віртуальна ФС ядра/пристроїв         │
│                                                                │
│   КОРИСТУВАЧІ ТА ПРАВА                                         │
│   ──────────────────────────────────────────────────────────── │
│   whoami / id           # Поточний користувач                  │
│   chmod 755 file        # Змінити права (rwxr-xr-x)            │
│   chown user:group file # Змінити власника                     │
│   r=4, w=2, x=1         # Числове представлення прав           │
│                                                                │
│   ПРОЦЕСИ ТА SYSTEMD                                           │
│   ──────────────────────────────────────────────────────────── │
│   ps aux                # Список процесів                      │
│   pstree -p             # Дерево процесів                      │
│   systemctl status X    # Статус сервісу                       │
│   journalctl -u X -f    # Логи сервісу в реальному часі        │
│                                                                │
│   ПАКЕТИ (DEBIAN/UBUNTU)                                       │
│   ──────────────────────────────────────────────────────────── │
│   apt update            # Оновити список пакетів               │
│   apt install X         # Встановити пакет                     │
│   apt remove X          # Видалити пакет                       │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

## 🎯 Міні-проект (30 хв)

### Завдання: Створіть власний скрипт System Information Dashboard

Створіть Bash-скрипт, який красиво виводить всю інформацію про вашу Linux-систему. Це ваш перший "справжній" Linux-інструмент!

**Кроки:**

1. Створіть файл скрипта:
```bash
mkdir -p ~/scripts
nano ~/scripts/sysinfo.sh
```

2. Напишіть скрипт з наступним вмістом:
```bash
#!/bin/bash
# System Information Dashboard
# Автор: [Ваше ім'я]

# LC_ALL=C — щоб вивід lscpu і free був англійською за будь-якої мови
# системи: на українській локалі lscpu пише «Назва моделі», і grep
# за 'Model name' нічого б не знайшов
export LC_ALL=C

clear
echo "╔══════════════════════════════════════════════════════════════╗"
echo "║              SYSTEM INFORMATION DASHBOARD                    ║"
echo "╚══════════════════════════════════════════════════════════════╝"
echo ""
echo "[ СИСТЕМА ]"
echo "   Hostname:     $(hostname)"
echo "   Дистрибутив:  $(grep PRETTY_NAME /etc/os-release | cut -d'"' -f2)"
echo "   Ядро:         $(uname -r)"
echo "   Uptime:       $(uptime -p)"
echo ""
echo "[ HARDWARE ]"
echo "   CPU:          $(lscpu | grep 'Model name' | cut -d':' -f2 | xargs)"
echo "   RAM:          $(free -h | awk '/Mem:/ {print $3 " / " $2}')"
echo "   Диск (/):     $(df -h / | awk 'NR==2 {print $3 " / " $2 " (" $5 ")"}')"
echo ""
echo "[ ПРОЦЕСИ ]"
echo "   Всього:       $(ps aux | wc -l)"
echo "   Init:         $(ps -p 1 -o comm=)"
echo "   Top 3 CPU:    $(ps aux --sort=-%cpu | awk 'NR<=4 && NR>1 {print $11}' | tr '\n' ' ')"
echo ""
echo "[ МЕРЕЖА ]"
echo "   IP:           $(hostname -I | awk '{print $1}')"
echo "   Gateway:      $(ip route | grep default | awk '{print $3}')"
echo ""
echo "Звіт від $(date '+%Y-%m-%d %H:%M:%S')"
```

3. Зробіть скрипт виконуваним та запустіть:
```bash
chmod +x ~/scripts/sysinfo.sh
~/scripts/sysinfo.sh
```

4. Додайте alias для швидкого запуску:
```bash
echo 'alias sysinfo="~/scripts/sysinfo.sh"' >> ~/.bashrc
source ~/.bashrc
sysinfo
```

**Очікуваний результат:**
- Готовий скрипт `~/scripts/sysinfo.sh`
- Alias `sysinfo` для швидкого виклику
- Красивий вивід інформації про систему у форматі dashboard

**Бонус (для допитливих):**
- Додайте кольоровий вивід (використовуйте `\e[32m` для зеленого, `\e[0m` для скидання)
- Додайте інформацію про завантажені модулі ядра (`lsmod | wc -l`)
- Зробіть так, щоб скрипт автоматично запускався при кожному відкритті терміналу. Обережно: `~/.bashrc` виконується й для неінтерактивних оболонок — `scp` чи `rsync` через SSH ламаються, якщо він щось друкує. Тому виклик огортають перевіркою: `[[ $- == *i* ]] && ~/scripts/sysinfo.sh`
- Збережіть вивід у файл: `sysinfo > ~/system-report-$(date +%Y%m%d).txt`

## Тест для самоперевірки

Десять запитань, у кожному одна правильна відповідь. Якщо тема винесена вашій групі на самостійне опрацювання, цей самий тест публікується в Google Classroom — 10 балів, по одному за кожне запитання.

**1.** Що саме створив Лінус Торвальдс 1991 року?

- ядро операційної системи
- повноцінний дистрибутив із графічною оболонкою
- набір утиліт GNU
- систему ініціалізації

**2.** Чому систему коректно називати GNU/Linux?

- ядро дає Linux, а базові утиліти й компілятор — проєкт GNU
- GNU — це назва графічної оболонки
- GNU розробив ядро, а Linux — утиліти
- так називають лише комерційні дистрибутиви

**3.** Що таке systemd?

- система ініціалізації та керування службами
- пакетний менеджер
- файлова система
- графічне середовище

**4.** Яка команда покаже стан служби?

- systemctl status назва
- journalctl -u назва
- service list
- ps aux

**5.** Які права дає `chmod 755`?

- rwxr-xr-x
- rw-r--r--
- rwxrwxrwx
- r-xr-xr-x

**6.** Яке числове представлення має набір прав `rw-r--r--`?

- 644
- 755
- 664
- 600

**7.** Що містить віртуальна файлова система `/proc`?

- відомості про процеси та стан ядра, згенеровані на льоту
- резервні копії конфігурацій
- тимчасові файли програм
- журнали служб

**8.** У якому каталозі за стандартом FHS лежать конфігураційні файли системи?

- /etc
- /var
- /usr
- /opt

**9.** Яка команда оновить список доступних пакетів у Debian чи Ubuntu?

- apt update
- apt upgrade
- apt install
- dpkg -l

**10.** Чому Linux посідає всі 500 позицій у списку TOP500 суперкомп'ютерів?

- відкритий код дає змогу налаштувати ядро під конкретне обладнання, а ліцензія нічого не коштує
- він єдиний підтримує багатоядерні процесори
- інші системи не мають мережевого стека
- виробники обладнання не випускають драйверів під інші системи

## Підсумок

| Компонент | Опис |
|-----------|------|
| **Kernel** | Ядро Linux — керує hardware |
| **GNU tools** | Базові утиліти (bash, coreutils) |
| **systemd** | Система ініціалізації |
| **Package Manager** | apt, dnf, pacman |
| **Filesystem** | ext4, XFS, Btrfs |

**Ключові файли:**
- `/etc/passwd` — користувачі
- `/etc/fstab` — монтування
- `/var/log/` — логи
- `/proc/`, `/sys/` — інформація про систему

На наступній лекції розглянемо різні дистрибутиви Linux та як обрати потрібний.
