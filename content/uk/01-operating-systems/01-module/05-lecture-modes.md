---
title: "Режими роботи. Функціональні компоненти ОС"
type: lecture
order: 5
preview: "User mode, Kernel mode, компоненти ядра."
---

## Чому програма не може форматувати диск без дозволу?

Уявіть ситуацію: ви завантажили гру з інтернету, запустили її, а вона тихо видалила всі файли на диску. Або вірус отримав доступ до вашої веб-камери без вашого відома. Як операційна система захищає від таких сценаріїв?

Відповідь — **апаратне розділення привілеїв**. Процесор фізично не дозволяє звичайним програмам виконувати небезпечні операції.

## Кільця захисту процесора (Protection Rings)

Сучасні процесори x86/x64 мають 4 рівні привілеїв, але більшість ОС використовують лише два:

```
┌─────────────────────────────────────────────────────────────────┐
│                    КІЛЬЦЯ ЗАХИСТУ x86                           │
│                                                                 │
│                    ┌───────────────────┐                       │
│                    │     Ring 3        │  ← User Mode          │
│                    │   (Найменше      │     Звичайні програми  │
│                    │    привілеїв)    │     Браузер, Word...   │
│                    │  ┌─────────────┐  │                       │
│                    │  │   Ring 2    │  │  ← Не використ.       │
│                    │  │ ┌─────────┐ │  │                       │
│                    │  │ │ Ring 1  │ │  │  ← Не використ.       │
│                    │  │ │ ┌─────┐ │ │  │                       │
│                    │  │ │ │ R 0 │ │ │  │  ← Kernel Mode        │
│                    │  │ │ │     │ │ │  │     Ядро ОС           │
│                    │  │ │ └─────┘ │ │  │     Повний доступ     │
│                    │  │ └─────────┘ │  │                       │
│                    │  └─────────────┘  │                       │
│                    └───────────────────┘                       │
│                                                                 │
│   Ring 0: Kernel Mode — повний доступ до заліза                │
│   Ring 3: User Mode — обмежений доступ                         │
│   Ring 1, 2: Зарезервовані (використ. для віртуалізації)       │
└─────────────────────────────────────────────────────────────────┘
```

## User Mode vs Kernel Mode

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   USER MODE (Ring 3)              KERNEL MODE (Ring 0)         │
│   ══════════════════              ═══════════════════          │
│                                                                 │
│   ✅ Доступ до власної           ✅ Доступ до ВСІЄЇ пам'яті    │
│      пам'яті процесу                                           │
│                                   ✅ Виконання БУДЬ-ЯКИХ       │
│   ❌ Доступ до пам'яті              інструкцій CPU             │
│      інших процесів                                            │
│                                   ✅ Пряма робота з            │
│   ❌ Привілейовані                  пристроями (I/O ports)     │
│      інструкції CPU                                            │
│                                   ✅ Керування перериваннями   │
│   ❌ Прямий доступ до                                          │
│      пристроїв                   ✅ Зміна таблиць сторінок     │
│                                     (віртуальна пам'ять)       │
│   ❌ Зміна системних                                           │
│      структур                                                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Що відбувається при порушенні?

```
┌─────────────────────────────────────────────────────────────────┐
│                    ПОРУШЕННЯ ПРИВІЛЕЇВ                          │
│                                                                 │
│   User Mode програма намагається:                               │
│                                                                 │
│   1. Прочитати пам'ять ядра                                    │
│      ──────────────────────────────────────────────────────    │
│      → CPU генерує General Protection Fault (#GP)              │
│      → ОС завершує процес: "Segmentation fault"                │
│                                                                 │
│   2. Виконати привілейовану інструкцію (напр. CLI)            │
│      ──────────────────────────────────────────────────────    │
│      → CPU генерує #GP                                         │
│      → ОС завершує процес                                      │
│                                                                 │
│   3. Звернутися до I/O порту (напр. диск)                     │
│      ──────────────────────────────────────────────────────    │
│      → CPU генерує #GP                                         │
│      → ОС завершує процес                                      │
│                                                                 │
│   Це АПАРАТНИЙ захист — ОС не може його обійти навіть якби     │
│   хотіла. Процесор фізично не виконує заборонені інструкції.   │
└─────────────────────────────────────────────────────────────────┘
```

**Практичний приклад:**

```bash
# Спроба прочитати пам'ять ядра
sudo cat /proc/kcore | head -c 100
# Працює тільки з sudo (root має доступ до kernel space)

# Без sudo:
cat /proc/kcore
# cat: /proc/kcore: Permission denied

# Подивитися, в якому режимі працює процес
cat /proc/self/status | grep -i "seccomp\|cap"
```

## Перехід між режимами

Єдиний легальний спосіб перейти з User Mode в Kernel Mode — через **системний виклик**.

```
┌─────────────────────────────────────────────────────────────────┐
│                    ПЕРЕХІД USER → KERNEL                        │
│                                                                 │
│   User Mode                                                     │
│   ┌────────────────────────────────────┐                       │
│   │  Програма: printf("Hello");        │                       │
│   │            ↓                        │                       │
│   │  libc: write(1, "Hello", 5);       │                       │
│   │            ↓                        │                       │
│   │  Інструкція: SYSCALL (x86-64)      │                       │
│   │              або INT 0x80 (x86)    │                       │
│   └──────────────┬─────────────────────┘                       │
│                  │                                              │
│   ═══════════════╪══════════════════════  ← Межа режимів       │
│                  │                                              │
│   Kernel Mode    ↓                                              │
│   ┌────────────────────────────────────┐                       │
│   │  1. CPU зберігає стан (регістри)   │                       │
│   │  2. Перемикає Ring 3 → Ring 0      │                       │
│   │  3. Переходить до sys_write()      │                       │
│   │  4. Ядро пише в stdout             │                       │
│   │  5. Відновлює стан                 │                       │
│   │  6. SYSRET — назад в Ring 3        │                       │
│   └────────────────────────────────────┘                       │
│                                                                 │
│   Час перемикання: 100-1000 наносекунд                         │
└─────────────────────────────────────────────────────────────────┘
```

**Практичний приклад — відстеження системних викликів:**

```bash
# Подивитися syscalls простої команди
strace -c ls /tmp 2>&1 | tail -20

# Приклад виводу:
# % time     seconds  usecs/call     calls    errors syscall
# ------ ----------- ----------- --------- --------- ----------------
#  25.00    0.000050          12         4           openat
#  20.00    0.000040          10         4           read
#  15.00    0.000030           7         4           close
#  ...

# Кожен виклик — це перехід User→Kernel→User
```

## Функціональні компоненти ядра ОС

```
┌─────────────────────────────────────────────────────────────────┐
│                    КОМПОНЕНТИ ЯДРА LINUX                        │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    KERNEL SPACE                          │   │
│  │                                                          │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │   │
│  │  │  ПЛАНУВАЛЬНИК │  │  КЕРУВАННЯ   │  │   ФАЙЛОВІ   │   │   │
│  │  │  ПРОЦЕСІВ     │  │  ПАМ'ЯТТЮ    │  │   СИСТЕМИ   │   │   │
│  │  │  (Scheduler)  │  │  (MM)        │  │   (VFS)     │   │   │
│  │  │               │  │              │  │             │   │   │
│  │  │  CFS, RT,     │  │  Сторінки,   │  │  ext4, xfs, │   │   │
│  │  │  deadline     │  │  swap, OOM   │  │  btrfs, nfs │   │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘   │   │
│  │                                                          │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │   │
│  │  │   МЕРЕЖЕВИЙ  │  │   ДРАЙВЕРИ   │  │    БЕЗПЕКА   │   │   │
│  │  │    СТЕК      │  │  ПРИСТРОЇВ   │  │              │   │   │
│  │  │              │  │              │  │              │   │   │
│  │  │  TCP/IP,     │  │  block, char,│  │  LSM,        │   │   │
│  │  │  sockets     │  │  network     │  │  SELinux     │   │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘   │   │
│  │                                                          │   │
│  │  ┌──────────────────────────────────────────────────┐   │   │
│  │  │            СИСТЕМНІ ВИКЛИКИ (syscall table)      │   │   │
│  │  │    read, write, open, close, fork, exec, ...     │   │   │
│  │  └──────────────────────────────────────────────────┘   │   │
│  │                                                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ▲                                  │
│                              │ syscall                          │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    USER SPACE                            │   │
│  │   Applications, Libraries (glibc, musl)                  │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 1. Планувальник процесів (Process Scheduler)

**Завдання:** Вирішує, який процес виконуватиметься наступним.

```
┌─────────────────────────────────────────────────────────────────┐
│                    ПЛАНУВАЛЬНИК CFS (Linux)                     │
│                                                                 │
│   Completely Fair Scheduler — "повністю справедливий"           │
│                                                                 │
│   Процеси в черзі за часом виконання:                          │
│                                                                 │
│      vruntime (віртуальний час)                                │
│      ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐                            │
│      │ A │ │ B │ │ C │ │ D │ │ E │                            │
│      │ 5 │ │ 8 │ │12 │ │15 │ │20 │  мс                        │
│      └───┘ └───┘ └───┘ └───┘ └───┘                            │
│        ▲                                                        │
│        │                                                        │
│        └── Наступний для виконання                             │
│            (найменший vruntime)                                │
│                                                                 │
│   Red-Black Tree для O(log n) пошуку                           │
└─────────────────────────────────────────────────────────────────┘
```

**Практичні команди:**

```bash
# Подивитися пріоритети процесів
ps -eo pid,ni,pri,comm | head -20

# Змінити пріоритет (nice value: -20 до +19)
nice -n 10 ./heavy_task    # Знизити пріоритет
sudo nice -n -10 ./important_task  # Підвищити пріоритет

# Real-time політики
chrt -f 50 ./realtime_task  # FIFO з пріоритетом 50
chrt -r 50 ./realtime_task  # Round-robin

# Статистика планувальника
cat /proc/schedstat | head -5
```

### 2. Менеджер пам'яті (Memory Manager)

**Завдання:** Розподіляє RAM між процесами, забезпечує ізоляцію.

```
┌─────────────────────────────────────────────────────────────────┐
│                    ВІРТУАЛЬНА ПАМ'ЯТЬ                           │
│                                                                 │
│   Процес A                        Процес B                      │
│   ┌─────────────────┐             ┌─────────────────┐          │
│   │ Virtual: 0x1000 │             │ Virtual: 0x1000 │          │
│   └────────┬────────┘             └────────┬────────┘          │
│            │                               │                    │
│            │    Page Table A               │    Page Table B    │
│            ▼                               ▼                    │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                    MMU (Memory Management Unit)          │  │
│   │                    Апаратна трансляція адрес             │  │
│   └─────────────────────────────────────────────────────────┘  │
│            │                               │                    │
│            ▼                               ▼                    │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                    ФІЗИЧНА ПАМ'ЯТЬ (RAM)                │  │
│   │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐         │  │
│   │  │ 0x10 │ │ 0x20 │ │ 0x30 │ │ 0x40 │ │ 0x50 │         │  │
│   │  │  A   │ │  B   │ │ kernel│ │  A   │ │  B   │         │  │
│   │  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘         │  │
│   └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│   Однакові віртуальні адреси → різні фізичні адреси            │
│   Процеси не можуть побачити пам'ять один одного               │
└─────────────────────────────────────────────────────────────────┘
```

**Практичні команди:**

```bash
# Карта пам'яті процесу
cat /proc/self/maps

# Використання пам'яті
free -h

# Детальна інформація
cat /proc/meminfo | head -20

# OOM Killer — хто буде "вбитий" при нестачі RAM
cat /proc/*/oom_score | sort -rn | head -5
```

### 3. Віртуальна файлова система (VFS)

**Завдання:** Єдиний інтерфейс для різних файлових систем.

```
┌─────────────────────────────────────────────────────────────────┐
│                    VIRTUAL FILE SYSTEM                          │
│                                                                 │
│   Користувач:  open("/home/user/file.txt", O_RDONLY)           │
│                              │                                  │
│                              ▼                                  │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                         VFS                              │  │
│   │              Єдиний інтерфейс для всіх ФС               │  │
│   │    open(), read(), write(), close(), stat(), ...        │  │
│   └─────────────────────────────────────────────────────────┘  │
│                │              │              │                  │
│                ▼              ▼              ▼                  │
│   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│   │     ext4     │  │     XFS      │  │     NFS      │        │
│   │   (локальна) │  │  (локальна)  │  │  (мережева)  │        │
│   └──────────────┘  └──────────────┘  └──────────────┘        │
│          │                  │                  │                │
│          ▼                  ▼                  ▼                │
│   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│   │   /dev/sda1  │  │  /dev/sdb1   │  │  192.168.1.1 │        │
│   │     SSD      │  │     HDD      │  │    Server    │        │
│   └──────────────┘  └──────────────┘  └──────────────┘        │
└─────────────────────────────────────────────────────────────────┘
```

**Практичні команди:**

```bash
# Які файлові системи підтримує ядро
cat /proc/filesystems

# Змонтовані файлові системи
mount | grep -E "ext4|xfs|btrfs"

# Відкриті файли процесу
ls -la /proc/self/fd

# Файлові дескриптори в системі
cat /proc/sys/fs/file-nr
# формат: allocated  free  max
```

### 4. Мережевий стек

**Завдання:** Реалізація мережевих протоколів (TCP/IP, UDP, etc.)

```
┌─────────────────────────────────────────────────────────────────┐
│                    МЕРЕЖЕВИЙ СТЕК LINUX                         │
│                                                                 │
│   User Space                                                    │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │  Application: curl http://example.com                    │  │
│   └────────────────────────┬────────────────────────────────┘  │
│                            │ socket(), connect(), send()       │
│   ═════════════════════════╪═══════════════════════════════    │
│   Kernel Space             ▼                                    │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │  Socket Layer (BSD sockets API)                          │  │
│   ├─────────────────────────────────────────────────────────┤  │
│   │  Transport: TCP / UDP                                    │  │
│   │  - Надійність, порядок пакетів (TCP)                    │  │
│   │  - Контроль потоку, congestion control                  │  │
│   ├─────────────────────────────────────────────────────────┤  │
│   │  Network: IP (IPv4 / IPv6)                              │  │
│   │  - Маршрутизація                                        │  │
│   │  - Фрагментація                                         │  │
│   ├─────────────────────────────────────────────────────────┤  │
│   │  Link: Ethernet driver                                   │  │
│   │  - MAC адреси                                           │  │
│   │  - ARP                                                  │  │
│   └────────────────────────┬────────────────────────────────┘  │
│                            ▼                                    │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │  NIC (Network Interface Card)                            │  │
│   └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

**Практичні команди:**

```bash
# Мережеві інтерфейси
ip link show

# Активні з'єднання
ss -tuln

# Статистика мережі
cat /proc/net/dev

# Таблиця маршрутизації
ip route show
```

### 5. Підсистема безпеки (LSM)

**Завдання:** Контроль доступу понад стандартні UNIX-права.

```
┌─────────────────────────────────────────────────────────────────┐
│               LINUX SECURITY MODULES (LSM)                      │
│                                                                 │
│   Системний виклик: open("/etc/shadow", O_RDONLY)              │
│                              │                                  │
│                              ▼                                  │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │  1. Discretionary Access Control (DAC)                   │  │
│   │     Перевірка rwx прав (user/group/other)               │  │
│   │                    │                                     │  │
│   │                    ▼ Якщо OK                             │  │
│   │  2. Linux Security Module (LSM)                          │  │
│   │     ┌──────────────────────────────────────────┐        │  │
│   │     │  SELinux  │  AppArmor  │  Smack  │ TOMOYO│        │  │
│   │     └──────────────────────────────────────────┘        │  │
│   │     Mandatory Access Control (MAC)                       │  │
│   │                    │                                     │  │
│   │                    ▼ Якщо OK                             │  │
│   │  3. Виконання операції                                   │  │
│   └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│   DAC: "Хто ти?"                                               │
│   MAC: "Що тобі дозволено робити?"                             │
└─────────────────────────────────────────────────────────────────┘
```

**Практичні команди:**

```bash
# Перевірити статус SELinux
getenforce 2>/dev/null || echo "SELinux not installed"

# Перевірити AppArmor
aa-status 2>/dev/null || echo "AppArmor not installed"

# Capabilities процесу
cat /proc/self/status | grep Cap

# Розшифрувати capabilities
capsh --decode=0000000000000000
```

## Порівняння режимів

| Характеристика | User Mode | Kernel Mode |
|----------------|-----------|-------------|
| **Ring** | 3 | 0 |
| **Доступ до пам'яті** | Тільки своя | Вся RAM |
| **I/O операції** | Через syscall | Прямий доступ |
| **Привілейовані інструкції** | Заборонено | Дозволено |
| **Наслідки помилки** | Crash процесу | Kernel panic |
| **Приклади коду** | Браузер, редактор | Драйвери, VFS |

## Практичне завдання

Дослідіть режими роботи на вашій системі:

```bash
# 1. Перегляньте системні виклики простої команди
strace -c echo "Hello"

# 2. Подивіться час у kernel mode vs user mode
time dd if=/dev/zero of=/dev/null bs=1M count=1000

# 3. Перегляньте завантажені модулі ядра
lsmod | wc -l

# 4. Інформація про ядро
uname -a

# 5. Параметри ядра
sysctl -a 2>/dev/null | head -20

# 6. Процеси ядра (в квадратних дужках)
ps aux | grep "^\[" | head -10
```

## 🏢 Real World: Як це використовують у великих компаніях

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    РЕЖИМИ CPU ТА КОМПОНЕНТИ ЯДРА В PRODUCTION               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  GOOGLE CHROME (Protection Rings)                                           │
│  ├── Кожна вкладка = окремий процес у Ring 3 (User Mode)                   │
│  ├── Renderer процеси ізольовані через sandbox                             │
│  ├── Site Isolation: кожен домен у своєму процесі                          │
│  └── GPU процес окремий для захисту від експлойтів WebGL                   │
│                                                                             │
│  CLOUDFLARE (Kernel Bypass для швидкості)                                   │
│  ├── XDP (eXpress Data Path): обробка пакетів до kernel stack              │
│  ├── eBPF для DDoS фільтрації на швидкості 100+ Gbps                       │
│  ├── DPDK для найшвидших мережевих операцій                                │
│  └── Мінімізація переходів User↔Kernel для latency < 1ms                   │
│                                                                             │
│  NETFLIX (Scheduler Tuning)                                                 │
│  ├── Tuned Linux CFS для streaming workloads                               │
│  ├── CPU affinity: закріплення процесів за конкретними ядрами             │
│  ├── NUMA-aware scheduling для багатосокетних серверів                     │
│  └── Cgroups v2 для ізоляції ресурсів між мікросервісами                   │
│                                                                             │
│  AWS (Memory Management)                                                    │
│  ├── KVM + QEMU: віртуалізація через апаратні розширення                   │
│  ├── Memory ballooning для динамічного розподілу RAM                       │
│  ├── Huge pages (2MB, 1GB) для зменшення TLB misses                        │
│  └── Nitro: спеціальний hypervisor з мінімальним overhead                  │
│                                                                             │
│  APPLE SECURITY (Sandboxing)                                                │
│  ├── iOS: кожен додаток у своєму sandbox з обмеженими syscalls             │
│  ├── macOS SIP: System Integrity Protection для системних файлів           │
│  ├── T2/M-серія: Secure Enclave працює в окремому Ring (-1)               │
│  └── Kernel extensions замінюються на System Extensions (User Space)       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 💼 Career Spotlight

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    КАР'ЄРНІ ШЛЯХИ: РЕЖИМИ CPU ТА КОМПОНЕНТИ ЯДРА           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  🔧 KERNEL DEVELOPER                                                        │
│     Розробка компонентів ядра, драйверів, scheduler                        │
│     Зарплата: $150,000 - $300,000 (USA) | €80,000 - €160,000 (EU)          │
│     Компанії: Red Hat, SUSE, Google, Intel, AMD                            │
│     Навички: C, assembly, kernel debugging, git, lkml                      │
│                                                                             │
│  🛡️  SECURITY RESEARCHER / EXPLOIT DEVELOPER                                │
│     Пошук вразливостей, privilege escalation, sandboxing                   │
│     Зарплата: $120,000 - $250,000 (USA) | €70,000 - €140,000 (EU)          │
│     Компанії: Google Project Zero, Trail of Bits, CrowdStrike              │
│     Навички: Reverse engineering, C/C++, kernel internals, fuzzing         │
│                                                                             │
│  🌐 NETWORK SYSTEMS ENGINEER                                                │
│     High-performance networking, kernel bypass, eBPF                       │
│     Зарплата: $130,000 - $220,000 (USA) | €65,000 - €130,000 (EU)          │
│     Компанії: Cloudflare, Fastly, Akamai, trading firms                    │
│     Навички: TCP/IP, XDP, DPDK, Linux networking stack                     │
│                                                                             │
│  ⚙️  VIRTUALIZATION ENGINEER                                                │
│     KVM, QEMU, hypervisors, memory management                              │
│     Зарплата: $140,000 - $240,000 (USA) | €70,000 - €140,000 (EU)          │
│     Компанії: AWS, VMware, Red Hat, Nutanix                                │
│     Навички: KVM, QEMU, Xen, hardware virtualization extensions            │
│                                                                             │
│  📊 PERFORMANCE ENGINEER                                                    │
│     Syscall optimization, scheduler tuning, profiling                      │
│     Зарплата: $130,000 - $230,000 (USA) | €70,000 - €130,000 (EU)          │
│     Компанії: Netflix, Meta, trading companies, gaming                     │
│     Навички: perf, ftrace, bpftrace, flamegraphs, C                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🔗 Корисні ресурси

### Онлайн-практика

| Ресурс | Опис | Посилання |
|--------|------|-----------|
| **pwn.college** | CTF з privilege escalation та syscalls | pwn.college |
| **Linux Kernel Labs** | Практичні лабораторні роботи | linux-kernel-labs.github.io |
| **eBPF Tutorial** | Вивчення eBPF через приклади | ebpf.io/get-started |
| **Exploit Education** | Вправи з експлуатації (Phoenix, Protostar) | exploit.education |
| **syscall.sh** | Інтерактивна таблиця системних викликів | syscall.sh |

### Книги

| Назва | Автор | Фокус |
|-------|-------|-------|
| "Linux Kernel Development" | Robert Love | Внутрішня структура ядра |
| "Understanding the Linux Kernel" | Bovet & Cesati | Глибоке занурення в kernel |
| "The Linux Programming Interface" | Michael Kerrisk | Системні виклики та API |
| "BPF Performance Tools" | Brendan Gregg | eBPF та трасування |
| "A Guide to Kernel Exploitation" | Perla & Oldani | Безпека ядра |

### YouTube-канали

| Канал | Тематика |
|-------|----------|
| **LiveOverflow** | Exploit development, kernel security |
| **Low Level Learning** | Системне програмування, як працює CPU |
| **Brendan Gregg** | Linux performance, eBPF, tracing |
| **Julia Evans** | Зрозумілі пояснення Linux internals |
| **gamozolabs** | Low-level security research |

## 📋 Cheat Sheet

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    РЕЖИМИ CPU ТА КОМПОНЕНТИ ЯДРА                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  КІЛЬЦЯ ЗАХИСТУ (x86/x64):                                                  │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ Ring 3 │ User Mode   │ Звичайні програми, обмежені права            │  │
│  │ Ring 2 │ (не використ.)│ Зарезервовано                              │  │
│  │ Ring 1 │ (не використ.)│ Зарезервовано                              │  │
│  │ Ring 0 │ Kernel Mode │ Ядро ОС, повний доступ до hardware           │  │
│  │ Ring -1│ Hypervisor  │ VMM (KVM, Hyper-V) — віртуалізація           │  │
│  │ Ring -2│ SMM         │ System Management Mode — firmware            │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ПЕРЕХІД МІЖ РЕЖИМАМИ:                                                      │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ User → Kernel │ SYSCALL (x64), INT 0x80 (x86), SVC (ARM)            │  │
│  │ Kernel → User │ SYSRET (x64), IRET (x86), ERET (ARM)                │  │
│  │ Час переходу  │ 100-1000 наносекунд                                  │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  КОМПОНЕНТИ ЯДРА LINUX:                                                     │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ Scheduler        │ CFS, RT, Deadline — розподіл CPU часу            │  │
│  │ Memory Manager   │ Paging, swap, OOM killer, huge pages             │  │
│  │ VFS              │ Єдиний інтерфейс: ext4, xfs, nfs, proc           │  │
│  │ Network Stack    │ TCP/IP, sockets, netfilter, XDP                  │  │
│  │ LSM              │ SELinux, AppArmor — обов'язковий контроль        │  │
│  │ Device Drivers   │ Block, char, network — керування hardware        │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  КЛЮЧОВІ КОМАНДИ:                                                           │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ strace -c cmd           → Статистика syscalls команди               │  │
│  │ cat /proc/self/maps     → Карта пам'яті поточного процесу           │  │
│  │ cat /proc/self/status   → Capabilities та режим процесу             │  │
│  │ lsmod                   → Завантажені модулі ядра                   │  │
│  │ dmesg                   → Повідомлення ядра                         │  │
│  │ cat /proc/kallsyms      → Символи ядра (syscalls, функції)          │  │
│  │ perf top                → Профілювання в реальному часі             │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  НАСЛІДКИ ПОРУШЕННЯ ПРИВІЛЕЇВ:                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ Читання kernel memory   → General Protection Fault → SIGSEGV        │  │
│  │ Привілейована інструкція→ #GP → Процес завершується                 │  │
│  │ Помилка в user space    → Crash процесу (інші працюють)             │  │
│  │ Помилка в kernel space  → Kernel panic (система зупиняється)        │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## ❓ Питання для самоперевірки

1. **Що таке "кільця захисту" процесора і чому використовуються лише Ring 0 та Ring 3?**

2. **Що відбувається на апаратному рівні, коли програма намагається виконати привілейовану інструкцію з User Mode?**

3. **Поясніть процес системного виклику: які кроки виконує CPU при переході з User Mode в Kernel Mode?**

4. **Чому помилка в драйвері може призвести до kernel panic, а помилка в звичайній програмі — лише до crash цієї програми?**

5. **Як VFS (Virtual File System) забезпечує уніфікований доступ до різних файлових систем?**

## 🎯 Міні-проект (30 хв)

### Завдання: Вимірювання "вартості" системних викликів

Створіть benchmark, який виміряє реальний overhead переходу між User Mode та Kernel Mode на вашій системі.

**Кроки:**

1. Підготуйте середовище для експерименту:
```bash
mkdir ~/syscall-benchmark && cd ~/syscall-benchmark
```

2. Створіть простий benchmark-скрипт:
```bash
cat > benchmark.sh << 'EOF'
#!/bin/bash
echo "=== BENCHMARK СИСТЕМНИХ ВИКЛИКІВ ==="
echo "Дата: $(date)"
echo "Система: $(uname -r)"
echo ""

echo "--- Тест 1: getpid() (найпростіший syscall) ---"
echo "Виконуємо 10000 викликів через dd..."
START=$(date +%s%N)
dd if=/dev/zero of=/dev/null bs=1 count=10000 2>/dev/null
END=$(date +%s%N)
DURATION=$((($END - $START) / 1000000))
echo "Час: ${DURATION} мс"
echo ""

echo "--- Тест 2: read() syscalls ---"
echo "Читаємо /dev/zero 10000 разів..."
START=$(date +%s%N)
for i in $(seq 1 100); do
    head -c 100 /dev/zero > /dev/null
done
END=$(date +%s%N)
DURATION=$((($END - $START) / 1000000))
echo "Час: ${DURATION} мс"
echo ""

echo "--- Тест 3: open/close файлу ---"
echo "Відкриваємо/закриваємо файл 100 разів..."
touch testfile.tmp
START=$(date +%s%N)
for i in $(seq 1 100); do
    cat testfile.tmp > /dev/null 2>&1
done
END=$(date +%s%N)
DURATION=$((($END - $START) / 1000000))
echo "Час: ${DURATION} мс"
rm testfile.tmp
echo ""
EOF
chmod +x benchmark.sh
```

3. Запустіть benchmark та збережіть результати:
```bash
./benchmark.sh | tee benchmark-results.txt
```

4. Порівняйте User vs Kernel time:
```bash
echo "--- Тест 4: User vs Kernel time ---" | tee -a benchmark-results.txt
echo "Запускаємо 500 ls команд..." | tee -a benchmark-results.txt
time (for i in $(seq 1 500); do ls /tmp > /dev/null 2>&1; done) 2>&1 | tee -a benchmark-results.txt
echo "" | tee -a benchmark-results.txt
```

5. Дослідіть карту пам'яті процесу:
```bash
echo "--- Карта пам'яті shell ---" | tee -a benchmark-results.txt
cat /proc/self/maps | grep -E "heap|stack|vdso" | tee -a benchmark-results.txt
echo "" | tee -a benchmark-results.txt
```

6. Підрахуйте syscalls вашого улюбленого редактора:
```bash
echo "--- Syscalls при запуску vi ---" | tee -a benchmark-results.txt
timeout 1 strace -c vi 2>&1 | tail -15 | tee -a benchmark-results.txt
```

7. Сформулюйте висновки:
```bash
echo "" | tee -a benchmark-results.txt
echo "=== ВИСНОВКИ ===" | tee -a benchmark-results.txt
echo "1. Найшвидший syscall: getpid() (мінімальний overhead)" | tee -a benchmark-results.txt
echo "2. Overhead переходу User→Kernel: ~100-1000 нс" | tee -a benchmark-results.txt
echo "3. sys time / total time показує скільки часу в ядрі" | tee -a benchmark-results.txt
```

**Очікуваний результат:**
Файл `benchmark-results.txt` з результатами вимірювань overhead системних викликів на вашій конкретній системі.

**Бонус (для допитливих):**
- Порівняйте результати на різних ядрах (якщо є доступ до кількох машин)
- Виміряйте різницю між HDD та SSD для read() syscalls
- Створіть графік залежності часу від кількості syscalls

## Підсумок

| Компонент | Функція | Приклад |
|-----------|---------|---------|
| **Protection Rings** | Апаратна ізоляція привілеїв | Ring 0 (kernel), Ring 3 (user) |
| **Scheduler** | Розподіл CPU між процесами | CFS, FIFO, Round-Robin |
| **Memory Manager** | Віртуальна пам'ять, ізоляція | Paging, Swapping, OOM |
| **VFS** | Єдиний інтерфейс до ФС | ext4, XFS, NFS через один API |
| **Network Stack** | Мережеві протоколи | TCP/IP, sockets |
| **LSM** | Обов'язковий контроль доступу | SELinux, AppArmor |

**Ключовий висновок:** Розділення на User Mode та Kernel Mode — це не просто програмна концепція, а **апаратний механізм безпеки**. Процесор фізично блокує небезпечні операції, і жодна програма не може це обійти.

На наступній лекції детально розглянемо архітектури ядер ОС: монолітне, мікроядро та гібридне.
