---
title: "Базові поняття архітектури ОС. Принципи побудови"
type: lecture
order: 6
preview: "Монолітне ядро, мікроядро, гібридна архітектура."
---

## Дебати Торвальдса і Таненбаума

У 1992 році відбулася знаменита публічна дискусія між Лінусом Торвальдсом (автор Linux) та Ендрю Таненбаумом (автор MINIX, професор):

> **Таненбаум:** "Linux is obsolete. Monolithic kernels are a step back."
> (Linux застарів. Монолітні ядра — це крок назад.)

> **Торвальдс:** "Your langstrumpf is just about langstrumpf, linux is about langstrumpf."
> (Ваш MINIX — це академічна іграшка, Linux — це реальна система.)

30 років потому: Linux працює на 96% серверів, 85% смартфонів та 100% суперкомп'ютерів. MINIX... працює в Intel Management Engine всередині кожного процесора Intel.

**Обидва виявилися праві — для різних випадків.**

## Три архітектури ядра

```
┌─────────────────────────────────────────────────────────────────┐
│                    АРХІТЕКТУРИ ЯДЕР ОС                          │
│                                                                 │
│   МОНОЛІТНЕ            МІКРОЯДРО             ГІБРИДНЕ           │
│   (Monolithic)         (Microkernel)         (Hybrid)           │
│                                                                 │
│   User Space           User Space            User Space         │
│   ┌─────────┐          ┌─────────────────┐   ┌─────────┐        │
│   │  Apps   │          │Apps│FS │Net│Drv│   │  Apps   │        │
│   └────┬────┘          └─────────┬───────┘   └────┬────┘        │
│        │                         │                │             │
│   ═════╪═══════         ═════════╪═══════   ═════╪═══════      │
│        │                         │                │             │
│   Kernel Space          Kernel (mini)        Kernel Space      │
│   ┌─────────────────┐   ┌───────────────┐   ┌───────────────┐  │
│   │ FS │ Net │ Drv  │   │ IPC │ Sched   │   │Some FS │ Net  │  │
│   │ Sched │ MM │IPC │   │ MM  │ basic   │   │Sched │ MM    │  │
│   │ Security        │   └───────────────┘   │ Drivers (some)│  │
│   └─────────────────┘   (мінімум функцій)   └───────────────┘  │
│                                                                 │
│   Linux, FreeBSD        MINIX, QNX, L4      Windows, macOS     │
│   Швидкість ✅          Надійність ✅        Компроміс ✅        │
│   Надійність ⚠️         Швидкість ⚠️         Обидва ±           │
└─────────────────────────────────────────────────────────────────┘
```

## Монолітне ядро (Monolithic Kernel)

**Визначення:** Все ядро — один великий бінарний файл. Всі підсистеми (ФС, мережа, драйвери) працюють у Kernel Mode.

```
┌─────────────────────────────────────────────────────────────────┐
│                    МОНОЛІТНЕ ЯДРО (Linux)                       │
│                                                                 │
│   User Space                                                    │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │    Firefox    │    Terminal    │    VS Code             │  │
│   └───────────────┴────────────────┴────────────────────────┘  │
│                              │                                  │
│   ═══════════════════════════╪══════════════════════════════   │
│                              │ System Calls                     │
│   Kernel Space               ▼                                  │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                                                          │  │
│   │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │  │
│   │  │ Process  │  │  Memory  │  │   VFS    │  │ Network  │ │  │
│   │  │ Scheduler│  │ Manager  │  │          │  │  Stack   │ │  │
│   │  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │  │
│   │                                                          │  │
│   │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │  │
│   │  │   ext4   │  │   XFS    │  │   NVMe   │  │  e1000   │ │  │
│   │  │  driver  │  │  driver  │  │  driver  │  │  driver  │ │  │
│   │  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │  │
│   │                                                          │  │
│   │  ВСЕ працює в Ring 0 — повний доступ до заліза          │  │
│   │  Виклик функції = ~10 наносекунд                        │  │
│   │                                                          │  │
│   └─────────────────────────────────────────────────────────┘  │
│                              │                                  │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                      HARDWARE                            │  │
│   └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### Переваги монолітного ядра

| Перевага | Пояснення |
|----------|-----------|
| **Швидкість** | Виклик функції в ядрі ≈ 10 нс (без перемикання контексту) |
| **Простота** | Компоненти напряму викликають один одного |
| **Зрілість** | Десятиліття оптимізацій (Linux, FreeBSD) |

### Недоліки монолітного ядра

| Недолік | Пояснення |
|---------|-----------|
| **Один баг = kernel panic** | Помилка в драйвері "вбиває" всю систему |
| **Складність тестування** | 30+ мільйонів рядків коду (Linux 6.x) |
| **Безпека** | Вразливість у драйвері = доступ до всього |

**Практичний приклад:**

```bash
# Розмір ядра Linux
ls -lh /boot/vmlinuz-$(uname -r)
# ~10-15 МБ (стиснене), ~30 МБ (розпаковане)

# Скільки рядків коду в Linux
# (потрібні вихідники)
# find linux-source -name "*.c" -o -name "*.h" | xargs wc -l
# Результат: ~30 000 000 рядків

# Завантажені модулі (частина монолітного ядра, що завантажується динамічно)
lsmod | head -10

# Інформація про модуль
modinfo ext4 | head -10
```

### Модульність Linux

Linux — **модульне монолітне ядро**. Драйвери можна завантажувати/вивантажувати без перезавантаження.

```
┌─────────────────────────────────────────────────────────────────┐
│                    МОДУЛІ ЯДРА LINUX                            │
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                    Базове ядро (vmlinuz)                 │  │
│   │  Scheduler, MM, VFS, базові драйвери                    │  │
│   └───────────────────────────┬─────────────────────────────┘  │
│                               │                                 │
│        ┌──────────────────────┼──────────────────────┐         │
│        │                      │                      │         │
│        ▼                      ▼                      ▼         │
│   ┌─────────┐            ┌─────────┐            ┌─────────┐    │
│   │ nvidia  │            │  ext4   │            │bluetooth│    │
│   │  .ko    │            │  .ko    │            │  .ko    │    │
│   └─────────┘            └─────────┘            └─────────┘    │
│   Завантажується         Завантажується        Завантажується  │
│   при старті X           при mount             при потребі     │
│                                                                 │
│   Все одно Ring 0 — повні привілеї                             │
└─────────────────────────────────────────────────────────────────┘
```

```bash
# Завантажити модуль
sudo modprobe bluetooth

# Вивантажити модуль
sudo modprobe -r bluetooth

# Список модулів
lsmod | grep -E "bluetooth|nvidia|ext4"

# Параметри модуля
cat /sys/module/ext4/parameters/*
```

## Мікроядро (Microkernel)

**Визначення:** Ядро містить лише мінімум функцій. Все інше (драйвери, ФС, мережа) працює в User Space як окремі процеси.

```
┌─────────────────────────────────────────────────────────────────┐
│                    МІКРОЯДРО (QNX, MINIX, L4)                   │
│                                                                 │
│   User Space                                                    │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                                                          │  │
│   │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ │  │
│   │  │  App1  │ │  App2  │ │   FS   │ │Network │ │ Driver │ │  │
│   │  │        │ │        │ │ Server │ │ Server │ │ Server │ │  │
│   │  └───┬────┘ └───┬────┘ └───┬────┘ └───┬────┘ └───┬────┘ │  │
│   │      │          │          │          │          │       │  │
│   │      │          │          │          │          │       │  │
│   │      └──────────┴──────────┴──────────┴──────────┘       │  │
│   │                           │                               │  │
│   │                      IPC (повідомлення)                   │  │
│   │                           │                               │  │
│   └───────────────────────────┼───────────────────────────────┘  │
│                               │                                  │
│   ════════════════════════════╪════════════════════════════════ │
│                               │                                  │
│   Kernel Space (МІНІМАЛЬНЕ)   ▼                                 │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │  │
│   │  │     IPC      │  │  Scheduler   │  │  Basic MM    │   │  │
│   │  │ (message     │  │  (перемикання│  │  (сторінки)  │   │  │
│   │  │  passing)    │  │   контексту) │  │              │   │  │
│   │  └──────────────┘  └──────────────┘  └──────────────┘   │  │
│   │                                                          │  │
│   │  ~10 000 рядків коду (vs 30 000 000 у Linux)            │  │
│   └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### Переваги мікроядра

| Перевага | Пояснення |
|----------|-----------|
| **Надійність** | Crash драйвера ≠ crash системи (можна перезапустити) |
| **Безпека** | Мінімальна поверхня атаки в Ring 0 |
| **Модифікованість** | Легко замінити компонент без перекомпіляції ядра |
| **Формальна верифікація** | seL4 — математично доведена коректність |

### Недоліки мікроядра

| Недолік | Пояснення |
|---------|-----------|
| **Overhead IPC** | Кожен виклик = повідомлення через ядро (~1000 нс vs 10 нс) |
| **Складність** | Розподілена система, важче відлагоджувати |
| **Менша екосистема** | Менше драйверів, менше підтримки |

### Приклад: читання файлу

```
┌─────────────────────────────────────────────────────────────────┐
│           МОНОЛІТНЕ ЯДРО              МІКРОЯДРО                 │
│                                                                 │
│   App: read(fd, buf, 100)         App: read(fd, buf, 100)      │
│            │                               │                    │
│            │ syscall                       │ send(FS_SERVER,    │
│            ▼                               │      READ_MSG)     │
│   ┌─────────────────┐                      ▼                    │
│   │  Kernel Space   │            ┌─────────────────┐           │
│   │  ┌───────────┐  │            │  Microkernel    │           │
│   │  │    VFS    │  │            │  ┌───────────┐  │           │
│   │  └─────┬─────┘  │            │  │    IPC    │  │           │
│   │        │        │            │  └─────┬─────┘  │           │
│   │  ┌─────▼─────┐  │            └────────┼────────┘           │
│   │  │   ext4    │  │                     │                    │
│   │  └─────┬─────┘  │                     ▼                    │
│   │        │        │            ┌─────────────────┐           │
│   │  ┌─────▼─────┐  │            │   FS Server     │           │
│   │  │   block   │  │            │  (User Space)   │           │
│   │  │   driver  │  │            └────────┬────────┘           │
│   │  └───────────┘  │                     │                    │
│   └─────────────────┘                     │ send(DISK_SERVER,  │
│                                           │      READ_BLOCK)   │
│   1 syscall, ~1000 нс                     ▼                    │
│                                  ┌─────────────────┐           │
│                                  │  Disk Server    │           │
│                                  │  (User Space)   │           │
│                                  └─────────────────┘           │
│                                                                 │
│                                  4+ IPC messages, ~5000 нс     │
└─────────────────────────────────────────────────────────────────┘
```

### Де використовуються мікроядра

| Система | Сфера | Чому мікроядро |
|---------|-------|----------------|
| **QNX** | Автомобілі, медицина | Сертифікація безпеки, перезапуск компонентів |
| **seL4** | Оборона, аерокосмос | Математично доведена коректність |
| **MINIX 3** | Intel ME (в кожному CPU) | Мінімальна поверхня атаки |
| **Fuchsia (Zircon)** | Google, IoT | Модульність, безпека |
| **L4 family** | Дослідження, вбудовані | Швидкий IPC |

## Гібридне ядро (Hybrid Kernel)

**Визначення:** Компроміс — частина сервісів у ядрі (для швидкості), частина — в User Space (для надійності).

```
┌─────────────────────────────────────────────────────────────────┐
│                    ГІБРИДНЕ ЯДРО (Windows NT)                   │
│                                                                 │
│   User Space                                                    │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │  Applications  │  Subsystems   │  Services               │  │
│   │  (Win32 apps)  │  (POSIX, OS/2)│  (Print, Event Log)     │  │
│   └───────────────┴───────────────┴─────────────────────────┘  │
│                               │                                 │
│                               │ NT API                          │
│   ┌───────────────────────────▼───────────────────────────────┐│
│   │                    NTDLL.DLL                               ││
│   │              (User-mode NT interface)                      ││
│   └───────────────────────────┬───────────────────────────────┘│
│                               │ syscall                         │
│   ═══════════════════════════════════════════════════════════  │
│                               │                                 │
│   Kernel Space                ▼                                 │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                    Executive                             │  │
│   │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ │  │
│   │  │  I/O   │ │ Object │ │Process │ │ Memory │ │Security│ │  │
│   │  │Manager │ │Manager │ │Manager │ │Manager │ │  Ref   │ │  │
│   │  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘ │  │
│   ├─────────────────────────────────────────────────────────┤  │
│   │                    Kernel (microkernel-like)             │  │
│   │  Scheduling, Interrupts, Synchronization                 │  │
│   ├─────────────────────────────────────────────────────────┤  │
│   │                    HAL (Hardware Abstraction Layer)      │  │
│   └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│   Деякі драйвери в kernel, деякі в user space                  │
└─────────────────────────────────────────────────────────────────┘
```

### macOS XNU (X is Not Unix)

```
┌─────────────────────────────────────────────────────────────────┐
│                    macOS XNU KERNEL                             │
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                      User Space                          │  │
│   │  Applications, Frameworks, Daemons                       │  │
│   └───────────────────────────┬─────────────────────────────┘  │
│                               │                                 │
│   ═══════════════════════════════════════════════════════════  │
│                               │                                 │
│   ┌───────────────────────────▼─────────────────────────────┐  │
│   │                        XNU                               │  │
│   │                                                          │  │
│   │  ┌──────────────────────────────────────────────────┐   │  │
│   │  │                    Mach                           │   │  │
│   │  │  (Мікроядро: IPC, scheduling, VM)                │   │  │
│   │  └──────────────────────────────────────────────────┘   │  │
│   │                          +                               │  │
│   │  ┌──────────────────────────────────────────────────┐   │  │
│   │  │                    BSD                            │   │  │
│   │  │  (UNIX layer: VFS, networking, POSIX)            │   │  │
│   │  └──────────────────────────────────────────────────┘   │  │
│   │                          +                               │  │
│   │  ┌──────────────────────────────────────────────────┐   │  │
│   │  │                   I/O Kit                         │   │  │
│   │  │  (Driver framework, C++)                          │   │  │
│   │  └──────────────────────────────────────────────────┘   │  │
│   │                                                          │  │
│   └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│   Mach (мікроядро) + BSD (монолітний) = гібрид                 │
└─────────────────────────────────────────────────────────────────┘
```

## Порівняльна таблиця

| Критерій | Монолітне | Мікроядро | Гібридне |
|----------|-----------|-----------|----------|
| **Приклади** | Linux, FreeBSD | QNX, MINIX, seL4 | Windows NT, macOS |
| **Розмір ядра** | 30M+ рядків | 10-50K рядків | Середній |
| **Продуктивність** | ★★★★★ | ★★★☆☆ | ★★★★☆ |
| **Надійність** | ★★★☆☆ | ★★★★★ | ★★★★☆ |
| **Безпека** | ★★★☆☆ | ★★★★★ | ★★★★☆ |
| **Складність розробки** | Середня | Висока | Висока |
| **Драйвери** | В ядрі | User space | Частково/частково |
| **Crash драйвера** | Kernel panic | Перезапуск сервісу | Залежить |

## Принципи побудови ОС

### 1. Принцип модульності

```
┌─────────────────────────────────────────────────────────────────┐
│                    МОДУЛЬНА СТРУКТУРА                           │
│                                                                 │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐                     │
│   │ Module A │◄─┤ Module B │◄─┤ Module C │                     │
│   │          │  │          │  │          │                     │
│   │ Чітко    │  │ Чітко    │  │ Чітко    │                     │
│   │ визначений│  │ визначений│  │ визначений│                     │
│   │ інтерфейс │  │ інтерфейс │  │ інтерфейс │                     │
│   └──────────┘  └──────────┘  └──────────┘                     │
│                                                                 │
│   ✅ Можна замінити модуль без зміни інших                     │
│   ✅ Легше тестувати окремо                                    │
│   ✅ Паралельна розробка                                       │
└─────────────────────────────────────────────────────────────────┘
```

### 2. Принцип абстракції

```
┌─────────────────────────────────────────────────────────────────┐
│                    РІВНІ АБСТРАКЦІЇ                             │
│                                                                 │
│   Програміст бачить:        Реальність:                        │
│                                                                 │
│   open("/file.txt")    →    Block device 8:1                   │
│                              Sector 12345                       │
│                              Inode 67890                        │
│                              Disk controller command            │
│                                                                 │
│   malloc(1024)         →    Page table entry                   │
│                              Physical frame 0x1A2B3000          │
│                              TLB update                         │
│                                                                 │
│   Абстракція приховує складність                               │
└─────────────────────────────────────────────────────────────────┘
```

### 3. Принцип розділення механізму та політики

```
┌─────────────────────────────────────────────────────────────────┐
│              МЕХАНІЗМ vs ПОЛІТИКА                               │
│                                                                 │
│   МЕХАНІЗМ (Kernel):           ПОЛІТИКА (User Space):          │
│   "Як робити"                  "Що робити"                     │
│                                                                 │
│   • Перемикання контексту      • Який процес запустити         │
│   • Блокування сторінок        • Які сторінки вивантажити      │
│   • Передача повідомлень       • Кому дозволити доступ         │
│                                                                 │
│   Механізми стабільні          Політики змінюються             │
│   (змінюються рідко)           (налаштовуються)                │
│                                                                 │
│   Приклад: Linux scheduler     CFS, FIFO, RR — різні політики  │
│            реалізує механізм   на одному механізмі             │
└─────────────────────────────────────────────────────────────────┘
```

## Практичне завдання

Дослідіть архітектуру вашої системи:

```bash
# 1. Тип ядра
uname -s  # Linux = монолітне модульне

# 2. Кількість модулів ядра
lsmod | wc -l

# 3. Інформація про ядро
cat /proc/version

# 4. Системні виклики (монолітний стиль)
cat /proc/kallsyms | grep sys_ | wc -l

# 5. IPC механізми (є в Linux, але основні сервіси в ядрі)
ipcs -a

# 6. Час syscall (швидкий = монолітний)
time dd if=/dev/zero of=/dev/null bs=1 count=100000

# 7. На macOS: перевірити компоненти XNU
# sysctl kern.ostype kern.osrelease
```

## 🏢 Real World: Як це використовують у великих компаніях

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    АРХІТЕКТУРИ ЯДЕР У PRODUCTION                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  GOOGLE (Монолітне ядро з модифікаціями)                                    │
│  ├── Власний fork Linux kernel з тисячами патчів                           │
│  ├── Custom scheduler для warehouse-scale computing                        │
│  ├── BPF/eBPF для observability без зміни ядра                            │
│  └── Android: Linux kernel + Hardware Abstraction Layer                    │
│                                                                             │
│  BLACKBERRY QNX (Мікроядро в автомобілях)                                   │
│  ├── 215+ мільйонів автомобілів використовують QNX                         │
│  ├── ISO 26262 сертифікація для автомобільної безпеки                      │
│  ├── Crash драйвера = перезапуск сервісу, не системи                       │
│  └── BMW, Mercedes, Audi, Ford — інфотейнмент та ADAS                      │
│                                                                             │
│  MICROSOFT (Гібридне ядро для екосистеми)                                   │
│  ├── Windows NT kernel: Executive + microkernel-like core                  │
│  ├── Драйвери частково в kernel, частково в user space                     │
│  ├── Xbox Series X: модифікований Windows kernel для gaming               │
│  └── Azure: Windows + Linux (більше Linux серверів!)                       │
│                                                                             │
│  APPLE (Гібрид XNU = Mach + BSD)                                            │
│  ├── Mach microkernel: IPC, VM, scheduling                                 │
│  ├── BSD layer: POSIX API, VFS, networking                                 │
│  ├── I/O Kit: об'єктно-орієнтовані драйвери                               │
│  └── M-серія: оптимізації для ARM64 в XNU kernel                           │
│                                                                             │
│  AEROSPACE (Мікроядро для критичних систем)                                 │
│  ├── seL4 у військових дронах — математично верифікований                  │
│  ├── Boeing 787: VxWorks для flight management                             │
│  ├── NASA Mars Helicopter: VxWorks RTOS                                    │
│  └── SpaceX Dragon: Linux для некритичних систем                           │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 💼 Career Spotlight

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    КАР'ЄРНІ ШЛЯХИ: АРХІТЕКТУРА ЯДРА                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  🐧 LINUX KERNEL ENGINEER                                                   │
│     Розробка та підтримка ядра Linux, підсистем, драйверів                 │
│     Зарплата: $150,000 - $350,000 (USA) | €80,000 - €180,000 (EU)          │
│     Компанії: Red Hat, Google, Meta, Intel, AMD, NVIDIA                    │
│     Навички: C, git, lkml, kernel debugging, patch submission              │
│                                                                             │
│  🚗 AUTOMOTIVE OS ENGINEER                                                  │
│     Розробка для QNX, AUTOSAR, automotive Linux                            │
│     Зарплата: $120,000 - $200,000 (USA) | €70,000 - €130,000 (EU)          │
│     Компанії: BlackBerry QNX, Tesla, BMW, Waymo, Bosch                     │
│     Навички: QNX, RTOS, CAN bus, ISO 26262, functional safety              │
│                                                                             │
│  🛡️  SECURITY ENGINEER (OS Level)                                           │
│     Аналіз архітектури ядра, пошук вразливостей, hardening                 │
│     Зарплата: $140,000 - $280,000 (USA) | €75,000 - €150,000 (EU)          │
│     Компанії: Google Project Zero, Microsoft MSRC, Apple                   │
│     Навички: Kernel exploits, sandboxing, formal verification              │
│                                                                             │
│  🍎 MACOS/IOS KERNEL ENGINEER                                               │
│     Розробка XNU kernel, драйверів, system extensions                      │
│     Зарплата: $160,000 - $300,000 (USA) | (Apple рідко в EU)               │
│     Компанії: Apple (Cupertino переважно)                                  │
│     Навички: XNU, Mach, BSD, I/O Kit, Swift/Objective-C                    │
│                                                                             │
│  🔬 OS RESEARCH SCIENTIST                                                   │
│     Нові архітектури, формальна верифікація, unikernels                    │
│     Зарплата: $120,000 - $250,000 (USA) | €60,000 - €120,000 (EU)          │
│     Організації: MIT, Stanford, ETH Zurich, MSR, Google Research          │
│     Навички: Theory, Coq/Isabelle, Rust, research papers, PhD              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🔗 Корисні ресурси

### Онлайн-практика

| Ресурс | Опис | Посилання |
|--------|------|-----------|
| **OS Dev Wiki** | Створення ядра з нуля | wiki.osdev.org |
| **Writing an OS in Rust** | Туторіал: мікроядро на Rust | os.phil-opp.com |
| **xv6 (MIT)** | Навчальна UNIX-like ОС | pdos.csail.mit.edu/6.828 |
| **seL4 Tutorials** | Практика з верифікованим мікроядром | sel4.systems/tutorials |
| **Linux From Scratch** | Побудова Linux дистрибутиву з нуля | linuxfromscratch.org |

### Книги

| Назва | Автор | Архітектура |
|-------|-------|-------------|
| "Operating Systems: Design & Implementation" | Tanenbaum | Мікроядро (MINIX) |
| "Linux Kernel Development" | Robert Love | Монолітне (Linux) |
| "Windows Internals" | Russinovich | Гібридне (Windows NT) |
| "Mac OS X Internals" | Amit Singh | Гібридне (XNU) |
| "Operating Systems: Three Easy Pieces" | Arpaci-Dusseau | Загальні концепції |

### YouTube-канали

| Канал | Тематика |
|-------|----------|
| **Andreas Kling (SerenityOS)** | Розробка ОС live, монолітне ядро |
| **OSDev Wiki (community)** | Туторіали зі створення ядра |
| **FOSDEM** | Доповіді про архітектуру ОС |
| **Linux Plumbers Conference** | Глибокі технічні доповіді |
| **CppCon** | Системне програмування, OS dev |

## 📋 Cheat Sheet

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    АРХІТЕКТУРИ ЯДЕР ОС                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ТРИ ОСНОВНІ АРХІТЕКТУРИ:                                                   │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ МОНОЛІТНЕ       │ Все в Ring 0    │ Linux, FreeBSD, OpenBSD         │  │
│  │ (Monolithic)    │ Швидко, але     │ 30M+ рядків коду                 │  │
│  │                 │ один баг = panic│ Модулі: можна довантажувати      │  │
│  ├─────────────────┼─────────────────┼──────────────────────────────────┤  │
│  │ МІКРОЯДРО       │ Мінімум в Ring 0│ QNX, MINIX, seL4, L4             │  │
│  │ (Microkernel)   │ Драйвери в user │ 10-50K рядків коду               │  │
│  │                 │ space, надійно  │ Crash ≠ система, можна restart   │  │
│  ├─────────────────┼─────────────────┼──────────────────────────────────┤  │
│  │ ГІБРИДНЕ        │ Компроміс       │ Windows NT, macOS (XNU)          │  │
│  │ (Hybrid)        │ Критичне в Ring │ Баланс швидкості та надійності   │  │
│  │                 │ 0, інше - user  │                                  │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ПОРІВНЯННЯ:                                                                │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ Критерій       │ Монолітне │ Мікро     │ Гібридне                   │  │
│  │────────────────┼───────────┼───────────┼────────────────────────────│  │
│  │ Швидкість      │ ★★★★★    │ ★★★☆☆    │ ★★★★☆                      │  │
│  │ Надійність     │ ★★★☆☆    │ ★★★★★    │ ★★★★☆                      │  │
│  │ Безпека        │ ★★★☆☆    │ ★★★★★    │ ★★★★☆                      │  │
│  │ IPC overhead   │ ~10 нс    │ ~1000 нс  │ ~100 нс                     │  │
│  │ Crash драйвера │ Panic     │ Restart   │ Залежить                    │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ПРИНЦИПИ ПОБУДОВИ ОС:                                                      │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ 1. Модульність       │ Чіткі інтерфейси між компонентами            │  │
│  │ 2. Абстракція        │ Приховання складності (файл vs сектори)      │  │
│  │ 3. Механізм/Політика │ "Як" в ядрі, "Що" в user space               │  │
│  │ 4. Layering          │ Рівні абстракції (hardware→driver→VFS→app)   │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  КОМАНДИ ДЛЯ ДОСЛІДЖЕННЯ:                                                   │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ uname -s                  → Тип ядра (Linux, Darwin, etc.)          │  │
│  │ lsmod | wc -l             → Кількість модулів (монолітне+модулі)    │  │
│  │ cat /proc/version         → Деталі збірки ядра                      │  │
│  │ modinfo <module>          → Інформація про модуль                   │  │
│  │ ls /sys/module/           → Всі завантажені модулі                  │  │
│  │ sysctl -a | wc -l         → Кількість параметрів ядра               │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ВИБІР АРХІТЕКТУРИ ЗА СЦЕНАРІЄМ:                                            │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ Web-сервер, хмара   → Монолітне (Linux) — швидкість                 │  │
│  │ Автомобіль, медицина→ Мікроядро (QNX) — надійність, сертифікація   │  │
│  │ Desktop             → Гібридне (Windows/macOS) — баланс            │  │
│  │ Дослідження         → Мікроядро (seL4) — верифікація               │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## ❓ Питання для самоперевірки

1. **У чому полягає суть дебатів Торвальдса і Таненбаума? Чому обидва виявилися праві для різних випадків?**

2. **Поясніть, чому виклик функції в монолітному ядрі займає ~10 нс, а IPC в мікроядрі ~1000 нс. Які операції відбуваються?**

3. **Чому Linux вважається "модульним монолітним" ядром? Як це відрізняється від чистого мікроядра?**

4. **Для яких застосувань мікроядро QNX підходить краще, ніж Linux? Обгрунтуйте відповідь.**

5. **Поясніть принцип "розділення механізму та політики" на прикладі планувальника процесів.**

## 🎯 Міні-проект (30 хв)

### Завдання: Карта архітектури вашого ядра

Створіть детальну "карту" архітектури ядра вашої системи — візуалізуйте модулі, підсистеми та їх взаємозв'язки.

**Кроки:**

1. Створіть робочу директорію:
```bash
mkdir ~/kernel-map && cd ~/kernel-map
```

2. Зберіть базову інформацію про архітектуру:
```bash
echo "=== КАРТА АРХІТЕКТУРИ ЯДРА ===" > kernel-map.txt
echo "Дата: $(date)" >> kernel-map.txt
echo "" >> kernel-map.txt

echo "--- БАЗОВА ІНФОРМАЦІЯ ---" >> kernel-map.txt
echo "Тип ядра: $(uname -s)" >> kernel-map.txt
echo "Версія: $(uname -r)" >> kernel-map.txt
echo "Архітектура: $(uname -m)" >> kernel-map.txt

# Визначте тип архітектури
if [ "$(uname -s)" = "Linux" ]; then
    echo "Архітектура ядра: Монолітне модульне" >> kernel-map.txt
elif [ "$(uname -s)" = "Darwin" ]; then
    echo "Архітектура ядра: Гібридне (XNU = Mach + BSD)" >> kernel-map.txt
fi
echo "" >> kernel-map.txt
```

3. Проаналізуйте модулі ядра:
```bash
echo "--- СТАТИСТИКА МОДУЛІВ ---" >> kernel-map.txt
echo "Загальна кількість: $(lsmod | wc -l)" >> kernel-map.txt
echo "" >> kernel-map.txt

echo "Топ-10 модулів за розміром:" >> kernel-map.txt
lsmod | sort -k2 -n -r | head -10 >> kernel-map.txt
echo "" >> kernel-map.txt
```

4. Категоризуйте модулі за типом:
```bash
echo "--- КАТЕГОРІЇ МОДУЛІВ ---" >> kernel-map.txt

echo "Мережеві драйвери:" >> kernel-map.txt
lsmod | grep -iE "net|eth|wifi|wlan|iwl|ath|rtl|e1000" | wc -l >> kernel-map.txt

echo "Файлові системи:" >> kernel-map.txt
lsmod | grep -iE "ext4|xfs|btrfs|ntfs|fat|nfs" | wc -l >> kernel-map.txt

echo "USB драйвери:" >> kernel-map.txt
lsmod | grep -i "usb" | wc -l >> kernel-map.txt

echo "Графіка/GPU:" >> kernel-map.txt
lsmod | grep -iE "drm|nvidia|amd|intel|nouveau|radeon" | wc -l >> kernel-map.txt

echo "" >> kernel-map.txt
```

5. Створіть візуальну структуру:
```bash
echo "--- ВІЗУАЛЬНА СТРУКТУРА ---" >> kernel-map.txt
echo "" >> kernel-map.txt
echo "┌─────────────────────────────────────────┐" >> kernel-map.txt
echo "│              USER SPACE                  │" >> kernel-map.txt
echo "│  (ваші програми: $(ps aux | wc -l) процесів)  │" >> kernel-map.txt
echo "├─────────────────────────────────────────┤" >> kernel-map.txt
echo "│           СИСТЕМНІ ВИКЛИКИ               │" >> kernel-map.txt
echo "├─────────────────────────────────────────┤" >> kernel-map.txt
echo "│              KERNEL SPACE                │" >> kernel-map.txt
echo "│                                          │" >> kernel-map.txt
echo "│  ┌────────────────────────────────────┐ │" >> kernel-map.txt
echo "│  │ Базове ядро (vmlinuz)              │ │" >> kernel-map.txt
echo "│  │ Розмір: $(ls -lh /boot/vmlinuz-$(uname -r) 2>/dev/null | awk '{print $5}')              │ │" >> kernel-map.txt
echo "│  └────────────────────────────────────┘ │" >> kernel-map.txt
echo "│            ↑↓                            │" >> kernel-map.txt
echo "│  ┌────────────────────────────────────┐ │" >> kernel-map.txt
echo "│  │ Модулі (.ko): $(lsmod | wc -l) завантажено │ │" >> kernel-map.txt
echo "│  └────────────────────────────────────┘ │" >> kernel-map.txt
echo "├─────────────────────────────────────────┤" >> kernel-map.txt
echo "│              HARDWARE                    │" >> kernel-map.txt
echo "└─────────────────────────────────────────┘" >> kernel-map.txt
echo "" >> kernel-map.txt
```

6. Порівняйте з теорією:
```bash
echo "--- ПОРІВНЯННЯ З ТЕОРІЄЮ ---" >> kernel-map.txt
echo "" >> kernel-map.txt
echo "Linux (монолітне): ~30M рядків коду, $(ls -lh /boot/vmlinuz-$(uname -r) 2>/dev/null | awk '{print $5}')" >> kernel-map.txt
echo "seL4 (мікроядро): ~10K рядків коду, ~10 KB" >> kernel-map.txt
echo "Різниця: монолітне ~1000x більше, але швидше" >> kernel-map.txt
```

7. Перегляньте результат:
```bash
cat kernel-map.txt
```

**Очікуваний результат:**
Файл `kernel-map.txt` з повною картою архітектури вашого ядра, статистикою модулів та візуальною структурою.

**Бонус (для допитливих):**
- Знайдіть залежності між модулями (`modinfo <module> | grep depends`)
- Порівняйте розмір ядра до і після завантаження модулів
- Дослідіть, які модулі можна безпечно вивантажити

## Підсумок

| Архітектура | Коли використовувати | Приклади |
|-------------|---------------------|----------|
| **Монолітне** | Максимальна продуктивність, загальне призначення | Linux, FreeBSD |
| **Мікроядро** | Критична надійність, сертифікація, безпека | QNX (авто), seL4 (оборона) |
| **Гібридне** | Баланс продуктивності та надійності | Windows, macOS |

**Ключовий висновок:** Немає "найкращої" архітектури — є архітектура, оптимальна для конкретної задачі. Linux домінує на серверах через продуктивність. QNX керує автомобілями через надійність. Windows NT — компроміс для десктопів.

На наступній лекції почнемо детальне вивчення операційної системи Windows.
