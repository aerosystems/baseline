---
title: "Огляд операційної системи Linux"
type: lecture
order: 11
preview: "Історія Linux, ядро, GNU/Linux."
---

## Linux — основа сучасної інфраструктури

Linux працює скрізь:
- **96%** веб-серверів
- **85%** смартфонів (Android)
- **100%** суперкомп'ютерів TOP500
- **Tesla**, SpaceX, NASA Mars rovers
- Ваш роутер, Smart TV, холодильник

При цьому на десктопах лише ~4%. Чому така різниця? Розберемося.

## Народження Linux

```
┌─────────────────────────────────────────────────────────────────┐
│                    25 СЕРПНЯ 1991 РОКУ                          │
│                                                                 │
│   Лінус Торвальдс (21 рік, студент Університету Гельсінкі)     │
│   публікує в comp.os.minix:                                    │
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │ "Hello everybody out there using minix -                │  │
│   │                                                          │  │
│   │  I'm doing a (free) operating system (just a hobby,     │  │
│   │  won't be big and professional like gnu) for 386(486)   │  │
│   │  AT clones."                                             │  │
│   │                                                          │  │
│   │  "It is NOT portable (uses 386 task switching etc),     │  │
│   │   and it probably never will support anything other     │  │
│   │   than AT-harddisks, as that's all I have :-("          │  │
│   └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│   Сьогодні Linux:                                              │
│   • 30+ мільйонів рядків коду                                  │
│   • 15,000+ розробників                                        │
│   • Працює на ARM, x86, RISC-V, PowerPC, s390...               │
│   • Підтримує 99.9% сучасного обладнання                       │
└─────────────────────────────────────────────────────────────────┘
```

### Хронологія

| Рік | Подія |
|-----|-------|
| 1991 | Linux 0.01 — перший реліз (10,000 рядків) |
| 1992 | Linux під GPL, перші дистрибутиви |
| 1994 | Linux 1.0 |
| 1996 | Tux (пінгвін) — офіційний маскот |
| 2000 | IBM інвестує $1 млрд в Linux |
| 2003 | Linux 2.6 — масштабованість для серверів |
| 2008 | Android 1.0 (Linux kernel) |
| 2011 | Linux 3.0 |
| 2015 | Linux 4.0 |
| 2020 | Linux 5.x — основа хмарної інфраструктури |
| 2024 | Linux 6.x — 30+ млн рядків коду |

## GNU/Linux

```
┌─────────────────────────────────────────────────────────────────┐
│                    GNU + LINUX = GNU/Linux                      │
│                                                                 │
│   1983: Richard Stallman оголошує проект GNU                   │
│         (GNU's Not Unix)                                        │
│                                                                 │
│   Мета: створити повністю вільну UNIX-подібну ОС               │
│                                                                 │
│   GNU проект створив:                                          │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │  • GCC (компілятор)                                     │  │
│   │  • Bash (shell)                                          │  │
│   │  • coreutils (ls, cp, mv, rm...)                        │  │
│   │  • glibc (стандартна бібліотека C)                      │  │
│   │  • Emacs (редактор)                                      │  │
│   │  • ...але не ядро (GNU Hurd досі в розробці)            │  │
│   └─────────────────────────────────────────────────────────┘  │
│                              +                                  │
│   1991: Лінус Торвальдс створює ядро Linux                     │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │  • Kernel (ядро операційної системи)                    │  │
│   └─────────────────────────────────────────────────────────┘  │
│                              =                                  │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │              Повноцінна ОС: GNU/Linux                    │  │
│   │                                                          │  │
│   │  Формально правильно: GNU/Linux                         │  │
│   │  Всі кажуть просто: Linux                               │  │
│   └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## Архітектура Linux

```
┌─────────────────────────────────────────────────────────────────┐
│                    АРХІТЕКТУРА LINUX                            │
│                                                                 │
│   USER SPACE                                                    │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                                                          │  │
│   │  Applications                                            │  │
│   │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐    │  │
│   │  │ Firefox  │ │ Terminal │ │ VS Code  │ │  Docker  │    │  │
│   │  └──────────┘ └──────────┘ └──────────┘ └──────────┘    │  │
│   │                                                          │  │
│   │  System Libraries                                        │  │
│   │  ┌──────────┐ ┌──────────┐ ┌──────────┐                 │  │
│   │  │  glibc   │ │ libpthread│ │  libm    │ ...            │  │
│   │  └──────────┘ └──────────┘ └──────────┘                 │  │
│   │                                                          │  │
│   │  Shell & Utilities (GNU)                                 │  │
│   │  ┌──────────┐ ┌──────────┐ ┌──────────┐                 │  │
│   │  │  bash    │ │coreutils │ │  grep    │ ...             │  │
│   │  └──────────┘ └──────────┘ └──────────┘                 │  │
│   │                                                          │  │
│   └───────────────────────────┬─────────────────────────────┘  │
│                               │ System Calls                    │
│   ════════════════════════════╪════════════════════════════    │
│                               │                                 │
│   KERNEL SPACE                ▼                                 │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                                                          │  │
│   │  System Call Interface                                   │  │
│   │  ────────────────────────────────────────────────────   │  │
│   │                                                          │  │
│   │  ┌────────────┐ ┌────────────┐ ┌────────────┐          │  │
│   │  │  Process   │ │   Memory   │ │   Virtual  │          │  │
│   │  │ Management │ │ Management │ │ Filesystem │          │  │
│   │  └────────────┘ └────────────┘ └────────────┘          │  │
│   │                                                          │  │
│   │  ┌────────────┐ ┌────────────┐ ┌────────────┐          │  │
│   │  │  Network   │ │  Device    │ │  Security  │          │  │
│   │  │   Stack    │ │  Drivers   │ │   (LSM)    │          │  │
│   │  └────────────┘ └────────────┘ └────────────┘          │  │
│   │                                                          │  │
│   │  ────────────────────────────────────────────────────   │  │
│   │  Architecture-dependent code (arch/x86, arch/arm...)    │  │
│   └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                      HARDWARE                            │  │
│   └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## Ядро Linux

### Що входить в ядро

```
┌─────────────────────────────────────────────────────────────────┐
│                    КОМПОНЕНТИ ЯДРА LINUX                        │
│                                                                 │
│   ┌──────────────────────────────────────────────────────────┐ │
│   │ ПЛАНУВАЛЬНИК ПРОЦЕСІВ (scheduler)                        │ │
│   │ • CFS (Completely Fair Scheduler) — за замовчуванням    │ │
│   │ • FIFO, Round-Robin — для real-time                     │ │
│   │ • Deadline — для задач з дедлайнами                     │ │
│   └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│   ┌──────────────────────────────────────────────────────────┐ │
│   │ ПІДСИСТЕМА ПАМ'ЯТІ (mm)                                  │ │
│   │ • Віртуальна пам'ять                                    │ │
│   │ • Сторінки (pages), swap                                │ │
│   │ • NUMA support                                          │ │
│   │ • OOM Killer                                            │ │
│   └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│   ┌──────────────────────────────────────────────────────────┐ │
│   │ ВІРТУАЛЬНА ФАЙЛОВА СИСТЕМА (VFS)                         │ │
│   │ • Єдиний інтерфейс для ext4, XFS, Btrfs, NFS...         │ │
│   │ • Кешування (page cache)                                │ │
│   │ • Inode, dentry, superblock                             │ │
│   └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│   ┌──────────────────────────────────────────────────────────┐ │
│   │ МЕРЕЖЕВИЙ СТЕК                                           │ │
│   │ • TCP/IP, UDP, ICMP                                     │ │
│   │ • Sockets API                                           │ │
│   │ • Netfilter (iptables/nftables)                         │ │
│   └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│   ┌──────────────────────────────────────────────────────────┐ │
│   │ ДРАЙВЕРИ ПРИСТРОЇВ                                       │ │
│   │ • Block devices (NVMe, SATA, USB storage)               │ │
│   │ • Character devices (terminals, serial)                 │ │
│   │ • Network devices (Ethernet, WiFi)                      │ │
│   │ • GPU (DRM subsystem)                                   │ │
│   └──────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### Практичні команди

```bash
# Версія ядра
uname -r
# Приклад: 6.5.0-44-generic

# Повна інформація
uname -a

# Де знаходиться ядро
ls -la /boot/vmlinuz*

# Параметри завантаження
cat /proc/cmdline

# Завантажені модулі ядра
lsmod | head -20

# Інформація про модуль
modinfo ext4

# Параметри ядра (runtime)
sysctl -a | head -20

# Змінити параметр (тимчасово)
sudo sysctl vm.swappiness=10

# Логи ядра
dmesg | tail -30
journalctl -k
```

## Файлова система Linux

### Ієрархія FHS (Filesystem Hierarchy Standard)

```
┌─────────────────────────────────────────────────────────────────┐
│                    СТРУКТУРА КАТАЛОГІВ LINUX                    │
│                                                                 │
│   /                          Root — корінь усього               │
│   ├── bin/                   Базові команди (ls, cp, bash)     │
│   ├── boot/                  Ядро та завантажувач              │
│   │   ├── vmlinuz-*          Ядро                              │
│   │   └── initrd.img-*       Initial ramdisk                   │
│   ├── dev/                   Файли пристроїв                   │
│   │   ├── sda                Перший диск                       │
│   │   ├── tty*               Термінали                         │
│   │   └── null               /dev/null — "чорна діра"          │
│   ├── etc/                   Конфігурація системи              │
│   │   ├── passwd             Список користувачів               │
│   │   ├── fstab              Монтування файлових систем        │
│   │   └── ssh/               Конфігурація SSH                  │
│   ├── home/                  Домашні каталоги користувачів     │
│   │   └── username/                                            │
│   ├── lib/, lib64/           Системні бібліотеки               │
│   ├── mnt/, media/           Точки монтування                  │
│   ├── opt/                   Опціональне ПЗ                    │
│   ├── proc/                  Віртуальна ФС — процеси           │
│   │   ├── 1/                 PID 1 (init/systemd)              │
│   │   ├── cpuinfo            Інформація про CPU                │
│   │   └── meminfo            Інформація про пам'ять            │
│   ├── root/                  Домашній каталог root             │
│   ├── sbin/                  Системні команди (адмін)          │
│   ├── sys/                   Віртуальна ФС — ядро/пристрої     │
│   ├── tmp/                   Тимчасові файли                   │
│   ├── usr/                   Userland програми                 │
│   │   ├── bin/               Користувацькі програми            │
│   │   ├── lib/               Бібліотеки                        │
│   │   ├── local/             Локальні встановлення             │
│   │   └── share/             Дані (документація, icons)        │
│   └── var/                   Змінні дані                       │
│       ├── log/               Логи системи                      │
│       ├── www/               Web-сервер                        │
│       └── lib/               Бази даних, стан                  │
└─────────────────────────────────────────────────────────────────┘
```

### Практика

```bash
# Де я зараз?
pwd

# Перейти
cd /var/log

# Показати вміст
ls -la

# Розмір каталогів
du -sh /var/*

# Монтування
mount | column -t

# Дисковий простір
df -h

# Тип файлової системи
df -T
```

## Користувачі та права

```
┌─────────────────────────────────────────────────────────────────┐
│                    ПРАВА ДОСТУПУ В LINUX                        │
│                                                                 │
│   ls -la file.txt                                              │
│   -rw-r--r-- 1 user group 1234 Jan 15 10:30 file.txt          │
│   ││  │  │                                                     │
│   ││  │  └── others: read only                                │
│   ││  └───── group: read only                                 │
│   │└──────── owner: read + write                              │
│   └───────── type: - (file), d (dir), l (link)                │
│                                                                 │
│   Числове представлення:                                       │
│   r=4, w=2, x=1                                                │
│   rw-r--r-- = 644                                              │
│   rwxr-xr-x = 755                                              │
│   rwx------ = 700                                              │
└─────────────────────────────────────────────────────────────────┘
```

```bash
# Поточний користувач
whoami
id

# Змінити права
chmod 755 script.sh
chmod +x script.sh      # Додати executable
chmod u+w,g-w file.txt  # User +write, group -write

# Змінити власника
sudo chown user:group file.txt

# Список користувачів
cat /etc/passwd

# Групи користувача
groups
groups username
```

## Процеси та systemd

```
┌─────────────────────────────────────────────────────────────────┐
│                    ІЄРАРХІЯ ПРОЦЕСІВ                            │
│                                                                 │
│   PID 0: kernel (scheduler)                                    │
│       └── PID 1: systemd (або init)                            │
│               ├── PID 100: systemd-journald                    │
│               ├── PID 150: sshd                                │
│               │       └── PID 1000: sshd (session)             │
│               │               └── PID 1001: bash               │
│               │                       └── PID 1050: vim        │
│               ├── PID 200: NetworkManager                      │
│               └── PID 300: docker                              │
│                       ├── PID 400: containerd                  │
│                       └── PID 500: container processes         │
│                                                                 │
│   systemd — сучасна система ініціалізації                      │
│   Замінила SysVinit (init.d скрипти)                           │
└─────────────────────────────────────────────────────────────────┘
```

```bash
# Дерево процесів
pstree -p

# Список процесів
ps aux
ps aux | grep nginx

# Інтерактивний моніторинг
htop
top

# systemd: статус сервісу
systemctl status nginx

# Запустити/зупинити
sudo systemctl start nginx
sudo systemctl stop nginx
sudo systemctl restart nginx

# Увімкнути автозапуск
sudo systemctl enable nginx

# Логи сервісу
journalctl -u nginx -f
```

## Пакетний менеджер

```
┌─────────────────────────────────────────────────────────────────┐
│                    ПАКЕТНІ МЕНЕДЖЕРИ                            │
│                                                                 │
│   Debian/Ubuntu:              Red Hat/Fedora:                  │
│   ┌────────────────────┐      ┌────────────────────┐           │
│   │ apt / apt-get      │      │ dnf / yum          │           │
│   │                    │      │                    │           │
│   │ .deb пакети        │      │ .rpm пакети        │           │
│   │ dpkg (низький      │      │ rpm (низький       │           │
│   │ рівень)            │      │ рівень)            │           │
│   └────────────────────┘      └────────────────────┘           │
│                                                                 │
│   Arch:                       Universal:                       │
│   ┌────────────────────┐      ┌────────────────────┐           │
│   │ pacman             │      │ snap, flatpak      │           │
│   └────────────────────┘      └────────────────────┘           │
└─────────────────────────────────────────────────────────────────┘
```

```bash
# Debian/Ubuntu
sudo apt update                    # Оновити список пакетів
sudo apt upgrade                   # Оновити встановлені
sudo apt install nginx             # Встановити
sudo apt remove nginx              # Видалити
apt search nginx                   # Пошук

# Fedora/RHEL
sudo dnf update
sudo dnf install nginx
sudo dnf remove nginx

# Arch
sudo pacman -Syu                   # Оновити все
sudo pacman -S nginx               # Встановити
```

## Чому Linux?

| Перевага | Пояснення |
|----------|-----------|
| **Безкоштовний** | Немає ліцензійних витрат |
| **Open Source** | Можна модифікувати, аудитувати код |
| **Стабільний** | Сервери працюють роками без ребуту |
| **Безпечний** | Швидкі патчі, менше malware |
| **Гнучкий** | Від IoT до суперкомп'ютерів |
| **Ефективний** | Менше ресурсів для тієї ж роботи |
| **Спільнота** | Величезна база знань, підтримка |

## Практичне завдання

```bash
# 1. Інформація про систему
uname -a
cat /etc/os-release
hostnamectl

# 2. Ресурси
free -h
df -h
lscpu

# 3. Процеси
ps aux | wc -l
pstree | head -30

# 4. Мережа
ip addr
ss -tuln

# 5. Логи
journalctl -n 50 --no-pager

# 6. Встановлені пакети (Debian/Ubuntu)
dpkg -l | wc -l
apt list --installed | head -20
```

## 🏢 Real World: Як це використовують у великих компаніях

```
┌─────────────────────────────────────────────────────────────────┐
│           LINUX KERNEL ТА GNU TOOLS У PRODUCTION               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   NETFLIX                                                       │
│   ├── FreeBSD + Linux на CDN серверах                          │
│   ├── Кастомні kernel modules для streaming                    │
│   └── systemd для оркестрації 1000+ мікросервісів              │
│                                                                 │
│   GOOGLE                                                        │
│   ├── Модифіковане ядро Linux для всіх серверів               │
│   ├── Borg/Kubernetes — власний планувальник процесів          │
│   └── GNU coreutils в основі всіх DevOps pipelines             │
│                                                                 │
│   SPOTIFY                                                       │
│   ├── Linux контейнери для всіх сервісів                       │
│   ├── /proc та /sys для моніторингу (Prometheus)               │
│   └── systemd services для 500+ мікросервісів                  │
│                                                                 │
│   META (FACEBOOK)                                               │
│   ├── Власні патчі до Linux kernel                             │
│   ├── Btrfs файлова система (розробка Meta)                    │
│   └── Control groups (cgroups) для ресурсів                    │
│                                                                 │
│   TESLA / SpaceX                                                │
│   ├── Real-time Linux kernel для автопілота                    │
│   ├── Embedded Linux на всіх бортових комп'ютерах              │
│   └── OTA оновлення через Linux package management             │
│                                                                 │
│   AWS / AZURE / GCP                                             │
│   └── 90%+ віртуальних машин клієнтів працюють на Linux        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 💼 Career Spotlight

```
┌─────────────────────────────────────────────────────────────────┐
│        КАР'ЄРА: LINUX SYSTEM ADMINISTRATOR / SRE                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   LINUX SYSTEM ADMINISTRATOR                                    │
│   ├── Рівень: Junior → Senior                                  │
│   ├── Зарплата: $50,000 - $120,000 USD / €45,000 - €100,000    │
│   ├── Навички з цієї лекції:                                   │
│   │   ✓ Розуміння архітектури Linux kernel                    │
│   │   ✓ Робота з systemd та процесами                          │
│   │   ✓ Управління пакетами (apt, dnf)                         │
│   │   ✓ Файлова система та права доступу                       │
│   └── Компанії: будь-яка IT-компанія                           │
│                                                                 │
│   DEVOPS ENGINEER                                               │
│   ├── Рівень: Middle → Senior                                  │
│   ├── Зарплата: $80,000 - $180,000 USD / €70,000 - €150,000    │
│   ├── Навички з цієї лекції:                                   │
│   │   ✓ Глибоке розуміння /proc, /sys                          │
│   │   ✓ Налаштування kernel через sysctl                       │
│   │   ✓ Логування через journalctl                             │
│   └── Компанії: Netflix, Spotify, tech startups                │
│                                                                 │
│   SITE RELIABILITY ENGINEER (SRE)                               │
│   ├── Рівень: Senior → Principal                               │
│   ├── Зарплата: $120,000 - $250,000 USD / €100,000 - €200,000  │
│   ├── Навички з цієї лекції:                                   │
│   │   ✓ Kernel tuning та performance                           │
│   │   ✓ Планувальник процесів (CFS)                            │
│   │   ✓ Memory management, OOM Killer                          │
│   └── Компанії: Google, Meta, Amazon, Microsoft                │
│                                                                 │
│   EMBEDDED LINUX ENGINEER                                       │
│   ├── Рівень: Junior → Senior                                  │
│   ├── Зарплата: $70,000 - $150,000 USD / €60,000 - €130,000    │
│   └── Компанії: Tesla, SpaceX, IoT стартапи                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 🔗 Корисні ресурси

### Онлайн-практика

| Ресурс | Опис | Посилання |
|--------|------|-----------|
| **Linux Journey** | Безкоштовний інтерактивний курс | linuxjourney.com |
| **OverTheWire: Bandit** | Wargame для вивчення Linux CLI | overthewire.org/wargames/bandit |
| **Linux Survival** | Інтерактивний тренажер команд | linuxsurvival.com |
| **Katacoda (O'Reilly)** | Hands-on Linux scenarios | katacoda.com |
| **LPI Learning** | Офіційні матеріали Linux Professional Institute | learning.lpi.org |

## 📋 Cheat Sheet

```
┌─────────────────────────────────────────────────────────────────┐
│                    LINUX OVERVIEW CHEAT SHEET                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ІНФОРМАЦІЯ ПРО СИСТЕМУ                                        │
│   ──────────────────────────────────────────────────────────── │
│   uname -r              # Версія ядра                          │
│   uname -a              # Повна інформація                     │
│   cat /etc/os-release   # Інформація про дистрибутив           │
│   hostnamectl           # Hostname та система                   │
│                                                                 │
│   ЯДРО ТА МОДУЛІ                                                │
│   ──────────────────────────────────────────────────────────── │
│   lsmod                 # Завантажені модулі                   │
│   modinfo <module>      # Інформація про модуль                │
│   dmesg                 # Логи ядра                            │
│   sysctl -a             # Параметри ядра                       │
│                                                                 │
│   ФАЙЛОВА СИСТЕМА                                               │
│   ──────────────────────────────────────────────────────────── │
│   /etc/                 # Конфігурація системи                 │
│   /var/log/             # Логи                                 │
│   /proc/                # Віртуальна ФС процесів               │
│   /sys/                 # Віртуальна ФС ядра/пристроїв         │
│                                                                 │
│   КОРИСТУВАЧІ ТА ПРАВА                                          │
│   ──────────────────────────────────────────────────────────── │
│   whoami / id           # Поточний користувач                  │
│   chmod 755 file        # Змінити права (rwxr-xr-x)            │
│   chown user:group file # Змінити власника                     │
│   r=4, w=2, x=1         # Числове представлення прав           │
│                                                                 │
│   ПРОЦЕСИ ТА SYSTEMD                                            │
│   ──────────────────────────────────────────────────────────── │
│   ps aux                # Список процесів                      │
│   pstree -p             # Дерево процесів                      │
│   systemctl status X    # Статус сервісу                       │
│   journalctl -u X -f    # Логи сервісу в реальному часі        │
│                                                                 │
│   ПАКЕТИ (DEBIAN/UBUNTU)                                        │
│   ──────────────────────────────────────────────────────────── │
│   apt update            # Оновити список пакетів               │
│   apt install X         # Встановити пакет                     │
│   apt remove X          # Видалити пакет                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## ❓ Питання для самоперевірки

1. **Яка різниця між Linux kernel та GNU/Linux?** Що надає GNU проект, а що — ядро Linux?

2. **Що таке systemd і яку роль він відіграє в сучасних Linux-системах?** Яка команда показує статус сервісу?

3. **Поясніть структуру прав доступу в Linux.** Що означає `chmod 755` і яке числове представлення має `rw-r--r--`?

4. **Для чого використовуються віртуальні файлові системи `/proc` та `/sys`?** Наведіть приклади інформації, яку можна отримати з цих каталогів.

5. **Чому Linux домінує на серверах (96%) і суперкомп'ютерах (100%), але має лише ~4% на десктопах?** Назвіть 3 ключові переваги Linux для серверів.

## 🎯 Міні-проект (30 хв)

### Завдання: Створіть власний скрипт System Information Dashboard

Створіть Bash-скрипт, який красиво виводить всю інформацію про вашу Linux-систему. Це ваш перший "справжній" Linux-інструмент!

**Кроки:**

1. Створіть файл скрипта:
```bash
mkdir -p ~/scripts
nano ~/scripts/sysinfo.sh
```

2. Напишіть скрипт з наступним вмістом:
```bash
#!/bin/bash
# System Information Dashboard
# Автор: [Ваше ім'я]
# Дата: $(date +%Y-%m-%d)

clear
echo "╔══════════════════════════════════════════════════════════════╗"
echo "║              SYSTEM INFORMATION DASHBOARD                      ║"
echo "╚══════════════════════════════════════════════════════════════╝"
echo ""
echo "🖥️  СИСТЕМА"
echo "   Hostname:     $(hostname)"
echo "   Дистрибутив:  $(grep PRETTY_NAME /etc/os-release | cut -d'"' -f2)"
echo "   Ядро:         $(uname -r)"
echo "   Uptime:       $(uptime -p)"
echo ""
echo "⚙️  HARDWARE"
echo "   CPU:          $(lscpu | grep 'Model name' | cut -d':' -f2 | xargs)"
echo "   RAM:          $(free -h | awk '/Mem:/ {print $3 " / " $2}')"
echo "   Диск (/):     $(df -h / | awk 'NR==2 {print $3 " / " $2 " (" $5 ")"}')"
echo ""
echo "📊  ПРОЦЕСИ"
echo "   Всього:       $(ps aux | wc -l)"
echo "   Init:         $(ps -p 1 -o comm=)"
echo "   Top 3 CPU:    $(ps aux --sort=-%cpu | awk 'NR<=4 && NR>1 {print $11}' | tr '\n' ' ')"
echo ""
echo "🌐  МЕРЕЖА"
echo "   IP:           $(hostname -I | awk '{print $1}')"
echo "   Gateway:      $(ip route | grep default | awk '{print $3}')"
echo ""
echo "📅  $(date '+%Y-%m-%d %H:%M:%S')"
```

3. Зробіть скрипт виконуваним та запустіть:
```bash
chmod +x ~/scripts/sysinfo.sh
~/scripts/sysinfo.sh
```

4. Додайте alias для швидкого запуску:
```bash
echo 'alias sysinfo="~/scripts/sysinfo.sh"' >> ~/.bashrc
source ~/.bashrc
sysinfo
```

**Очікуваний результат:**
- Готовий скрипт `~/scripts/sysinfo.sh`
- Alias `sysinfo` для швидкого виклику
- Красивий вивід інформації про систему у форматі dashboard

**Бонус (для допитливих):**
- Додайте кольоровий вивід (використовуйте `\e[32m` для зеленого, `\e[0m` для скидання)
- Додайте інформацію про завантажені модулі ядра (`lsmod | wc -l`)
- Зробіть так, щоб скрипт автоматично запускався при кожному відкритті терміналу (додайте виклик у `~/.bashrc`)
- Збережіть вивід у файл: `sysinfo > ~/system-report-$(date +%Y%m%d).txt`

## Підсумок

| Компонент | Опис |
|-----------|------|
| **Kernel** | Ядро Linux — керує hardware |
| **GNU tools** | Базові утиліти (bash, coreutils) |
| **systemd** | Система ініціалізації |
| **Package Manager** | apt, dnf, pacman |
| **Filesystem** | ext4, XFS, Btrfs |

**Ключові файли:**
- `/etc/passwd` — користувачі
- `/etc/fstab` — монтування
- `/var/log/` — логи
- `/proc/`, `/sys/` — інформація про систему

На наступній лекції розглянемо різні дистрибутиви Linux та як обрати потрібний.
