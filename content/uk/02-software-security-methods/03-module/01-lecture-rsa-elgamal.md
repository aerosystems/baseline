---
title: "Криптосистеми RSA та Ель-Гамаля"
type: lecture
order: 1
preview: "Асиметричні алгоритми RSA та Ель-Гамаля, математичні основи."
---

## Революція 1976-1977 років

У 1976 році Whitfield Diffie та Martin Hellman опублікували статтю "New Directions in Cryptography", де запропонували ідею **криптографії з відкритим ключем**.

У 1977 році Ron Rivest, Adi Shamir та Leonard Adleman реалізували цю ідею в алгоритмі **RSA** — першій практичній асиметричній криптосистемі.

```
┌─────────────────────────────────────────────────────────────────────┐
│                              ХРОНОЛОГІЯ                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1970–1974: Елліс, Кокс, Вільямсон (GCHQ) — засекречено до 1997     │
│    ▼                                                                │
│  1976: Діффі–Геллман — ідея асиметричної криптографії               │
│    ▼                                                                │
│  1977: RSA — перша практична реалізація                             │
│    ▼                                                                │
│  1985: Ель-Гамаль — шифрування на дискретному логарифмі             │
│    ▼                                                                │
│  1985: ECC — еліптичні криві (Міллер, Кобліц)                       │
│    ▼                                                                │
│  2000: сплив патент на RSA                                          │
│    ▼                                                                │
│  2020: розкладено RSA-250 (829 біт) — ~2700 років роботи ядра       │
│    ▼                                                                │
│  2024: NIST FIPS 203–204 — постквантові ML-KEM і ML-DSA             │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**Запитання до аудиторії**: як дві людини, що ніколи не зустрічалися, можуть домовитися про секрет, якщо все їхнє листування читає третя?

## Криптосистема RSA

### Математична основа

RSA базується на складності **факторизації** великих чисел:

```
Легко:   p × q = n    (множення)
         17 × 23 = 391

Важко:   n → p, q     (факторизація)
         391 = ? × ?
```

Для малих чисел — тривіально. Для чисел з 600+ цифр (саме стільки має 2048-бітний модуль — 617 десяткових цифр) — практично неможливо. Рекорд публічної факторизації — RSA-250, 829 біт: 2020 року на це пішло близько 2700 років роботи одного процесорного ядра.

### Генерація ключів RSA

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ГЕНЕРАЦІЯ КЛЮЧІВ RSA                             │
│                                                                     │
│  Крок 1: Обираємо два великих простих числа p та q                  │
│          (типово 1024+ біт кожне)                                   │
│                                                                     │
│  Крок 2: Обчислюємо n = p × q                                       │
│          (модуль, частина публічного ключа)                         │
│                                                                     │
│  Крок 3: Обчислюємо φ(n) = (p-1)(q-1)                               │
│          (функція Ейлера)                                           │
│                                                                     │
│  Крок 4: Обираємо e таке, що:                                       │
│          • 1 < e < φ(n)                                             │
│          • gcd(e, φ(n)) = 1                                         │
│          (типово e = 65537 = 2¹⁶ + 1)                               │
│                                                                     │
│  Крок 5: Обчислюємо d = e⁻¹ mod φ(n)                                │
│          (d × e ≡ 1 (mod φ(n)))                                     │
│                                                                     │
│  Результат:                                                         │
│  • Публічний ключ:  (n, e)                                          │
│  • Приватний ключ:  (n, d)   [або (p, q, d)]                        │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### Приклад з малими числами

```
Крок 1: p = 61, q = 53

Крок 2: n = 61 × 53 = 3233

Крок 3: φ(n) = (61-1)(53-1) = 60 × 52 = 3120

Крок 4: Обираємо e = 17 (gcd(17, 3120) = 1) ✓

Крок 5: Знаходимо d: 17 × d ≡ 1 (mod 3120)
        d = 2753 (бо 17 × 2753 = 46801 = 15 × 3120 + 1)

Публічний ключ:  (3233, 17)
Приватний ключ:  (3233, 2753)
```

### Шифрування та дешифрування

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ШИФРУВАННЯ RSA                                   │
│                                                                     │
│  Шифрування (публічним ключем):                                     │
│                                                                     │
│      C = Mᵉ mod n                                                   │
│                                                                     │
│  Дешифрування (приватним ключем):                                   │
│                                                                     │
│      M = Cᵈ mod n                                                   │
│                                                                     │
│  Чому це працює?                                                    │
│                                                                     │
│      Cᵈ = (Mᵉ)ᵈ = Mᵉᵈ mod n                                         │
│                                                                     │
│      e×d = 1 + t×φ(n), тому Mᵉᵈ = M × (M^φ(n))ᵗ                     │
│      За теоремою Ейлера M^φ(n) ≡ 1 (mod n), отже Mᵉᵈ ≡ M (mod n)    │
│      (для M, не взаємно простих з n, це доводять через КТЛ)         │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**Приклад:**

```
Повідомлення M = 123
Публічний ключ: (n=3233, e=17)
Приватний ключ: (n=3233, d=2753)

Шифрування:
C = 123¹⁷ mod 3233 = 855

Дешифрування:
M = 855²⁷⁵³ mod 3233 = 123 ✓
```

### Алгоритм швидкого піднесення до степеня

Для великих показників використовують **бінарне піднесення** (square-and-multiply):

```cpp
#include <cstdint>

using u64 = unsigned long long;

// Швидке піднесення до степеня за модулем: base^exp mod modulus
// Метод "піднести до квадрата й помножити" — O(log exp) множень
u64 fastPower(u64 base, u64 exp, u64 modulus) {
    u64 result = 1;
    base %= modulus;

    while (exp > 0) {
        if (exp & 1ULL) {                  // непарний показник — множимо на base
            result = (result * base) % modulus;
        }
        exp >>= 1;                         // exp = exp / 2
        base = (base * base) % modulus;    // base = base^2
    }
    return result;
}

// Приклад: 123^17 mod 3233
// 17 = 10001 у двійковій системі
// 123^17 = 123^16 * 123^1 = ((123^8)^2) * 123
//
// Обмеження: добуток двох u64 переповнюється, якщо modulus > 2^32.
// Для навчальних ключів беріть p, q < 2^16 (тоді n < 2^32) або
// множте через unsigned __int128 (GCC, Clang)
```

### Реалізація RSA мовою C++

```cpp
#include <iostream>
#include <random>
#include <stdexcept>

using u64 = unsigned long long;

u64 fastPower(u64 base, u64 exp, u64 modulus);   // з попереднього прикладу

// Тест простоти Міллера-Рабіна: ймовірнісний, але за 10 раундів
// похибка менша за 4^-10
bool isPrime(u64 n, int rounds = 10) {
    if (n < 2) return false;
    if (n == 2 || n == 3) return true;
    if (n % 2 == 0) return false;

    u64 d = n - 1;
    int r = 0;
    while (d % 2 == 0) { d /= 2; ++r; }    // n - 1 = 2^r * d

    std::random_device rd;
    std::mt19937_64 gen(rd());
    std::uniform_int_distribution<u64> dist(2, n - 2);

    for (int i = 0; i < rounds; ++i) {
        u64 x = fastPower(dist(gen), d, n);
        if (x == 1 || x == n - 1) continue;

        bool composite = true;
        for (int j = 0; j < r - 1; ++j) {
            x = fastPower(x, 2, n);
            if (x == n - 1) { composite = false; break; }
        }
        if (composite) return false;
    }
    return true;
}

// Випадкове просте число заданої розрядності
u64 generatePrime(int bits) {
    std::random_device rd;
    std::mt19937_64 gen(rd());
    std::uniform_int_distribution<u64> dist(1ULL << (bits - 1), (1ULL << bits) - 1);

    while (true) {
        u64 candidate = dist(gen) | 1ULL;   // число має бути непарним
        if (isPrime(candidate)) return candidate;
    }
}

// Обернений елемент e^-1 mod phi, розширений алгоритм Евкліда
u64 modInverse(u64 e, u64 phi) {
    long long oldR = static_cast<long long>(e), r = static_cast<long long>(phi);
    long long oldS = 1, s = 0;

    while (r != 0) {
        long long q = oldR / r;
        long long tmp = oldR - q * r;  oldR = r;  r = tmp;
        tmp = oldS - q * s;            oldS = s;  s = tmp;
    }

    if (oldR != 1) throw std::runtime_error("Оберненого елемента не існує");

    long long mod = static_cast<long long>(phi);
    return static_cast<u64>(((oldS % mod) + mod) % mod);
}

class RSA {
public:
    RSA(u64 p, u64 q) {
        n_ = p * q;
        u64 phi = (p - 1) * (q - 1);

        e_ = 65537;
        if (e_ >= phi) e_ = 17;                        // для малих навчальних ключів
        while (gcd(e_, phi) != 1) e_ += 2;

        d_ = modInverse(e_, phi);
    }

    u64 encrypt(u64 message) const { return fastPower(message, e_, n_); }
    u64 decrypt(u64 cipher) const  { return fastPower(cipher, d_, n_); }

    u64 modulus() const { return n_; }
    u64 e() const { return e_; }
    u64 d() const { return d_; }

private:
    static u64 gcd(u64 a, u64 b) {
        while (b != 0) { u64 t = a % b; a = b; b = t; }
        return a;
    }

    u64 n_ = 0, e_ = 0, d_ = 0;
};

int main() {
    RSA rsa(61, 53);

    std::cout << "n = " << rsa.modulus() << ", e = " << rsa.e() << ", d = " << rsa.d() << "\n";

    u64 message = 65;
    u64 cipher = rsa.encrypt(message);
    std::cout << "M = " << message << " → C = " << cipher
              << " → M' = " << rsa.decrypt(cipher) << "\n";
    return 0;
}
```

### Безпека RSA

**Рекомендовані розміри ключів:**

| Рік | Мінімум | Рекомендовано |
|-----|---------|---------------|
| до 2030 | 2048 біт | 3072 біт |
| після 2030 | 3072 біт | 3072–4096 біт |

За планом NIST (IR 8547, 2024) RSA-2048 оголосять застарілим 2030 року, а 2035-го RSA і ECC буде заборонено взагалі — через загрозу квантових комп'ютерів (алгоритм Шора розкладає n за поліноміальний час). Їм на заміну вже стандартизовано ML-KEM і ML-DSA.

**Атаки на RSA:**

1. **Факторизація n** — найпряміша атака
2. **Близькі p і q** — тоді n ≈ p², і метод Ферма (пошук n = a² − b²) знаходить множники за лічені кроки
3. **Малий публічний показник** — якщо e мале і M мале, C = Mᵉ може не "обернутися" за модулем
4. **Спільний модуль** — не використовуйте один n для різних користувачів
5. **Timing attacks** — час розшифрування видає інформацію про d
6. **«Шкільний» RSA без доповнення** — детермінований (однакові M дають однакові C) і мультиплікативний: E(M₁)·E(M₂) = E(M₁·M₂). Тому на практиці M перед шифруванням доповнюють випадковими байтами за схемою OAEP

### Реальні провали RSA

Математика RSA не зламана, ламаються реалізації:

- **2008, Debian.** Через помилку в пакеті OpenSSL генератор випадкових чисел мав лише 32 768 можливих станів. Два роки всі SSH- і TLS-ключі, згенеровані в Debian і Ubuntu, можна було просто перебрати.
- **2012, «Ron was wrong, Whit is right».** Дослідники зібрали мільйони відкритих ключів з інтернету й порахували НСД для пар модулів. Близько 0,2 % ключів мали спільний простий множник з іншим ключем — і розкладалися миттєво. Причина: пристрої генерували ключі одразу після ввімкнення, коли випадковості ще не було.
- **2017, ROCA.** Бібліотека Infineon генерувала прості числа особливого вигляду, і модулі розкладалися в тисячі разів швидше. Естонія заблокувала сертифікати 750 тисяч ID-карток.
- **1998 і 2017, Bleichenbacher і ROBOT.** Сервер, що по-різному відповідав на правильне й неправильне доповнення PKCS#1 v1.5, дозволяв розшифрувати сеанс TLS — той самий padding oracle, що й у блочних шифрах.

## Криптосистема Ель-Гамаля

### Математична основа

Ель-Гамаль (Taher Elgamal, 1985) базується на складності **дискретного логарифму**:

```
Легко:   y = gˣ mod p    (піднесення до степеня)

Важко:   x = log_g(y) mod p    (дискретний логарифм)
         Знайти x, знаючи y, g, p
```

### Генерація ключів

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ГЕНЕРАЦІЯ КЛЮЧІВ ЕЛЬ-ГАМАЛЯ                      │
│                                                                     │
│  Крок 1: Обираємо велике просте p та генератор g групи Z*_p         │
│          (g — примітивний корінь за модулем p)                      │
│                                                                     │
│  Крок 2: Обираємо випадкове x, де 1 < x < p-1                       │
│          (приватний ключ)                                           │
│                                                                     │
│  Крок 3: Обчислюємо y = gˣ mod p                                    │
│          (публічний ключ)                                           │
│                                                                     │
│  Результат:                                                         │
│  • Публічний ключ:  (p, g, y)                                       │
│  • Приватний ключ:  x                                               │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### Шифрування

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ШИФРУВАННЯ ЕЛЬ-ГАМАЛЯ                            │
│                                                                     │
│  Вхід: повідомлення M (число < p)                                   │
│                                                                     │
│  Крок 1: Обираємо випадкове k, де 1 < k < p-1                       │
│          (сесійний ключ, новий для кожного повідомлення!)           │
│                                                                     │
│  Крок 2: Обчислюємо:                                                │
│          a = gᵏ mod p                                               │
│          b = M × yᵏ mod p                                           │
│                                                                     │
│  Шифротекст: (a, b)                                                 │
│                                                                     │
│  Зверніть увагу: шифротекст удвічі більший за відкритий текст!      │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### Дешифрування

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ДЕШИФРУВАННЯ ЕЛЬ-ГАМАЛЯ                          │
│                                                                     │
│  Вхід: шифротекст (a, b)                                            │
│                                                                     │
│  Обчислюємо:                                                        │
│          M = b × a⁻ˣ mod p                                          │
│            = b × (aˣ)⁻¹ mod p                                       │
│                                                                     │
│  Чому це працює?                                                    │
│                                                                     │
│          b × a⁻ˣ = (M × yᵏ) × (gᵏ)⁻ˣ                                │
│                  = M × (gˣ)ᵏ × g⁻ᵏˣ                                 │
│                  = M × gᵏˣ × g⁻ᵏˣ                                   │
│                  = M                                                │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### Приклад

```
Параметри: p = 23, g = 5

Генерація ключів:
  x = 6 (приватний)
  y = 5⁶ mod 23 = 15625 mod 23 = 8 (публічний)

Публічний ключ: (23, 5, 8)
Приватний ключ: 6

Шифрування M = 7:
  k = 3 (випадковий)
  a = 5³ mod 23 = 125 mod 23 = 10
  yᵏ = 8³ mod 23 = 512 mod 23 = 6   (512 = 22×23 + 6)
  b = 7 × 6 mod 23 = 42 mod 23 = 19

Шифротекст: (10, 19)

Дешифрування:
  aˣ = 10⁶ mod 23:  10² = 100 ≡ 8,  10³ ≡ 8 × 10 = 80 ≡ 11,
                    10⁶ ≡ 11² = 121 ≡ 6
  (зверніть увагу: aˣ = gᵏˣ = yᵏ = 6 — те саме число, що при шифруванні)
  (aˣ)⁻¹ mod 23: 6 × 4 = 24 = 23 + 1, отже 4

  M = 19 × 4 mod 23 = 76 mod 23 = 7 ✓
```

### Реалізація Ель-Гамаля

```cpp
#include <iostream>
#include <random>
#include <utility>

using u64 = unsigned long long;

u64 fastPower(u64 base, u64 exp, u64 modulus);   // див. вище

// Криптосистема Ель-Гамаля будується на складності дискретного логарифма
class ElGamal {
public:
    ElGamal(u64 p, u64 g) : p_(p), g_(g) {
        std::random_device rd;
        std::mt19937_64 gen(rd());
        std::uniform_int_distribution<u64> dist(2, p_ - 2);

        x_ = dist(gen);                  // закритий ключ
        y_ = fastPower(g_, x_, p_);      // відкритий ключ y = g^x mod p
    }

    // Зашифрування дає пару (a, b); для кожного повідомлення
    // береться новий випадковий сеансовий ключ k
    std::pair<u64, u64> encrypt(u64 message) const {
        std::random_device rd;
        std::mt19937_64 gen(rd());
        std::uniform_int_distribution<u64> dist(2, p_ - 2);

        u64 k = dist(gen);
        u64 a = fastPower(g_, k, p_);
        u64 b = (message % p_) * fastPower(y_, k, p_) % p_;
        return { a, b };
    }

    // Розшифрування: m = b * (a^x)^-1 mod p,
    // обернений елемент знаходимо за малою теоремою Ферма
    u64 decrypt(u64 a, u64 b) const {
        u64 s = fastPower(a, x_, p_);
        u64 sInv = fastPower(s, p_ - 2, p_);
        return (b * sInv) % p_;
    }

    void printPublicKey() const {
        std::cout << "Відкритий ключ: (p=" << p_ << ", g=" << g_ << ", y=" << y_ << ")\n";
    }

private:
    u64 p_, g_, x_, y_;
};

int main() {
    ElGamal elgamal(467, 2);            // малі числа для демонстрації
    elgamal.printPublicKey();

    u64 message = 123;
    std::pair<u64, u64> cipher = elgamal.encrypt(message);

    std::cout << "Шифротекст:   (" << cipher.first << ", " << cipher.second << ")\n";
    std::cout << "Розшифровано: " << elgamal.decrypt(cipher.first, cipher.second) << "\n";
    return 0;
}
```

### Чому k має бути щоразу новим

Якщо два повідомлення зашифровано одним k, обидва шифротексти мають однакове a = gᵏ, а b₁ = M₁·yᵏ і b₂ = M₂·yᵏ. Поділивши одне на інше, зловмисник отримує b₁/b₂ = M₁/M₂ — і, знаючи одне повідомлення (наприклад, стандартний заголовок), обчислює друге без приватного ключа. У схемах підпису на основі Ель-Гамаля (DSA, ECDSA) повтор k ще гірший: він видає сам приватний ключ. Саме так 2010 року зламали захист консолі PlayStation 3 — Sony підписувала прошивки з тим самим k.

## Порівняння RSA та Ель-Гамаля

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ПОРІВНЯННЯ                                       │
├──────────────────────┬──────────────────┬───────────────────────────┤
│                      │      RSA         │      Ель-Гамаль           │
├──────────────────────┼──────────────────┼───────────────────────────┤
│ Математична основа   │ Факторизація     │ Дискретний логарифм       │
├──────────────────────┼──────────────────┼───────────────────────────┤
│ Розмір шифротексту   │ = відкр. тексту  │ 2× відкритого тексту      │
├──────────────────────┼──────────────────┼───────────────────────────┤
│ Детермінований       │ Так*             │ Ні (випадкове k)          │
├──────────────────────┼──────────────────┼───────────────────────────┤
│ Швидкість            │ Швидше           │ Повільніше                │
├──────────────────────┼──────────────────┼───────────────────────────┤
│ Патенти              │ Був (до 2000)    │ Без патенту               │
├──────────────────────┼──────────────────┼───────────────────────────┤
│ Стандарти            │ PKCS#1, X.509    │ DSA (підпис)              │
└──────────────────────┴──────────────────┴───────────────────────────┘

* Чистий RSA детермінований; на практиці використовують padding (OAEP)
```

## Практичні завдання

### Завдання 1

Згенеруйте ключі RSA для p=11, q=13, e=7. Обчисліть d.

### Завдання 2

Зашифруйте та розшифруйте M=9 з ключами із завдання 1.

### Завдання 3

Чому небезпечно повторно використовувати k в Ель-Гамаля?

<details>
<summary>Відповіді до завдань 1–2</summary>

n = 143, φ(n) = 120, d = 103 (7 × 103 = 721 = 6 × 120 + 1).
C = 9⁷ mod 143 = 48; перевірка: 48¹⁰³ mod 143 = 9.
</details>

## 🏢 Real World: Як це використовують у великих компаніях

```
┌─────────────────────────────────────────────────────────────────────┐
│                           RSA У PRODUCTION                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  TLS/HTTPS                                                          │
│  ├── Сертифікати сайтів підписують RSA-2048 або ECDSA P-256;        │
│  │   частка ECDSA швидко зростає, бо ключ і підпис коротші          │
│  ├── Let's Encrypt видав мільярди безкоштовних сертифікатів,        │
│  │   кожен живе лише 90 днів; CA/B Forum 2025 року ухвалив          │
│  │   поступово скоротити строк для всіх до 47 днів (2029)           │
│  └── У TLS 1.3 RSA лише підписує; ключ сеансу — ECDHE               │
│                                                                     │
│  УКРАЇНА: КЕП І «ДІЯ»                                               │
│  ├── Кваліфікований електронний підпис історично будувався          │
│  │   на національному стандарті ДСТУ 4145-2002 (еліптичні криві)    │
│  └── Нині дозволено й міжнародні RSA та ECDSA — для сумісності      │
│      з європейськими системами за регламентом eIDAS                 │
│                                                                     │
│  ХМАРИ Й HSM                                                        │
│  ├── AWS KMS, Azure Key Vault, Google Cloud KMS: ключі RSA          │
│  │   генеруються й живуть в апаратних модулях FIPS 140-3            │
│  └── Приватний ключ ніколи не залишає HSM — назовні видно           │
│      лише результат підпису чи розшифрування                        │
│                                                                     │
│  ПОСТКВАНТОВИЙ ПЕРЕХІД                                              │
│  └── Chrome і Cloudflare з 2024 року за замовчуванням               │
│      поєднують X25519 із ML-KEM у рукостисканні TLS                 │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### Як влаштовано підпис PDF-договору

Сервіси електронного підпису (DocuSign, «Дія.Підпис», Adobe Sign) працюють за однією схемою стандарту PAdES:

```
┌─────────────────────────────────────────────────────────────────────┐
│                          ПІДПИС PDF (PAdES)                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   1. Документ ──► SHA-256 ──► хеш (32 байти)                        │
│                                                                     │
│   2. Хеш ──► HSM або смарт-картка ──► підпис приватним ключем       │
│      (сам документ у HSM не потрапляє, лише хеш)                    │
│                                                                     │
│   3. У PDF вбудовують: підпис + сертифікат підписувача              │
│      + позначку часу від TSA (коли саме підписано)                  │
│                                                                     │
│   4. Перевірка: хеш документа ↔ підпис, перевірений відкритим       │
│      ключем із сертифіката, + ланцюжок сертифікатів до кореня       │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

## 💼 Career Spotlight

```
┌─────────────────────────────────────────────────────────────────────┐
│                         КАР'ЄРНІ МОЖЛИВОСТІ                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  CRYPTOGRAPHY ENGINEER                                              │
│  RSA, ECC, постквантові алгоритми; OpenSSL, BoringSSL, libsodium    │
│  Зарплата: $140K-$200K (USA) | €80K-€130K (EU)                      │
│  Компанії: Google, Apple, Signal, Cloudflare                        │
│                                                                     │
│  PKI ENGINEER                                                       │
│  X.509, центри сертифікації, EJBCA, Microsoft AD CS, Vault          │
│  Зарплата: $120K-$170K (USA) | €70K-€110K (EU)                      │
│  Компанії: DigiCert, Sectigo, банки, КНЕДП, уряди                   │
│                                                                     │
│  HSM SPECIALIST                                                     │
│  PKCS#11, CNG, сертифікація FIPS 140-3                              │
│  Зарплата: $130K-$180K (USA) | €70K-€110K (EU)                      │
│  Компанії: Thales, Utimaco, Entrust, платіжні системи               │
│                                                                     │
│  SECURITY ARCHITECT                                                 │
│  Проєктування захисту систем, 10+ років досвіду, CISSP              │
│  Зарплата: $150K-$220K (USA) | €90K-€150K (EU)                      │
│  Компанії: Big Tech, консалтинг, фінтех                             │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

## 🔗 Корисні ресурси

### Онлайн-платформи для практики

| Платформа | Опис | Рівень |
|-----------|------|--------|
| **CryptoHack.org** | Інтерактивні задачі з криптографії | Початковий → Експерт |
| **Cryptopals.com** | 8 сетів криптографічних челенджів | Середній → Експерт |
| **HackTheBox** | CTF платформа з крипто-челенджами | Середній |
| **PicoCTF** | Освітні CTF від Carnegie Mellon | Початковий |

### Офіційні ресурси

- **RFC 8017** — PKCS#1 RSA Cryptography Specifications
- **NIST SP 800-57** — рекомендації щодо довжини ключів
- **OpenSSL Wiki** — документація та приклади

---

## 📋 Cheat Sheet

### Ключові алгоритми RSA

```
┌─────────────────────────────────────────────────────────────────────┐
│                    RSA CHEAT SHEET                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ГЕНЕРАЦІЯ КЛЮЧІВ:                                                  │
│  1. p, q — великі прості числа (1024+ біт кожне)                    │
│  2. n = p × q                                                       │
│  3. φ(n) = (p-1)(q-1)                                               │
│  4. e = 65537 (стандартна публічна експонента)                      │
│  5. d = e⁻¹ mod φ(n)                                                │
│                                                                     │
│  ШИФРУВАННЯ:   C = Mᵉ mod n                                         │
│  ДЕШИФРУВАННЯ: M = Cᵈ mod n                                         │
│  ПІДПИС:       S = H(M)ᵈ mod n                                      │
│  ПЕРЕВІРКА:    H(M) == Sᵉ mod n                                     │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### OpenSSL команди

```bash
# Генерація RSA ключа
openssl genrsa -out private.pem 2048

# Витяг публічного ключа
openssl rsa -in private.pem -pubout -out public.pem

# Перегляд параметрів ключа
openssl rsa -in private.pem -text -noout

# Шифрування файлу публічним ключем (OAEP; rsautl у OpenSSL 3 застарів)
openssl pkeyutl -encrypt -pubin -inkey public.pem -pkeyopt rsa_padding_mode:oaep \
    -in plaintext.txt -out encrypted.bin

# Дешифрування приватним ключем
openssl pkeyutl -decrypt -inkey private.pem -pkeyopt rsa_padding_mode:oaep \
    -in encrypted.bin -out decrypted.txt

# Підпис файлу
openssl dgst -sha256 -sign private.pem -out signature.bin document.txt

# Перевірка підпису
openssl dgst -sha256 -verify public.pem -signature signature.bin document.txt

# Генерація RSA-4096 для продакшену
openssl genrsa -out private_4096.pem 4096
```

### Готові бібліотеки: OpenSSL

```cpp
// У промислових застосунках власну реалізацію RSA не пишуть:
// беруть перевірену бібліотеку. Для C++ це, наприклад, OpenSSL.
// Нижче — шифрування з доповненням OAEP (RSA без доповнення небезпечний).
#include <openssl/evp.h>
#include <openssl/rsa.h>
#include <vector>
#include <string>

std::vector<unsigned char> rsaEncrypt(EVP_PKEY* publicKey, const std::string& message) {
    EVP_PKEY_CTX* ctx = EVP_PKEY_CTX_new(publicKey, nullptr);
    EVP_PKEY_encrypt_init(ctx);

    // Доповнення OAEP з хешуванням SHA-256
    EVP_PKEY_CTX_set_rsa_padding(ctx, RSA_PKCS1_OAEP_PADDING);
    EVP_PKEY_CTX_set_rsa_oaep_md(ctx, EVP_sha256());

    size_t outLen = 0;
    EVP_PKEY_encrypt(ctx, nullptr, &outLen,
                     reinterpret_cast<const unsigned char*>(message.data()), message.size());

    std::vector<unsigned char> ciphertext(outLen);
    EVP_PKEY_encrypt(ctx, ciphertext.data(), &outLen,
                     reinterpret_cast<const unsigned char*>(message.data()), message.size());
    ciphertext.resize(outLen);

    EVP_PKEY_CTX_free(ctx);
    return ciphertext;
}

// Ключ генерують окремо: EVP_RSA_gen(2048) або утилітою
// openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:2048
```

---

## 🎯 Міні-проект (30 хв)

### Завдання: Станьте криптографом — згенеруйте RSA ключі та підпишіть документ

Створіть власну пару RSA ключів та підпишіть файл — саме так працює електронний підпис у реальному світі!

**Кроки:**

1. Згенеруйте RSA ключі через OpenSSL:
```bash
# Створіть папку для ключів
mkdir -p ~/crypto-lab && cd ~/crypto-lab

# Генерація приватного ключа RSA-2048
openssl genrsa -out my_private.pem 2048

# Витягніть публічний ключ
openssl rsa -in my_private.pem -pubout -out my_public.pem

# Перегляньте параметри ключа
openssl rsa -in my_private.pem -text -noout | head -20
```

2. Підпишіть файл:
```bash
# Створіть документ
echo "Я, $(whoami), підтверджую цей документ. Дата: $(date)" > document.txt

# Підпишіть його
openssl dgst -sha256 -sign my_private.pem -out document.sig document.txt

# Перевірте підпис
openssl dgst -sha256 -verify my_public.pem -signature document.sig document.txt
```

3. Спробуйте зламати:
```bash
# Змініть документ
echo "HACKED" >> document.txt

# Перевірте підпис знову — має бути помилка!
openssl dgst -sha256 -verify my_public.pem -signature document.sig document.txt
```

**Очікуваний результат:**
- Пара ключів: `my_private.pem` та `my_public.pem`
- Підписаний документ з валідним підписом
- Розуміння, що змінений документ не проходить верифікацію

**Бонус (для допитливих):**
- Зашифруйте файл публічним ключем: `openssl pkeyutl -encrypt -pubin -inkey my_public.pem -pkeyopt rsa_padding_mode:oaep -in secret.txt -out secret.enc`. Спробуйте файл, довший за 214 байтів (межа OAEP із SHA-1 для RSA-2048), і поясніть помилку.
- Порівняйте час генерації RSA-2048 vs RSA-4096
- Спробуйте ECDSA: `openssl ecparam -genkey -name prime256v1 -out ecdsa.pem`

---

## 🔧 Далі — лабораторна робота

Власну реалізацію RSA з генерацією ключів, шифруванням, підписом і атаками на малий показник ви виконаєте в лабораторній роботі «Реалізація алгоритму RSA».

---

## Тест для самоперевірки

Десять запитань, у кожному одна правильна відповідь. Якщо тема винесена вашій групі на самостійне опрацювання, цей самий тест публікується в Google Classroom — 10 балів, по одному за кожне запитання.

**1.** На складності якої задачі тримається стійкість RSA?

- факторизації великого числа на два прості множники
- обчислення дискретного логарифма
- пошуку колізій хеш-функції
- перевірки числа на простоту

**2.** Як обчислюється функція Ейлера φ(n) під час генерації ключів RSA?

- (p − 1)(q − 1)
- p × q
- (p + 1)(q + 1)
- p × q − 1

**3.** Яку умову має задовольняти публічна експонента e?

- gcd(e, φ(n)) = 1
- e має ділити φ(n) без остачі
- e має бути взаємно простим з n
- e має бути більшим за n

**4.** Що є публічним ключем RSA?

- пара (n, e)
- пара (n, d)
- пара простих чисел (p, q)
- саме лише значення φ(n)

**5.** За якою формулою шифрують повідомлення в RSA?

- C = Mᵉ mod n
- C = Mᵈ mod n
- C = M × e mod n
- C = eᴹ mod n

**6.** Чому стандартним вибором для e є 65537?

- у двійковому записі має лише дві одиниці, тож піднесення до степеня швидке
- це найбільше просте число, менше за 2¹⁶
- воно взаємно просте з будь-яким φ(n)
- цього вимагає формат сертифіката X.509

**7.** Чим небезпечно взяти p і q близькими одне до одного?

- n ≈ p², і атака Ферма швидко знаходить множники
- φ(n) стає непарним, і d не існує
- шифротекст стає коротшим за повідомлення
- дешифрування перестає бути однозначним

**8.** На складності якої задачі тримається криптосистема Ель-Гамаля?

- дискретного логарифма
- факторизації великих чисел
- пошуку прообразу хешу
- задачі про рюкзак

**9.** Чому шифротекст Ель-Гамаля вдвічі довший за повідомлення?

- це пара (a, b), і кожна частина завдовжки з модуль p
- повідомлення шифрується двома різними ключами
- до шифротексту додається цифровий підпис
- сесійний ключ k передається разом із текстом

**10.** Що отримає зловмисник, якщо одне й те саме k використати для двох повідомлень?

- відношення відкритих текстів M₁/M₂ = b₁/b₂
- приватний ключ x у явному вигляді
- нічого: k можна повторювати
- два однакових шифротексти, які не розшифрувати

## Підсумок

| Термін | Визначення |
|--------|------------|
| **RSA** | Криптосистема на факторизації |
| **Ель-Гамаль** | Криптосистема на дискретному логарифмі |
| **φ(n)** | Функція Ейлера: кількість чисел < n, взаємно простих з n |
| **e, d** | Публічна та приватна експоненти RSA |
| **Сесійний ключ k** | Випадкове число в Ель-Гамаля, нове для кожного повідомлення |
| **OAEP** | Випадкове доповнення перед RSA-шифруванням; без нього RSA детермінований і податливий |

**Ключові формули:**
- RSA шифрування: `C = Mᵉ mod n`
- RSA дешифрування: `M = Cᵈ mod n`
- Ель-Гамаль: `(a, b) = (gᵏ, M × yᵏ)`

На наступній лекції розглянемо електронний цифровий підпис та хеш-функції.
