---
title: "Реалізація алгоритму хешування SHA. Блок-схема алгоритму"
shortTitle: "Реалізація алгоритму SHA"
type: lab
order: 4
labNumber: 7
subject: pmzi
duration: "2 академічні години"
equipment:
  - "ПК з компілятором або інтерпретатором мови програмування за вибором студента"
  - "Середовище розробки (Visual Studio, VS Code або інше)"
  - "Калькулятор для шістнадцяткових обчислень"
preview: "Програмна реалізація хеш-функції SHA-256."
---

**Мета:** вивчити принципи роботи криптографічних хеш-функцій на прикладі SHA-256. Реалізувати алгоритм програмно та дослідити його властивості.

**Обладнання:** ПК з компілятором або інтерпретатором мови програмування за вибором студента; Середовище розробки (Visual Studio, VS Code або інше); Калькулятор для шістнадцяткових обчислень.

**Тривалість:** 4 академічні години.

## Передумови

| Вимога | Опис |
|--------|------|
| **Знання** | Лекція 3: Електронний цифровий підпис. Хеш-функції |
| **Навички** | Побітові операції (XOR, AND, зсуви), робота з big-endian |
| **Середовище** | ПК з компілятором або інтерпретатором мови програмування за вибором студента |

## Теоретичні відомості

### 1 Призначення криптографічних хеш-функцій

Хеш-функція — це функція, яка перетворює вхідне повідомлення довільної довжини на вихідний рядок фіксованої довжини (дайджест). Криптографічна хеш-функція повинна мати такі властивості:

- **Детермінованість**: однакове повідомлення завжди дає однаковий хеш
- **Швидкість**: обчислення хешу має бути ефективним
- **Стійкість до прообразу**: за хешем неможливо відновити повідомлення
- **Стійкість до колізій**: важко знайти два різних повідомлення з однаковим хешем
- **Лавинний ефект**: зміна одного біта повідомлення кардинально змінює хеш

### 2 Структура SHA-256

SHA-256 (Secure Hash Algorithm 256-bit) належить до сімейства SHA-2, розробленого NSA у 2001 році.

```
┌───────────────────────────────────────────────────────────────────────┐
│                      АЛГОРИТМ SHA-256                                 │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  Повідомлення ──► [PADDING] ──► [РОЗБИТТЯ] ──► [ОБРОБКА] ──► Хеш      │
│                                  512 біт      64 раунди    256 біт    │
│                                                                       │
│  ЕТАПИ:                                                               │
│  1. Доповнення повідомлення до кратного 512 біт                       │
│  2. Розбиття на блоки по 512 біт                                      │
│  3. Ініціалізація 8 робочих змінних (H0-H7)                           │
│  4. Для кожного блоку:                                                │
│     - Розширення 16 слів до 64 слів                                   │
│     - 64 раунди функції стиснення                                     │
│     - Додавання результату до H0-H7                                   │
│  5. Конкатенація H0-H7 → 256-бітний хеш                               │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```
### 3 Доповнення повідомлення (Padding)

Повідомлення доповнюється за схемою:

1. Додати біт `1` після повідомлення
2. Додати нулі, щоб довжина стала на 64 біти менше кратної 512
3. Додати 64-бітне представлення довжини початкового повідомлення

```
Оригінал:     M (L біт)
Доповнене:    M || 1 || 0...0 || L(64 біти)
              ↑    ↑    ↑         ↑
              |    |    |         Довжина у бітах (big-endian)
              |    |    Нулі для вирівнювання
              |    Обов'язковий біт "1"
              Повідомлення
```
**Приклад для "abc":**

- Довжина: 24 біти (3 байти)
- Потрібно: 512 - 64 - 24 - 1 = 423 нульових біти
- Результат: `61626380 00000000 ... 00000018` (у hex)

### 4 Функції раунду

SHA-256 використовує шість логічних функцій:

```
// Функції вибору та більшості
#define Ch(x,y,z)  ((x & y) ^ (~x & z))
#define Maj(x,y,z) ((x & y) ^ (x & z) ^ (y & z))

// Функції обертання (Σ - sigma)
#define ROTR(x,n)  ((x >> n) | (x << (32-n)))

#define Σ0(x)  (ROTR(x,2) ^ ROTR(x,13) ^ ROTR(x,22))
#define Σ1(x)  (ROTR(x,6) ^ ROTR(x,11) ^ ROTR(x,25))
#define σ0(x)  (ROTR(x,7) ^ ROTR(x,18) ^ (x >> 3))
#define σ1(x)  (ROTR(x,17) ^ ROTR(x,19) ^ (x >> 10))
```
### 5 Константи алгоритму

**Початкові значення H0-H7** (перші 32 біти дробових частин квадратних коренів перших 8 простих чисел):

```cpp
uint32_t H[8] = {
    0x6a09e667, 0xbb67ae85, 0x3c6ef372, 0xa54ff53a,
    0x510e527f, 0x9b05688c, 0x1f83d9ab, 0x5be0cd19
};
```
**Раундові константи K** (перші 32 біти дробових частин кубічних коренів перших 64 простих чисел):

```cpp
uint32_t K[64] = {
    0x428a2f98, 0x71374491, 0xb5c0fbcf, 0xe9b5dba5,
    0x3956c25b, 0x59f111f1, 0x923f82a4, 0xab1c5ed5,
    // ... (всього 64 значення)
};
```
### 6 Цикл стиснення (один раунд)

```
┌─────────────────────────────────────────────────────────────┐
│                   РАУНД SHA-256                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  a ─────────────────────────────────────────────────► a'    │
│  b ─────────────────────────────────────────────────► b'    │
│  c ─────────────────────────────────────────────────► c'    │
│  d ────────────────────────────[+T1]────────────────► d'    │
│  e ─────────────────────────────────────────────────► e'    │
│  f ─────────────────────────────────────────────────► f'    │
│  g ─────────────────────────────────────────────────► g'    │
│  h ──[+Σ1(e)+Ch(e,f,g)+K+W]─────────────────────────► h'    │
│                                                             │
│  T1 = h + Σ1(e) + Ch(e,f,g) + K[i] + W[i]                   │
│  T2 = Σ0(a) + Maj(a,b,c)                                    │
│                                                             │
│  h' = g                                                     │
│  g' = f                                                     │
│  f' = e                                                     │
│  e' = d + T1                                                │
│  d' = c                                                     │
│  c' = b                                                     │
│  b' = a                                                     │
│  a' = T1 + T2                                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```
## Приклад виконання

### Крок 1. Структура програми

```cpp
#include <iostream>
#include <cstdint>
#include <cstring>
#include <iomanip>
#include <sstream>

// Функції обертання вправо
#define ROTR(x, n) (((x) >> (n)) | ((x) << (32 - (n))))

// Логічні функції SHA-256
#define Ch(x, y, z)  (((x) & (y)) ^ (~(x) & (z)))
#define Maj(x, y, z) (((x) & (y)) ^ ((x) & (z)) ^ ((y) & (z)))
#define Sigma0(x)    (ROTR(x, 2) ^ ROTR(x, 13) ^ ROTR(x, 22))
#define Sigma1(x)    (ROTR(x, 6) ^ ROTR(x, 11) ^ ROTR(x, 25))
#define sigma0(x)    (ROTR(x, 7) ^ ROTR(x, 18) ^ ((x) >> 3))
#define sigma1(x)    (ROTR(x, 17) ^ ROTR(x, 19) ^ ((x) >> 10))

class SHA256 {
private:
    uint32_t H[8];  // Стан хешу
    uint8_t buffer[64];  // Буфер для неповного блоку
    uint64_t bitLength;  // Довжина повідомлення в бітах
    uint32_t bufferLength;  // Заповнення буфера

    // Раундові константи K
    static const uint32_t K[64];

    void processBlock(const uint8_t* block);

public:
    SHA256();
    void update(const uint8_t* data, size_t length);
    std::string finalize();
};
```
### Крок 2. Ініціалізація констант

```cpp
// Початкові значення H
SHA256::SHA256() {
    H[0] = 0x6a09e667;
    H[1] = 0xbb67ae85;
    H[2] = 0x3c6ef372;
    H[3] = 0xa54ff53a;
    H[4] = 0x510e527f;
    H[5] = 0x9b05688c;
    H[6] = 0x1f83d9ab;
    H[7] = 0x5be0cd19;
    bitLength = 0;
    bufferLength = 0;
}

// Раундові константи K (перші 64)
const uint32_t SHA256::K[64] = {
    0x428a2f98, 0x71374491, 0xb5c0fbcf, 0xe9b5dba5,
    0x3956c25b, 0x59f111f1, 0x923f82a4, 0xab1c5ed5,
    0xd807aa98, 0x12835b01, 0x243185be, 0x550c7dc3,
    0x72be5d74, 0x80deb1fe, 0x9bdc06a7, 0xc19bf174,
    0xe49b69c1, 0xefbe4786, 0x0fc19dc6, 0x240ca1cc,
    0x2de92c6f, 0x4a7484aa, 0x5cb0a9dc, 0x76f988da,
    0x983e5152, 0xa831c66d, 0xb00327c8, 0xbf597fc7,
    0xc6e00bf3, 0xd5a79147, 0x06ca6351, 0x14292967,
    0x27b70a85, 0x2e1b2138, 0x4d2c6dfc, 0x53380d13,
    0x650a7354, 0x766a0abb, 0x81c2c92e, 0x92722c85,
    0xa2bfe8a1, 0xa81a664b, 0xc24b8b70, 0xc76c51a3,
    0xd192e819, 0xd6990624, 0xf40e3585, 0x106aa070,
    0x19a4c116, 0x1e376c08, 0x2748774c, 0x34b0bcb5,
    0x391c0cb3, 0x4ed8aa4a, 0x5b9cca4f, 0x682e6ff3,
    0x748f82ee, 0x78a5636f, 0x84c87814, 0x8cc70208,
    0x90befffa, 0xa4506ceb, 0xbef9a3f7, 0xc67178f2
};
```
### Крок 3. Обробка одного блоку (64 раунди)

```cpp
void SHA256::processBlock(const uint8_t* block) {
    uint32_t W[64];  // Розширений масив слів
    uint32_t a, b, c, d, e, f, g, h;

    // Перші 16 слів — з блоку (big-endian)
    for (int i = 0; i < 16; i++) {
        W[i] = (block[i*4] << 24) | (block[i*4+1] << 16) |
               (block[i*4+2] << 8) | block[i*4+3];
    }

    // Розширення до 64 слів
    for (int i = 16; i < 64; i++) {
        W[i] = sigma1(W[i-2]) + W[i-7] + sigma0(W[i-15]) + W[i-16];
    }

    // Ініціалізація робочих змінних
    a = H[0]; b = H[1]; c = H[2]; d = H[3];
    e = H[4]; f = H[5]; g = H[6]; h = H[7];

    // 64 раунди
    for (int i = 0; i < 64; i++) {
        uint32_t T1 = h + Sigma1(e) + Ch(e,f,g) + K[i] + W[i];
        uint32_t T2 = Sigma0(a) + Maj(a,b,c);
        h = g;
        g = f;
        f = e;
        e = d + T1;
        d = c;
        c = b;
        b = a;
        a = T1 + T2;
    }

    // Додавання до поточного хешу
    H[0] += a; H[1] += b; H[2] += c; H[3] += d;
    H[4] += e; H[5] += f; H[6] += g; H[7] += h;
}
```
### Крок 4. Доповнення та фіналізація

```cpp
void SHA256::update(const uint8_t* data, size_t length) {
    for (size_t i = 0; i < length; i++) {
        buffer[bufferLength++] = data[i];
        if (bufferLength == 64) {
            processBlock(buffer);
            bitLength += 512;
            bufferLength = 0;
        }
    }
}

std::string SHA256::finalize() {
    // Додати довжину оброблених бітів + залишок у буфері
    bitLength += bufferLength * 8;

    // Padding: додати біт 1
    buffer[bufferLength++] = 0x80;

    // Якщо не вистачає місця для довжини (8 байт), обробити блок
    if (bufferLength > 56) {
        while (bufferLength < 64) buffer[bufferLength++] = 0;
        processBlock(buffer);
        bufferLength = 0;
    }

    // Заповнити нулями до позиції 56
    while (bufferLength < 56) buffer[bufferLength++] = 0;

    // Додати довжину (big-endian, 64 біти)
    for (int i = 7; i >= 0; i--) {
        buffer[bufferLength++] = (bitLength >> (i * 8)) & 0xff;
    }

    processBlock(buffer);

    // Формування результату
    std::stringstream ss;
    for (int i = 0; i < 8; i++) {
        ss << std::hex << std::setfill('0') << std::setw(8) << H[i];
    }
    return ss.str();
}
```
### Крок 5. Тестування

```cpp
std::string sha256(const std::string& message) {
    SHA256 hasher;
    hasher.update(reinterpret_cast<const uint8_t*>(message.c_str()),
                  message.length());
    return hasher.finalize();
}

// Той самий хеш, але 32 байтами замість 64 шістнадцяткових символів.
// Потрібен там, де хеш є не текстом для показу, а даними: ключем,
// вкладеним повідомленням, аргументом HMAC (лабораторна робота №10).
std::vector<unsigned char> sha256Raw(const std::string& message) {
    std::string hex = sha256(message);
    std::vector<unsigned char> raw(32);

    for (int i = 0; i < 32; i++) {
        raw[i] = static_cast<unsigned char>(
            std::stoi(hex.substr(i * 2, 2), nullptr, 16));
    }

    return raw;
}

int main() {
    // Тестові вектори NIST
    std::cout << "=== Тестові вектори SHA-256 ===\n\n";

    // Тест 1: порожній рядок
    std::string test1 = "";
    std::cout << "Вхід: (порожній рядок)\n";
    std::string expect1 =
        "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855";
    std::cout << "Хеш: " << sha256(test1) << "\n";
    std::cout << "Очік: " << expect1 << "\n\n";

    // Тест 2: "abc"
    std::string test2 = "abc";
    std::cout << "Вхід: \"abc\"\n";
    std::string expect2 =
        "ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad";
    std::cout << "Хеш: " << sha256(test2) << "\n";
    std::cout << "Очік: " << expect2 << "\n\n";

    // Тест 3: довге повідомлення (448 біт)
    std::string test3 = "abcdbcdecdefdefgefghfghighijhijkijkljklmklmnlmnomnopnopq";
    std::cout << "Вхід: \"" << test3 << "\"\n";
    std::string expect3 =
        "248d6a61d20638b8e5c026930c3e6039a33ce45964ff2167f6ecedd419db06c1";
    std::cout << "Хеш: " << sha256(test3) << "\n";
    std::cout << "Очік: " << expect3 << "\n\n";

    // Демонстрація лавинного ефекту
    std::cout << "=== Лавинний ефект ===\n\n";
    std::string msg1 = "Hello";
    std::string msg2 = "hello";  // Відрізняється на 1 біт

    std::cout << "\"Hello\": " << sha256(msg1) << "\n";
    std::cout << "\"hello\": " << sha256(msg2) << "\n";
    std::cout << "(зміна 1 біта — повністю інший хеш)\n";

    return 0;
}
```
**Очікуваний результат:**

```
=== Тестові вектори SHA-256 ===

Вхід: (порожній рядок)
Хеш: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
Очік: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855

Вхід: "abc"
Хеш: ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad
Очік: ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad

Вхід: "abcdbcdecdefdefgefghfghighijhijkijkljklmklmnlmnomnopnopq"
Хеш: 248d6a61d20638b8e5c026930c3e6039a33ce45964ff2167f6ecedd419db06c1
Очік: 248d6a61d20638b8e5c026930c3e6039a33ce45964ff2167f6ecedd419db06c1

=== Лавинний ефект ===

"Hello": 185f8db32271fe25f561a6fc938b2e264306ec304eda518007d1764826381969
"hello": 2cf24dba5fb0a30e26e83b2ac5b9e29e1b161e5c1fa7425e73043362938b9824
(зміна 1 біта — повністю інший хеш)
```
## Порядок виконання роботи

1. Отримати в викладача номер індивідуального варіанта.

2. Реалізувати клас SHA256 згідно з наведеним алгоритмом. Переконатися, що використовуються правильні типи даних (uint32_t для 32-бітних слів).

3. Реалізувати всі шість логічних функцій (Ch, Maj, Σ0, Σ1, σ0, σ1).

4. Реалізувати процедуру доповнення (padding) повідомлення. Перевірити на короткому тексті "abc".

5. Реалізувати функцію обробки блоку (64 раунди). Звернути увагу на правильний порядок байтів (big-endian).

6. Перевірити реалізацію на трьох офіційних тестових векторах NIST.

7. Продемонструвати лавинний ефект: обчислити хеші двох повідомлень, що відрізняються на один символ. Порахувати, скільки біт змінилося у хеші.

8. Виконати індивідуальне завдання згідно з варіантом.

9. Оформити звіт та зробити висновок.

## Вимоги до звіту

Звіт оформлюється на бланку встановленого зразка і має містити:

- тему, мету та обладнання;
- постановку задачі за індивідуальним варіантом;
- повний код програми з коментарями;
- результати тестування на офіційних векторах;
- демонстрацію лавинного ефекту з підрахунком змінених біт;
- результат виконання індивідуального завдання;
- висновок про особливості алгоритму SHA-256.

## Варіанти індивідуальних завдань

| № | Завдання |
|----|----|
| 1 | Обчислити SHA-256 для свого імені та прізвища (латиницею). Визначити, скільки раундів було виконано. |
| 2 | Порівняти хеші "Password" і "Passw0rd" (заміна o на 0). Підрахувати кількість різних біт. |
| 3 | Обчислити SHA-256 для чисел від 1 до 10 (як рядків). Чи є закономірність у результатах? |
| 4 | Реалізувати функцію перевірки пароля з використанням хешування. |
| 5 | Обчислити хеш файлу (прочитати побайтно). Перевірити, що навіть маленький файл дає 256-бітний хеш. |
| 6 | Додати вивід проміжних значень H після кожного блоку. Показати на двоблоковому повідомленні. |
| 7 | Порівняти час обчислення хешу для повідомлень 1 КБ, 10 КБ, 100 КБ. Чи лінійна залежність? |
| 8 | Реалізувати HMAC-SHA256 для автентифікації повідомлень. |
| 9 | Знайти рядок, хеш якого починається з "00" (два нульових символи). Скільки спроб знадобилось? |
| 10 | Обчислити подвійний хеш SHA256(SHA256(x)) для свого варіанта. Пояснити, де це використовується (Bitcoin). |
| 11 | Реалізувати хешування з "сіллю": hash(salt + password). Продемонструвати захист від rainbow tables. |
| 12 | Порівняти SHA-256 і MD5 на однакових даних. Який хеш довший? Який швидший? |
| 13 | Обчислити SHA-256 для рядка з 1000 символів 'a'. Перевірити час виконання. |
| 14 | Реалізувати потокове хешування великого файлу (читання блоками по 64 байти). |
| 15 | Знайти два різних рядки з однаковими першими 4 символами хешу (часткова колізія). |
| 16 | Обчислити Merkle root для 4 транзакцій (як у Bitcoin): hash(hash(A+B) + hash(C+D)). |
| 17 | Реалізувати SHA-224 (відрізняється початковими значеннями і довжиною виходу). |
| 18 | Продемонструвати, що hash(A \|\| B) ≠ hash(B \|\| A) — порядок конкатенації має значення. |
| 19 | Обчислити хеш-ланцюжок: h1 = SHA256("start"), h2 = SHA256(h1), …, h10 = SHA256(h9). |
| 20 | Реалізувати простий proof-of-work: знайти число nonce, щоб hash("block" + nonce) починався з "000". |

## Контрольні запитання

Запитання згруповано за рівнями навчальних досягнень. Для позитивної оцінки студент має відповісти на запитання середнього рівня, оцінка «добре» потребує відповідей достатнього рівня, оцінка «відмінно» — високого.

### Середній рівень (репродуктивний)

1. Який розмір виводу має SHA-256? Скільки це байтів і шістнадцяткових символів?
2. Скільки раундів виконується під час обробки одного блоку?
3. Який розмір блоку обробляє SHA-256?
4. Для чого потрібне доповнення (padding) повідомлення?
5. Що таке лавинний ефект?
6. Що таке колізія хеш-функції?
7. Назвіть три властивості криптографічної хеш-функції.
8. Де застосовуються хеш-функції в реальних системах?

### Достатній рівень (конструктивно-варіативний)

1. Чому хеш-функція незворотна, хоча складається з оборотних операцій?
2. Чим SHA-256 відрізняється від MD5 і чому MD5 більше не вважають безпечним?
3. Чи можлива колізія для SHA-256 теоретично? Оцініть складність її пошуку.
4. Що таке HMAC і чим він відрізняється від простого хешування зі склеюванням ключа?
5. Чому хеші паролів зберігають із «сіллю» і що саме вона унеможливлює?
6. Чому для зберігання паролів рекомендують повільні функції, а не SHA-256?
7. Як перевірити реалізацію на офіційних тестових векторах NIST?
8. Як хеш-функції використовуються в блокчейні?

### Високий рівень (творчий)

1. Поясніть, чому довжина повідомлення обов'язково входить у доповнення, і яку атаку це унеможливлює.
2. Оцініть, скільки спроб у середньому потрібно для пошуку колізії SHA-256 за парадоксом днів народження, і що це означає практично.
3. Запропонуйте схему перевірки цілісності великого архіву файлів на основі хешування. Обґрунтуйте вибір структури.
4. Сформулюйте критерії, за якими обирають хеш-функцію для нової системи, і поясніть, чому «найновіша» не завжди означає «найкраща».

## Критерії оцінювання

Робота оцінюється за загальними критеріями курсу — див. [Критерії оцінювання лабораторних робіт](#/02-software-security-methods/grading).

## Cheat Sheet

```
┌───────────────────────────────────────────────────────────────────────┐
│                    ШПАРГАЛКА: SHA-256                                 │
├───────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ПАРАМЕТРИ:                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │ Розмір блоку    = 512 біт (64 байти)                            │  │
│  │ Розмір виходу   = 256 біт (32 байти, 64 hex-символи)            │  │
│  │ Кількість раундів = 64                                          │  │
│  │ Розмір слова    = 32 біти                                       │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  ФУНКЦІЇ:                                                             │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │ Ch(x,y,z)  = (x & y) ^ (~x & z)       "вибір"                   │  │
│  │ Maj(x,y,z) = (x & y) ^ (x & z) ^ (y & z)  "більшість"           │  │
│  │ Σ0(x) = ROTR(x,2) ^ ROTR(x,13) ^ ROTR(x,22)                     │  │
│  │ Σ1(x) = ROTR(x,6) ^ ROTR(x,11) ^ ROTR(x,25)                     │  │
│  │ σ0(x) = ROTR(x,7) ^ ROTR(x,18) ^ (x >> 3)                       │  │
│  │ σ1(x) = ROTR(x,17) ^ ROTR(x,19) ^ (x >> 10)                     │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  ТЕСТОВІ ВЕКТОРИ:                                                     │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │ ""    → e3b0c442...7852b855                                     │  │
│  │ "abc" → ba7816bf...f20015ad                                     │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```
