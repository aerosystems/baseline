# Building

1. Download zig 0.16.0 compiler from <https://ziglang.org/download/> for target os.
2. Unpack somwhere to PATH
3. Cd into this directory and run `zig build run` (`zig build run -- -h` for cli mode help)
